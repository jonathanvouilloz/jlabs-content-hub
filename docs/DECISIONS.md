# Decisions techniques

| Date | Decision | Contexte | Alternatives considerees |
|------|----------|----------|--------------------------|
| 2026-03-31 | SvelteKit + Svelte 5 (runes) | Stack habituel Jon Labs, SSR + API routes natifs | Next.js (trop lourd), Astro (pas assez dynamique) |
| 2026-03-31 | Turso (libSQL) + Drizzle ORM | Leger, edge-compatible, gratuit pour ce volume, deja maitrise | Supabase (overkill), PlanetScale (plus cher) |
| 2026-03-31 | Better Auth | Deja utilise sur d'autres projets Jon Labs, magic link + email/password | Auth.js (instable), Lucia (deprecated) |
| 2026-03-31 | Skeleton UI + Tailwind | Composants Svelte natifs, theme customisable, support dark mode | shadcn-svelte (moins mature), DaisyUI (moins flexible) |
| 2026-03-31 | GitHub = backup, DB = source de verite | Le repo GitHub existant contenait deja du contenu, mais le systeme de fichiers ne scale pas | GitHub comme source de verite (fragile avec >3 projets) |
| 2026-03-31 | Structure GitHub : {projet}/{type}/{YYYY}/{MM}/ | Avec 8-12 articles + posts par mois par projet, la separation par mois garde les dossiers navigables | Flat par type (trop de fichiers), par statut draft/published (supprime) |
| 2026-03-31 | 3 mecanismes auth : API key, client token, session admin | Chaque use case a besoin d'un niveau de securite different | Token unique pour tout (pas assez granulaire) |
| 2026-03-31 | createId() avec crypto.randomBytes | Simple, pas de dependance externe, suffisant pour le volume | cuid2 (dependance inutile), uuid (trop long) |
| 2026-03-31 | Skeleton UI v4 (pas v2) | v4.13 est la version actuelle compatible Svelte 5 | v2 (deprecated, pas Svelte 5) |
| 2026-03-31 | Layout custom (pas AppShell) | Skeleton UI v4 n'a plus AppShell/AppRail, layout custom avec Tailwind | Chercher un autre design system (inutile, Tailwind suffit) |
| 2026-03-31 | Better Auth auto-detection env vars | BETTER_AUTH_SECRET et BETTER_AUTH_URL detectes automatiquement | Passer manuellement via config (redondant) |
| 2026-03-31 | Endpoint /api/setup one-shot | Creer le premier admin sans seed script, bloque si user existe deja | Script CLI (plus complexe a executer en prod) |
| 2026-03-31 | Tailwind v4 Vite plugin (pas PostCSS) | Plus simple, pas besoin de postcss.config.js ni tailwind.config.js | PostCSS plugin (plus de config) |
| 2026-04-27 | csrf.checkOrigin desactive (svelte.config.js) | Les skills clients postent du multipart server-to-server vers /api/blob/upload depuis n'importe ou. Toutes les routes /api/* ont leur propre auth (Bearer API key ou session Better Auth), Better Auth gere la CSRF de /api/auth/*. checkOrigin est deprecie (warning svelte-check) au profit de trustedOrigins, mais trustedOrigins ne couvre pas le cas "pas de header Origin" (script Node fetch). A migrer le jour ou checkOrigin sera retire : ajouter un hook handle qui patch event.request avec un Origin synthetique pour les routes /api/* validees Bearer. | trustedOrigins liste fermee (impractical, le script Node n'envoie pas de header Origin par defaut), endpoint alternatif non-multipart (oblige a tout reencoder cote skill) |
| 2026-04-27 | Cle d'unicite GMB splittes = position 1-indexed | Re-POST d'un calendrier doit etre idempotent meme si les titres changent. Le slug `${batchSlug}-${i+1}` est stable, simple, sans dependance sur un champ payload. Pas de cleanup auto des orphelins (suppression manuelle via UI si calendrier reduit). | source_article_slug (casse pour les events), external_id explicite (oblige a modifier /gmb-generate), slugify(title) (instable - source du bug) |
| 2026-04-27 | Crons cale sur l'heure CEST (ete) | Vercel crons tournent en UTC sans option timezone. Schedule `0 7 * * *` = 9h CEST (avril-octobre) / 8h CET (novembre-mars). Le digest hebdo passe a `0 6 * * 1` = 8h CEST / 7h CET lundi. En hiver, les posts programmes a 9h locale (08h UTC) seront publies au cron de J+1 (decalage 1 jour, acceptable car le cron est idempotent et les posts ne sont pas time-critical a la minute pres). | Hardcoder UTC (incoherent pour le user), 2 crons saisonniers (anti-pattern, switch manuel 2x/an), cron toutes les heures avec dedup (gaspillage de quotas) |
| 2026-06-24 | Pivot produit "Content Hub" → **jokiSEO** (cockpit SEO & presence locale) | Apres 3 mois d'usage : la valeur recurrente est dans le monitoring (positions, GMB, avis, indexation), pas la production de contenu. La surface Articles/LinkedIn fait du bruit. Recentrage sur SEO + presence locale + livrables client. Voir PRD-jokiseo.md. | Garder le hub generaliste (dilue la valeur), repartir de zero (rejete, voir ligne suivante) |
| 2026-06-24 | **Refactor in-place, pas de rewrite** | Les moteurs durs (gsc-analytics 1160 l., gmb 850 l., indexing 460 l.) sont fiables et encapsulent un savoir durement acquis (gestion tokens/refresh/retry, latence GSC, normalisation URL, cannibalisation locale). Le schema est propre. Ce qui manque est ~100% surface neuve qui se branche a cote. L'outil est un daily driver → un rewrite = periode de regression inacceptable. | Rewrite from scratch (jette le dur deja resolu, reintroduit les bugs corriges, casse l'usage quotidien) |
| 2026-06-24 | **Turso conserve, pas de migration Neon** | Rien dans le scope jokiSEO n'exige Postgres. A 6 projets, libSQL/SQLite tient des annees. Le seul irritant (limite ~999 params/insert) est deja contourne par le chunking de 200 lignes. Migrer = reecrire dialecte SQL/Drizzle + Better Auth + data live + re-tester, pour zero feature gagnee. Si le volume time-series explose plus tard (geo-grid + rank quotidien) → ajouter un store analytique dedie, pas tout migrer. | Migration Neon/Postgres (risque pur, aucun gain fonctionnel a cette echelle), DB analytique d'emblee (premature) |
| 2026-06-24 | **DataForSEO = fournisseur SEO externe unique** | Deja utilise cote skill `/seo-keywords`. Couvre rang SERP reel, geo-grid local, backlinks et AI Overviews avec une seule integration. Centralise dans une couche `providers/` (epic 25) pour eviter la duplication d'appels. | Ahrefs (meilleur backlinks mais 2e fournisseur + cout), multi-fournisseurs des le debut (complexite prematuree), scraping maison (fragile, anti-pattern) |
| 2026-07-20 | **~~Turso conserve~~ → INVERSE : migration Neon (Postgres), 1 base / multi-schema** | La decision du 2026-06-24 (garder Turso) est annulee. Raison : tout l'ecosysteme Jonathan est deja sur **Neon** ; unifier le fournisseur DB simplifie l'exploitation. Surtout, seo-stats et **invoices** partagent les memes clients → une **seule base Neon** (`neondb`) avec **3 schemas Postgres** `core` / `invoices` / `seostats` permet une jointure SQL directe. `core.entities` = registre canonique des slugs (loi noyau n°3), possede par invoices ; seo-stats le reference en FK lecture-seule. Refactor code fait + commite (branche `feat/neon`, typecheck 0 err). Reste : migration des donnees Turso → Neon (voir [NEON-MIGRATION.md](NEON-MIGRATION.md) Phase 4). | Garder Turso (ecosysteme fragmente, pas de jointure cross-app clients), 2 bases Neon separees (perd la jointure client↔projet), Supabase (2e fournisseur) |
| 2026-07-20 | **Liaison client↔projet par SLUG via `core.entities`, pas de FK directe** | Au plus simple et aligne loi n°3 « un slug, partout ». Un client facture (`invoices.clients.slug`) et un projet SEO (`seostats.projects.slug`) se rejoignent parce qu'ils **pointent le meme slug canonique** dans `core.entities`. `core.entities` reste minimal (slug + display_name + created_at). Recoupement reel : exact `barberconcept` ; a canonicaliser `bis-repetita`/`bisrepetita` ; SEO seul `jonlabs`/`physiopommier`/`spinlink`/`wildcat` ; facturation seule `bc-chenois`/`cpv`/`swiss-dental-care`. | FK directe `projects.client_id → clients.id` (couple les 2 apps, casse pour les projets SEO sans facturation), table de jointure dediee (sur-ingenierie a cette echelle) |
| 2026-07-21 | **Pivot ~~jokiSEO~~ → seo-stats, cockpit agentique de monitoring** | Le pivot jokiSEO (2026-06-24) est absorbe et depasse par une cible plus ambitieuse : deleguer 90% du monitoring SEO/presence-locale recurrent a des agents IA, avec validation humaine des actions sensibles. Primitive centrale = le **finding** (fait persistant, fingerprint stable). Chaine : providers → observations → detectors → findings → agent → proposals → approval → execution. Source de verite : [SPEC.md](SPEC.md) ; execution : [BACKLOG.md](BACKLOG.md). Refactor in-place conserve. | Rester sur jokiSEO (surface de dashboard sans delegation, valeur plafonnee), rewrite complet (jette le dur GSC/GMB deja resolu) |
| 2026-07-21 | **UI cockpit neuve from-scratch, backend garde tel quel** | La reconstruction agentique coupe entre UI et backend, pas entre neuf et ancien repo. Backend conserve INTACT : 71 endpoints `/api/*`, schema Neon 29 tables (migre + verifie Phase 4), integrations GSC/GMB/LinkedIn/OAuth, Better Auth, crons — c'est le moat, la SPEC construit dessus (§1.1). L'UI, elle, est repensee a neuf car le paradigme change (dashboard de production → cockpit de lecture/validation/supervision) : nouveau groupe de routes `(cockpit)/` (layout + inbox findings + validation + supervision) qui CONSOMME les endpoints existants + les nouveaux du BACKLOG. L'ancien `(app)/` est gele puis retire une fois le cockpit a parite. Stack UI conservee (SvelteKit 2 / Svelte 5 runes / Tailwind v4 / Skeleton v4 / lucide) : deja moderne, aucune raison de migrer. | Reprise incrementale des 26 pages (traine la dette visuelle + ancien paradigme pendant des semaines, hybride), greenfield total nouveau repo (refait OAuth/GSC/GMB/auth/crons/schema pour zero gain, des mois de risque sur un daily driver), changer de design system vers shadcn-svelte (cout de migration sans benefice, Skeleton v4 fait le job) |
| 2026-07-21 | **Config runtime au boot log-only, fail-fast delegue a `requireEnv`** | Un cold-start serverless ne doit pas tomber sur une var optionnelle absente. `validateStartup()` (config.ts) LOG un rapport de presence par role sans throw ; l'exigence stricte est appliquee au point d'usage via `requireEnv(name, feature)` quand la fonctionnalite est reellement sollicitee. Flags de migration (flags.ts) OFF par defaut, un flag route le comportement sans jamais effacer de donnee. | Throw au boot sur toute var manquante (risque de tuer le daily driver a chaque cold-start), validation dispersee au point d'usage seule (pas de vue d'ensemble ni de .env.example fiable) |
| 2026-07-21 | **IDX-008 : garde Indexing API a deux etages (flag + type)** | La Google Indexing API n'est officiellement valable que pour JobPosting/BroadcastEvent (SPEC 9.4) ; la soumission generique d'articles/pages locales est non conforme et etait declenchee auto en prod au passage `published`. Garde unique dans `indexing.ts` en amont de tout reseau : flag maitre `indexnow` (OFF par defaut pendant la migration) ET validation de type d'eligibilite. Refus = ligne d'audit `status:'blocked'`, zero quota (aucun `fetch`). Aucun appelant ne porte de type eligible aujourd'hui → toutes les soumissions generiques sont neutralisees sans toucher le schema. | Flag seul (ne bloque pas un usage non conforme si le flag est ON), validation de type seule (le feature file exigeait explicitement le flag), retrait pur du code (perd la capacite pour les futurs JobPosting/BroadcastEvent) |
| 2026-07-21 | **DATA-002 en phase expand seule, application par SQL additif** | Creer `project_integrations` + `project_projections` (SPEC 7.1/7.2) SANS backfill ni retrait des tables heritees (project_contexts, *_settings, indexing_credentials, cms_connections restent) : migrate/contract = phase suivante, sur base non vide. `resource_key` discrimine plusieurs proprietes/locations d'un provider ; projections en historique (unique `(project_id, source_hash)` + unique partiel `current`) ; garde `assertNoInlineSecret` sur payload/config, secrets via `secret_ref`. Applique par `drizzle/manual-data-002.sql` (idempotent) + script Pool, PAS `db:push` (deterministe, zero risque sur les autres tables), verifie par re-run de l'introspection (32 tables, zero derive). | Expand + backfill immediat (touche la donnee prod, allonge, secrets a mapper), `db:push` (interactif/moins deterministe sous Windows), `db:generate` (impossible sans baseline snapshot cote seo-stats) |
| 2026-07-22 | **FIND-003 : la closure d'un run de detection = la liste COMPLETE des signaux, jamais la liste ecrite** | L'auto-resolution ferme un finding qui « ne matche plus ». Or le detecteur plafonne ses ecritures (`maxCandidates=50`) alors que barberconcept produit **1310** signaux au-dessus des seuils : se fonder sur les 50 ecrits fermerait 1260 findings bien vivants. `selectOpportunities` expose donc `matched` (complet, avant troncature) a cote de `candidates` (tronque, ecrit), et la reconciliation ne lit que `matched`. Deux gardes de plus : aucune reconciliation sur un **run non autoritaire** (fenetre absente ou zero observation — un projet sans donnee ne vide pas son inbox) et **N absences consecutives** requises avant resolution (defaut 2, configurable par projet, SPEC 10.3). Tout futur detecteur qui plafonne ses ecritures doit exposer sa liste complete. | Reconcilier depuis `candidates` (ferme des findings vivants des que le plafond mord), relever `maxCandidates` a l'infini (fait exploser l'inbox et le cout d'ecriture, ne resout pas le principe), resoudre des la 1re absence (une collecte partielle suffirait a fermer toute l'inbox) |
| 2026-07-22 | **FIND-003 : le snooze tient jusqu'a son echeance, le dismiss vaut a vie** | Deux promesses faites a l'humain que la machine ne reprend jamais. **Snooze** : aucune re-detection ni aggravation ne le rompt (l'aggravation est journalisee, donc rien n'est perdu et le finding remonte a l'expiration avec son historique) — sinon un snooze ne garantit plus le silence et la severite peut osciller d'une semaine a l'autre. **Dismiss** : vaut a vie pour ce fingerprint ; `occurrence_count` continue de monter (matiere premiere de la mesure de faux positifs, FIND-010) mais seul `reopenFinding` (humain) le defait. Corollaire : la legalite du graphe 10.1 est **gardee a l'ecriture** (`canTransition` dans `transitionFinding`, qui porte aussi tous les effets de bord du cycle de vie) et l'expiration de veille a son **propre type de job** (`findings:lifecycle`) — une veille doit expirer meme les semaines sans detection, sinon le snooze devient un enterrement. | Reveil du snooze sur aggravation (le snooze ne garantit plus rien), reveil seulement sur `critical` (compromis mais seuil arbitraire), dismiss avec duree de vie 90j (bruit recurrent, oblige a re-dismisser), dismiss lie a la version du detecteur (fin mais complique la lecture de l'inbox) |
| 2026-07-22 | **JOB-002 : l'exactly-once des effets externes est un MECANISME (registre + garde), pas une convention** | « Deux executions ne produisent pas deux effets externes » (acceptation JOB-002) : apres reprise d'un job mort, le handler re-tourne. Les 2 handlers actuels sont deja idempotents (upsert par fingerprint, expireSnoozes), mais s'appuyer la-dessus ferait reposer l'acceptation sur une discipline que le premier handler a effet reel (IndexNow, publication GMB) violerait sans que rien ne le detecte. D'ou `job_effects` + `guardExternalEffect` en **claim-then-apply** : on RESERVE la cle (`pending`, unique `(project_id, effect_key)`) PUIS on applique — appliquer avant de reserver laisserait une fenetre de double effet. Un effet `failed` reste reprenable (il n'a jamais abouti), un `applied` ne se rejoue jamais. Brique attendue par IDX-007 (outbox IndexNow) et la publication GMB. | S'appuyer sur l'idempotence des handlers + documenter le contrat (zero table, mais l'acceptation repose sur une convention non verifiable), verrou applicatif type mutex (ne survit pas au crash, c'est precisement le cas a couvrir) |
| 2026-07-22 | **JOB-002 : timeout provider vs crash local se tranche par OU le fait se constate** | L'acceptation demande de distinguer les deux, mais un reaper ne peut pas interroger un process mort. La separation retenue est donc mecanique, pas devinee : le **crash se constate de l'exterieur** (bail expire → `classifyAbandonedLease` lit le rythme des battements : plus de battement bien avant l'echeance = `worker_death`, battement jusqu'au bout puis silence = `lease_stall`) ; le **timeout se constate de l'interieur**, worker vivant, via le budget de duree du runner (defaut 30 min) → `ProviderTimeout`, rejouable. La politique de retry n'est PAS reinventee : `decideAfterAbandon` delegue a `decideAfterFailure` (DATA-003). Corollaires : le signal du job n'est **pas** relie au signal d'arret gracieux (sinon l'acceptation JOB-001 « un job commence avant l'arret est mene a son terme » casse en silence), et l'`UPDATE` du reaper est garde par `lease_owner` **ET** `lease_until` observes (sans le second, une course avec un renouvellement volerait un job vivant). | Heuristique sur la duree ecoulee seule (ne distingue pas un worker mort d'un provider lent), classification fine des erreurs des maintenant (c'est JOB-003, hors perimetre), pas de distinction du tout (l'acceptation l'exige et les deux cas appellent des reponses differentes) |
| 2026-07-22 | **JOB-002 : l'historique des tentatives est un JOURNAL append-only, la page reste JOB-007** | « L'interface montre la tentative abandonnee et la reprise » : `jobs` ne portait qu'un compteur `attempts` et un `last_error_*` **ecrase a chaque reprise** — l'historique etait donc structurellement absent. `job_attempts` (1 ligne par reclamation) le retablit ; `scripts/jobs-inspect.ts` le rend lisible tout de suite, et la console d'exploitation (JOB-007, qui depend deja de JOB-002) lira ces memes lignes. **Pas d'unique `(job_id, attempt_no)`** : `releaseJob` REND la tentative (`attempts - 1`, JOB-001), donc un numero se repete legitimement — et ecraser la ligne `released` effacerait justement l'historique qu'on cree. Meme idiome que `finding_events` / `policy_promotions`. Le reaper tourne dans la boucle worker (demarrage + tours a vide) + `scripts/reap.ts` ; le cron dedie reste JOB-005. Limite assumee : **sur Vercel aucun worker ne tourne en continu**, la reprise n'a lieu qu'au prochain lancement. | Colonnes `abandoned_at`/`abandon_kind` sur `jobs` (moins cher mais l'historique reste ecrase, et JOB-003 le redemanderait), page /jobs des maintenant (empiete sur JOB-007), cron Vercel dedie (duplique ce que JOB-005 doit cadrer) |
| 2026-07-22 | **JOB-003 : la RAISON de l'erreur prime sur son statut HTTP** | Classer sur le code HTTP nu ferait l'inverse exact de ce qu'exigent les acceptations, parce que Google ne respecte pas la semantique naive : un depassement de quota arrive en **403 + `rateLimitExceeded`** (le condamner en « structurel » tuerait un job sain) et un refresh token mort arrive en **400 + `invalid_grant`** (le traiter en « permanent » masquerait une cause d'auth qui appelle une action humaine precise). `classifyJobFailure` examine donc : codes internes connus → marqueurs de quota → d'auth → permanents → **puis seulement** le statut (429 = quota, 401 = auth, **tout autre 4xx = permanent** car une erreur du client rejouee a l'identique redonne la meme erreur, 5xx = retryable). Une erreur **illisible retombe sur `retryable`** : on ne condamne jamais un job sur ce qu'on n'a pas su lire. | Classer sur le statut HTTP seul (simple mais faux sur Google, le fournisseur principal), une classe par provider (duplique la logique a chaque integration), tout retenter uniformement (l'etat d'avant JOB-003 : 5 tentatives brulees sur un 403 sans espoir) |
| 2026-07-22 | **JOB-003 : un 429 ne consomme pas de tentative, il se compte a part et sous plafond** | Un quota n'est pas une faute du job. Lui faire consommer le budget `max_attempts` signifie qu'une journee de saturation provider envoie en dead-letter des jobs parfaitement sains. La tentative est donc **rendue** (`deferJob`, meme geste que `releaseJob`) et un compteur **separe** `deferrals` s'incremente, **plafonne a 20** — sans ce plafond, un provider definitivement ferme ferait tourner le job sans fin. Le `Retry-After` du provider est un **contrat** : honore (secondes ou date HTTP, plafonne a 6 h pour qu'un en-tete deraisonnable ne parque pas un job pendant des jours), et le jitter peut l'**allonger, jamais le raboter** (repasser dessous rejoue le 429 a coup sur). | Consommer une tentative comme tout echec (simple, aucun etat neuf, mais tue des jobs sains un jour de saturation), reports illimites (boucle infinie sur un provider mort), file d'attente par provider (c'est JOB-006, qui PREVIENT le 429 la ou JOB-003 y REAGIT) |
| 2026-07-22 | **JOB-003 : le jitter vit dans la couche IO, la decision reste pure** | Le backoff de DATA-003 est deterministe, donc N jobs echoues sur le meme provider reviennent **tous a la meme seconde** (thundering herd). `applyJitter` recoit son `random` en **injection** — meme discipline que le `nonce` de `deriveWorkerId` — et **sans `random`, le delai ressort inchange** : tout le comportement deterministe de JOB-001/002 et ses tests restent valides tels quels, seuls `failJob` et le reaper fournissent `Math.random`. Corollaire de la reprise manuelle : `attempts` **repart a zero** (sinon un job repris a 5/5 remourrait au premier echec et la reprise ne serait qu'un geste), et l'acceptation « une reprise manuelle conserve l'historique des tentatives » est portee par le **journal** `job_attempts` (append-only, la reprise y ecrit sa ligne `requeued` avec auteur et raison), jamais par le compteur. | Jitter tire dans le module pur (casse la purete et la rejouabilite, toute la discipline du repo), jitter par defaut sans injection (change silencieusement le comportement des appelants existants), conserver `attempts` et relever `max_attempts` a chaque reprise (oblige a choisir un nouveau plafond a chaque fois et le job remeurt vite) |
| 2026-07-22 | **JOB-007 : un job EN COURS s'annule en lui retirant son bail, pas en tuant son worker** | Le backlog exige l'annulation, mais rien ne permet de tuer un process depuis une page web — et un worker qui meurt en plein handler laisse exactement l'etat que JOB-002 a passe une session a reparer. `cancelJob` ecrit donc `status='cancelled'` et **libere `lease_owner`** : au prochain battement (<= `leaseMs/3`, ~100 s), `renewLease` — garde par `lease_owner` ET `status='running'` — ne matche plus, le runner l'apprend et `abort()` son handler, et ses ecritures finales (`completeJob`/`failJob`/`finishAttempt`) portent les memes gardes, donc ne reecrivent rien. C'est le mecanisme de JOB-002 reutilise, pas une voie parallele (le commentaire de `renewLease` anticipait deja « job annule »). Prix assume : l'arret n'est pas immediat, et un handler qui ignore son `signal` finit son travail en cours. | Colonne `cancel_requested` sondee par le runner (2e chemin d'arret a maintenir, meme latence), refuser l'annulation d'un `running` (le cas ou l'on veut un bouton est justement le job parti sur un provider bloque), tuer le process worker (ingerable depuis le web, et recree le probleme des jobs orphelins) |
| 2026-07-22 | **JOB-007 : l'audit d'une action d'exploitation est une ligne de journal, pas un champ sur le job** | « Retry et annulation sont audites » : `jobs` n'a aucune place pour un auteur ni une raison, et `last_error_*` est ecrase a chaque reprise. Chaque action ecrit donc sa propre ligne dans `job_attempts` (append-only) — `outcome='cancelled'`/`'requeued'`, l'acteur en `worker_id` (`user:{email}`, pris **dans la session**, jamais fourni par le client), la raison en `metadata_json` —, exactement l'idiome pose par `requeueDeadJob` en JOB-003. Consequence assumee : annuler un job qui tourne produit **deux** lignes, celle de la tentative (auteur = le worker) et celle de la decision (auteur = l'humain) ; ce sont deux faits distincts et le journal etant append-only, aucun n'ecrase l'autre. Un refus n'ecrit **rien**. Corollaire : l'acceptation « aucune operation ne permet de modifier arbitrairement le payload » tient par **absence de chemin** (aucune route n'accepte `payload_json`, `type`, `priority`, `max_attempts`), pas par une validation. | Table `audit_events` dediee (une table de plus pour 2 actions, et l'historique du job se lirait a deux endroits), colonnes `cancelled_by`/`cancel_reason` sur `jobs` (ecrasees a la reprise suivante, donc pas un audit), journaliser dans les logs applicatifs (non requetable, perdu au redeploiement) |
| 2026-07-22 | **JOB-007 : les horodatages restent affiches en UTC, comme ils sont stockes** | Les colonnes temporelles sont des `text` UTC (`timestamps.ts`), et `jobs-inspect.ts` les rend telles quelles. Convertir en Europe/Zurich dans la console ferait exister **deux lectures d'un meme instant selon l'outil consulte** — precisement la famille de bug qui a coute le correctif transverse de FIND-001/JOB-001 (deux formats dans une meme colonne). L'interface affiche donc l'heure stockee et **le dit** (« UTC » a cote des valeurs). Corollaire pratique : les libelles et formats vivent dans `$lib/utils/job-format.ts`, consommes par la CLI **et** par les pages (une page Svelte ne peut pas importer `$lib/server`), pour qu'un `deferred` ne soit jamais « reporte » d'un cote et « echoue » de l'autre. | Convertir en Europe/Zurich cote page (plus confortable, mais l'heure d'un meme job differe entre CLI et console — 2 h d'ecart en ete), stocker un fuseau par ligne (change le schema pour un besoin d'affichage), afficher les deux (bruit dans une table dense) |
| 2026-07-22 | **JOB-005 : un creneau se nomme par son heure LOCALE, jamais par son instant UTC** | Toute la garantie anti-doublon du scheduler tient dans ce choix. La cle d'idempotence porte `2026-07-20T09:00` (Europe/Zurich), pas l'instant : le lundi 09:00 metier s'ecrit alors 08:00 UTC en hiver et 07:00 UTC en ete **sans changer de cle logique**, et le retour a l'heure d'hiver ne peut pas produire deux jobs puisque 02:30 local n'existe qu'une fois au calendrier — meme quand l'instant correspondant, lui, se presente deux fois. Corollaire : rejouer un tick, redemarrer a 09:00 et rattraper un creneau manque sont **la meme operation**, sans effet la deuxieme fois. La conversion locale→instant teste les DEUX offsets en vigueur autour du creneau et garde ceux qui se relisent : deux valides = creneau ambigu (on prend le premier, convention `compatible` de Temporal), zero valide = creneau inexistant (29 mars 02:30) qui **glisse a 03:30** plutot que d'etre escamote. | Nommer le creneau par son instant UTC (deux creneaux distincts pour un meme lundi 09:00 selon la saison, et un seul le jour du retour a l'heure d'hiver), une passe unique de conversion (fausse aux bordieres : l'offset a appliquer n'est pas celui de l'instant qu'on calcule), une lib (date-fns-tz/Temporal : `Intl` fait deja le travail, base ICU du runtime, zero dependance) |
| 2026-07-22 | **JOB-005 : aucune table de planification — l'etat, c'est la cle d'idempotence** | « Un restart a 09:00 ne cree qu'un run logique » n'est PAS garde par un `last_fired_at` : un tel champ peut diverger du calcul, se perdre au redeploiement, ou mentir apres une restauration. Il l'est par l'unique `(project_id, idempotency_key)` **deja en base** (`jobs_idempotency_unique`, `monitoring_runs_idempotency_unique`). La « prochaine execution par projet » est donc **calculee** a la demande, ce qui la rend structurellement incapable de contredire ce que le tick fera. Zero DDL (57 tables, zero derive). La config par projet passe par `project_projections.payload.schedules`, meme idiome tolerant que les seuils detecteurs (override corrompu → defauts, jamais de planification muette). | Table `job_schedules` (etat editable en UI, mais un second endroit ou la verite peut diverger du calcul, plus DDL + script apply), fichier de config versionne (pas de reglage par projet sans redeploiement) |
| 2026-07-22 | **JOB-005 : le cron perd sa semantique metier — il bat, le code sait l'heure qu'il est** | Revise la decision du 2026-04-27 (« cron toutes les heures avec dedup = gaspillage de quotas »), qui calait les crons sur l'heure CEST et acceptait un decalage d'un jour en hiver. JOB-005 exige d'etre exact sur les DEUX bascules DST : un creneau metier fixe demanderait deux entrees `vercel.json` saisonnieres a permuter deux fois l'an a la main (anti-pattern deja ecarte en avril). Le tick `0 * * * *` n'a donc plus aucune semantique metier, et il ne fait plus que dedupliquer : il sert **aussi** de worker et de reaper. C'est ce qui leve enfin « sur Vercel aucun worker permanent », repete comme piege depuis JOB-002 — un scheduler seul aurait rempli une file que personne ne vide, avec l'illusion que ca tourne. Le drain est borne (`once`, 25 jobs, budget 240 s sous `maxDuration: 300`) : la plateforme tue sans repasser par les `finally`. | Deux crons saisonniers (switch manuel 2x/an), cron unique cale sur une saison (decalage d'une heure l'autre moitie de l'annee), scheduler et worker sur deux routes distinctes (ordre non garanti, deux entrees a surveiller), worker externe permanent (infra a payer et a exploiter pour ~1 min de travail par heure) |
| 2026-07-22 | **JOB-005 : un run se conclut a l'issue TERMINALE de ses jobs, pas a chaque tentative** | Bug trouve en verifiant : un run planifie restait `queued` **a vie**, parce que `classifyRunOutcome` derive le statut d'un run de ses STEPS et que personne n'en ecrivait pour un job de queue — la supervision aurait montre des runs eternellement en attente au-dessus de jobs reussis. `concludeRunStep` ecrit le step et recalcule le run, mais **seulement** quand le job a fini son histoire (reussi, ou mort apres epuisement) : un job encore rejouable n'a rien conclu, et lui ecrire un `failed` ferait basculer son run en echec avant la fin de la partie. Ecriture non bloquante (comme la passe de reaper) : l'execution du job fait foi, sa comptabilite de run ne doit jamais pouvoir la faire echouer apres coup. Limite assumee : un run dont un job est en retry reste `queued` — le suivi fin d'un run **en cours** est l'affaire de JOB-004 (DAG + statut `partial`). | Ecrire un step a chaque tentative (le run passe `failed` puis `partial` au gre des retries, il raconte l'histoire du retry au lieu du resultat), step `queued` a la planification (le run parait `running` alors que rien ne tourne), ne pas ouvrir de run du tout (perd la tracabilite creneau→job, et `monitoring_runs` etait fait pour ca) |
| 2026-07-22 | **AGT-000 : le payload d'une proposition ne contient AUCUNE mesure — c'est lui qui porte la dedup** | `createProposal` deduplique sur `payload_hash`. Y mettre la position (7.4 cette semaine, 7.1 la suivante) ou les impressions produirait un hash neuf a chaque run hebdomadaire, donc une proposition neuve : **l'inbox doublerait de volume toutes les semaines**, sans erreur, sans log, sans test rouge. Le payload ne porte donc que l'IDENTITE et l'INTENTION (action, cible, finding, fingerprint, skill) ; les chiffres du moment vivent dans `rationale`/`expected_impact` — **hors du hash**, donc rafraichis a chaque run par le `onConflictDoUpdate` — et dans `input_hashes_json`. Corollaire : `createProposal` a du etre etendu pour rafraichir ces champs non hashes, sinon une proposition afficherait eternellement les mesures de sa premiere semaine ; l'approbation liee reste valide puisque le hash n'a pas bouge (§12.2). Un test dedie garde l'invariant : memes findings, mesures differentes → payload identique. | Inclure les mesures et deduper sur `(projet, finding, action)` seul (perd la distinction « la situation a change » vs « rien n'a bouge »), arrondir les mesures avant hachage (repousse le probleme au premier franchissement de palier), comparer les payloads en SQL a l'ecriture (refait a la main ce que l'unique fait deja) |
| 2026-07-22 | **AGT-000 : le niveau d'approbation sort d'une table figee, jamais d'un calcul ni d'un modele** | La SPEC §12.1 donne les niveaux par EXEMPLES (« plan de refresh » = L2, « modification contenu » = L3, « 301, canonical, suppression, desindexation » = L4) : ils sont recopies tels quels dans `APPROVAL_LEVEL_BY_ACTION`, et un `action_type` inconnu rend `null` (donc aucune proposition) au lieu d'un defaut permissif. Raison de fond : si un modele — ou meme une heuristique — derivait le niveau, alors le NIVEAU D'AUTORISATION deviendrait lui-meme negociable, c'est-a-dire qu'un agent pourrait s'accorder un L4. C'est exactement l'invariant que `canActorApprove` existe pour interdire. Meme logique pour le choix de l'action : `keyword_opportunity` rend UNE action tranchee par la position (<= 10 → `meta_rewrite` L3 ; > 10 → `refresh_plan` L2), pas deux, et un type de finding sans correspondance ne produit RIEN (compte en `withoutAction`). L'IA viendra redigier le rationale et regrouper les findings correles, sur une structure deja gouvernee (SPEC §3.3). | Deriver le niveau du risque calcule (le risque devient la variable qui ouvre les droits), laisser un LLM proposer action+niveau (l'agent choisit son propre plafond), un niveau par defaut pour les types inconnus (une action non prevue devient approuvable) |
| 2026-07-22 | **AGT-000 : le risque vient de l'ACTION, module par ce qu'il y a a perdre — pas de la gravite du finding** | Le plan prevoyait `deriveRiskLevel(actionType, severity)`. Ecarte : un finding critique ne rend pas plus risque d'ecrire un brouillon — ce serait dire « cette action est plus dangereuse parce que le probleme est pire », ce qui est l'inverse. Le risque est intrinseque au niveau (L2 `low`, L3 `medium`, L4 `high`), avec une seule modulation qui a un sens reel : reecrire le title d'une page a 0 clic ne peut rien faire perdre, le reecrire sur une page a 55 clics/semaine expose un acquis → `medium` monte a `high` au-dela de `riskyClicksThreshold` (10). Un `action_type` inconnu est traite comme le plus risque, jamais banalise. | Moduler par la severite du finding (inverse le raisonnement), risque purement intrinseque (ne distingue pas une page morte d'une page qui performe), calculer un score de risque (un chiffre non explicable la ou trois classes suffisent a decider) |
| 2026-07-22 | **AGT-000 : une proposition APPROUVEE devenue obsolete est remontee, jamais perimee en silence** | Quand un nouveau payload remplace l'ancien pour la meme action, `decideSupersession` ne perime que les propositions encore OUVERTES (`proposed`/`invalidated`). Une proposition `approved` dont le payload ne reflete plus la realite pose un vrai dilemme : la perimer efface une decision humaine, la laisser filer ferait executer une action sur un etat perime. Elle est donc renvoyee dans `staleApproved` — visible en CLI, intacte en base — et un humain tranche. Meme discipline pour `executing`/`executed`/`rejected` : une machine ne reecrit pas une decision prise. | Perimer aussi les `approved` (efface une validation humaine sans trace), ne rien perimer du tout (deux propositions vivantes ciblent la meme page et l'une des deux sera approuvee au hasard), invalider automatiquement via `updateProposalPayload` (techniquement possible, mais transforme une approbation explicite en rejet implicite) |
| 2026-07-22 | **AGT-000 : deux corrections transverses trouvees en verifiant DATA-006/007** | (1) `proposals.ts` et `policies.ts` ecrivaient leurs horodatages en **ISO** dans des colonnes dont le DEFAULT SQL est au format DB — le piege lexical documente par `timestamps.ts` (`'T'` 0x54 > `' '` 0x20), et `proposal-state.isApprovalValid` compare precisement ces chaines : deux formats melanges y font expirer une approbation au mauvais moment. Rien n'avait casse **parce que les tables etaient vides** ; AGT-000 est le premier a y ecrire. (2) Les deux modules importaient `db` **statiquement** (`db/index.js` → `$env/dynamic/private`), donc etaient inchargeables hors runtime SvelteKit — aucune preuve sur Neon n'etait possible. Passes au client injecte de `findings.ts` (import dynamique, parametre `client?` optionnel, appelants app inchanges). Dette nommee et NON corrigee : `observations.ts` ecrit `fetched_at` en ISO dans une table peuplee, sans comparaison lexicale aujourd'hui. | Ecrire les propositions via un module parallele compatible runner (duplique la logique d'ecriture et ses gardes), convertir les formats a la lecture (le desordre reste en base et chaque nouveau lecteur doit y penser), corriger `observations.ts` dans la foulee (table peuplee = migration de donnees, hors perimetre d'un lot sans DDL) |
| 2026-07-22 | **JOB-004 : la garde de dependance vit DANS la reclamation, et elle bloque aussi les prerequis obligatoires DEJA MORTS** | Mettre le controle dans le worker aurait rendu la promesse dependante de chaque appelant : un futur runner qui reclamerait autrement l'aurait perdue en silence. La condition est donc dans le `SELECT ... FOR UPDATE SKIP LOCKED` de `claimJob` — structurelle, subie identiquement par deux workers concurrents. Elle a DEUX moities, et la seconde n'est pas cosmetique : bloquer aussi les jobs dont un prerequis obligatoire est mort/annule/saute/introuvable. Sans elle, un dependant dont le prerequis vient de mourir deviendrait reclamable AVANT que `settleBlockedJobs` ne l'ait conclu — une course, et le job tournerait sur des donnees absentes. Corollaire impose : `enqueueJob` serialise lui-meme `depends_on` (type `JobDependency[]`, plus jamais du texte libre), parce que la garde casse la colonne en `jsonb` et qu'une valeur malformee ferait echouer la reclamation de TOUTE la file. | Verifier les dependances dans le worker apres reclamation (perdue des qu'un appelant l'oublie ; et le job a deja consomme une tentative), une table `job_dependencies` (DDL pour une file de quelques dizaines de lignes, et un second etat qui peut diverger de `depends_on`), ne bloquer que sur les prerequis en cours (ouvre la course avec la passe de resolution) |
| 2026-07-22 | **JOB-004 : un prerequis obligatoire mort donne un statut `skipped`, distinct de `cancelled`** | Il fallait un statut TERMINAL, sinon le dependant restait `queued` a vie — non reclamable, invisible, et son run inachevi avec lui. Reutiliser `cancelled` etait tentant (zero vocabulaire nouveau) mais faux : `cancelled` est une decision HUMAINE, avec un acteur et une raison au journal (JOB-007), et `explainFailure` aurait annonce « annule par un operateur » la ou personne n'a rien decide — envoyant l'operateur relancer le mauvais job. `skipped` (colonne `text`, zero DDL) porte sa propre cause `DependencySkipped`, nomme le prerequis manquant, ne consomme AUCUNE tentative, et ecrit un step `skipped` qui fait basculer le run en `partial`. Il est REPRENABLE a la main (`requeueDeadJob` l'accepte) : rien ne ressuscite un statut terminal, meme prerequis repare, et sans cette reprise le skip rouvrait le cul-de-sac que JOB-003 avait ferme pour la dead-letter. | Reutiliser `cancelled` (confond consequence mecanique et decision humaine, fait mentir le verdict d'operateur), laisser le job `queued` indefiniment (invisible, run jamais conclu), un statut `blocked` persiste (deux etats qui peuvent diverger la ou l'attente se recalcule en une requete) |
| 2026-07-22 | **JOB-004 : le lien `detect` -> `propose` du catalogue hebdo est OBLIGATOIRE (decision de Jonathan)** | Jusqu'ici `priority: 10` contre `8` n'etait qu'un ordre de SERVICE : deux workers, ou un detecteur reporte pour quota, et le producteur travaillait sur les findings de la semaine precedente — sans doublon (il est idempotent) mais sans rien dire du decalage. Avec une arete obligatoire, une detection morte fait SAUTER le producteur et le run vaut `partial` : le trou de la semaine devient visible, au lieu d'etre masque par des propositions fondees sur des mesures perimees. Cout assume : pas de propositions cette semaine-la. L'alternative optionnelle gardait l'inbox vivante au prix d'un mensonge par omission. Un prerequis OPTIONNEL reste possible et ne bloque personne — c'est la forme qu'auront les providers non essentiels (Plausible, SPEC §8.2). | Arete optionnelle (l'inbox continue de vivre, mais sur des mesures perimees et sans que rien ne le signale), garder le simple ordre de priorite (le decalage reste silencieux), faire echouer le producteur au lieu de le sauter (un echec suggere un bug a corriger, alors que rien n'a ete tente) |
| 2026-07-22 | **JOB-004 : le dernier verdict d'un step se trie par le TEMPS, jamais par le numero de tentative** | Trouve en ecrivant les tests de `latestAttemptPerStep`. Le reflexe — « la tentative la plus haute gagne » — est faux ici, parce que `requeueDeadJob` remet `attempts` A ZERO (decision JOB-003) : la tentative qui reussit apres une reprise porte un numero PLUS PETIT que celle qui est morte. Trier sur `attempt` aurait fige le verdict d'echec pour toujours. C'est `finished_at` qui fait foi, `attempt` ne servant que de departage — et le comparateur normalise le separateur (`'T'` 0x54 > `' '` 0x20) pour qu'une ligne ISO egaree ne se trie pas apres toute la journee. Ce tri corrige un defaut ANTERIEUR au lot : `recomputeRunStatus` agregeait toutes les lignes de `monitoring_steps`, donc un job mort puis repris et reussi laissait son run `partial` a vie. | Trier par `attempt` (fige le verdict d'echec des qu'une reprise a eu lieu), ne garder qu'un step par type en ecrasant (detruit le journal des tentatives, que JOB-002 existe pour construire), comparer les horodatages sans normaliser (rejoue le piege lexical documente par `timestamps.ts`) |
| 2026-07-23 | **JOB-006 : une table `system_settings` pour les limites, et le « zéro DDL » n'était pas une règle** | L'acceptation exige des limites « configurables sans redéploiement ». Sur Vercel une variable d'environnement n'est relue **qu'au redéploiement** : elle ne peut pas la tenir. `gmb_settings` est pourtant déjà un KV (`key`/`value`) et sert déjà à des clés non-GMB (`critical_sent_*`) — mais y ranger les quotas GSC ferait mentir son nom à la prochaine lecture. Le « zéro DDL » de JOB-004/005/007 était un **constat** (« tout était là depuis DATA-003 »), pas une doctrine : ici il n'y avait rien. La table naît **vide**, et c'est ce qui rend le DDL sans risque : sans ligne, `resolveLimits` rend les défauts du code, donc appliquer la migration ne change **aucun** comportement — c'est y écrire qui en change un. Portées séparées : le **système** ici, le **projet** dans `project_projections.payload.limits` (calque des `schedules` de JOB-005). | Variables d'environnement (ne tiennent pas l'acceptation sur Vercel), réutiliser `gmb_settings` (zéro DDL mais le nom ment sur le contenu), garder les plafonds globaux en constantes de code (acceptation tenue à moitié, et un incident de quota exigerait un déploiement pour desserrer) |
| 2026-07-23 | **JOB-006 : le refroidissement provider est DÉRIVÉ de `job_attempts`, jamais persisté** | Un échec classé `quota` met **tout le provider** au repos pour sa cohorte, au lieu que chaque job redécouvre le 429 un par un. L'état de repos n'est stocké nulle part : il se recalcule depuis le dernier `error_class='quota'` du journal — table append-only déjà écrite à chaque tentative. Même raison qu'au « zéro table de planification » de JOB-005 : deux états qui peuvent diverger valent moins qu'un seul qui se recalcule. Corollaire assumé : ce lot **n'écrit pas** `project_integrations.health_status = 'quota_limited'` (SPEC §17.1), parce que ce serait un second état persistant dont personne ne serait clairement propriétaire du retour à `healthy` ; le statut est **dérivé et affiché** comme tel. Dette nommée, laissée à OPS-002. **Limite assumée** : le budget de fenêtre compte des **jobs**, pas des **appels API** (une détection = N appels GSC) — c'est un garde-fou grossier, la vraie prévention est le refroidissement, et le compte fin viendra avec les collecteurs E03 quand quelque chose pourra réellement l'écrire. | Une table `provider_usage` écrite par les collecteurs (exacte au niveau appel, mais **les collecteurs sont E03** : une table vide qui promet une précision qu'elle n'a pas — le piège exact d'AGT-000, un module complet que personne n'appelle pendant deux lots), écrire `health_status` (second état, propriétaire du reset indéfini), réagir seulement au 429 sans rien mesurer (ne tient pas le « prévenir » du backlog) |
| 2026-07-23 | **JOB-006 : l'équité est un TOUR qui se rouvre, pas un quota de drain** | Chaque projet prend au plus `perProjectPerLap` jobs ; quand **tous ceux qui ont du travail servable** ont eu leur part, un nouveau tour s'ouvre et les exclusions d'équité tombent. Un simple quota par drain aurait laissé le tick s'arrêter avec du budget restant et une file pleine — pire qu'un tick déséquilibré. La réouverture est calculée dans le module **pur**, donc un test la ferme. Deux subtilités payées comptant : un projet **occupé** (à son plafond de concurrence) ne compte pas parmi ceux « à servir », sinon il empêcherait la réouverture sans que rouvrir ne le libère ; et `projectsWithClaimableWork` doit filtrer sur **les types que le worker sait traiter** — sans ce filtre (bug trouvé par la preuve), un projet dont le seul travail est d'un type non géré figeait le tour **pour toujours** et affamait les projets réellement servables. | Un quota fixe par drain (tick qui s'arrête avec du budget et de la file), un round-robin strict par projet (impose un ordre là où la priorité doit rester le premier critère), pas d'équité du tout (un site volumineux repousse les cinq autres d'une heure — l'acceptation n'était tenue par rien) |
| 2026-07-23 | **JOB-006 : la garde de capacité vit DANS `claimJob`, et un bail EXPIRÉ ne compte pas comme « en cours »** | Comme la garde de dépendance de JOB-004 : les restrictions descendent dans le `SELECT ... FOR UPDATE SKIP LOCKED` plutôt que de rester dans le worker — aucun appelant futur ne peut les oublier, et deux workers concurrents les subissent tous les deux. Prouvé en base par contraste : le même job est refusé **avec** la garde et réclamé **sans**, alors qu'il est parfaitement disponible (sans cet ordre, un refus après la passe de refroidissement s'expliquerait tout autant par un `available_at` poussé, et la preuve aurait confondu deux mécanismes). Second point, trouvé en vérifiant : `status='running'` ne suffit pas à compter la concurrence — un job dont le **bail a expiré** ne tourne nulle part (c'est la définition de l'abandon, JOB-002), le compter occuperait une place au nom d'un worker mort, et quelques workers morts suffiraient à geler la file jusqu'au passage du reaper. Enfin, un report de capacité ne touche **ni `attempts` ni `deferrals`** et n'écrit aucune tentative : ces jobs n'ont pas été réclamés, ils n'ont rien tenté. | Vérifier la capacité dans le worker après réclamation (perdue dès qu'un appelant l'oublie, et le job a déjà consommé une tentative), compter tous les `running` (une poignée de workers morts gèle la file), réutiliser `deferJob` de JOB-003 (exige un bail, donc un job réclamé — et ferait consommer un report à un job qui n'a rien fait) |
| 2026-07-23 | **JOB-006 : `0` vaut « pas de limite », et un type inconnu vaut `none`** | Deux défauts qui vont dans des sens opposés, chacun choisi pour son mode de panne. (1) Un plafond à `0` signifie **aucune borne**, jamais « zéro job autorisé » — même sémantique que `maxJobs`/`reapLimit` — et une valeur illisible ou hors bornes retombe sur le **défaut du code**. Une file arrêtée par une limite corrompue serait le pire mode de panne : celui qui ne se plaint pas. `formatQuota` rend donc `3/∞` et jamais `3/0`. (2) Un type de job absent de `PROVIDER_BY_JOB_TYPE` vaut `none` : il reste soumis aux plafonds global et projet, mais aucun budget provider ne le bloque. C'est le choix **inverse** de `required` en JOB-004, et délibéré — là-bas relâcher la garde sur ce qu'on n'avait pas su lire faisait tourner un job sur des données absentes ; ici la resserrer empêcherait **tout nouveau handler** de démarrer. Corollaire d'exploitation : un worker bridé ne doit jamais se lire comme un worker inactif, d'où `throttledTicks`. | Traiter `0` comme un plafond réel (une config vide éteint la file en silence), rabattre une valeur hors bornes sur la borne la plus proche (fait croire le réglage appliqué — `scripts/limits.ts` annonce au contraire ce qui a été ignoré), traiter un type inconnu comme soumis à tous les budgets (aucun nouveau handler ne peut démarrer sans toucher la table) |
| 2026-07-23 | **DASH-005 : l'approbation est OPTIMISTE (hash attendu) et IDEMPOTENTE dans la transaction** | « Chaque approbation est liée au hash exact » ne tient pas sous concurrence si l'appelant approuve « la version courante, quelle qu'elle soit » : entre l'affichage et le clic, le run hebdomadaire peut avoir périmé la proposition. L'écran renvoie donc le `payloadHash` **qu'il a montré**, `approveProposal` le compare sous transaction et refuse (`stale_hash` → 409) **sans rien écrire** ; obligatoire côté endpoint, optionnel côté fonction (le chemin agent vient d'écrire le payload et n'a rien à comparer). Bénéfice gratuit : l'acceptation « modifier une proposition l'exclut du lot » est obtenue par la **même** comparaison, sans aucune règle d'interface. Second point : si la proposition est déjà `approved` avec une approbation `active` sur le même hash, l'existante est **rendue sans insertion** — sinon un double clic écrit deux lignes dans `proposal_approvals`, et §14.3 fait de cette table la trace de référence : l'audit dirait deux décisions là où un humain n'en a pris qu'une. Enfin, une garde de **statut** a été ajoutée : sans elle, une proposition `rejected` par un humain repassait `approved` au simple rappel de la fonction — une machine réécrivant une décision humaine, chaque semaine, en silence (jamais mordu : aucun projet n'a de policy, donc l'appel n'a jamais lieu). | Approuver le hash courant sans le comparer (rouvre la course, et l'acceptation devient invérifiable), dédupliquer la double soumission côté HTTP (une clé d'idempotence de plus à faire vivre, et deux workers concurrents la contournent), laisser `approveProposal` sans garde de statut (la machine réécrit les décisions humaines) |
| 2026-07-23 | **DASH-005 : « request changes » est un STATUT (`changes_requested`), pas un éditeur de payload** | Le backlog demande « approve, reject et request changes » alors que `PROPOSAL_STATUSES` n'avait rien pour la révision. Éditer le payload à la main — l'option évidente, `updateProposalPayload` existe déjà — était un piège à deux détentes : l'édition serait **`superseded` au lundi suivant** (`decideSupersession` périme tout `proposed` dont le hash a changé, donc la version humaine face à la version canonique du producteur), et tout champ volatil ajouté à la main ferait **exploser la dédup** (le piège central d'AGT-000). Le statut, lui, a un effet de bord voulu : `decideSupersession` ne touchant que `proposed`/`invalidated`, une révision demandée **survit au run hebdomadaire** — prouvé en base. Colonne `text` : zéro DDL. La raison va dans `finding_events` (`agent_comment`, acteur `user:{email}`), seul journal disponible ; conséquence nommée : une proposition sans `finding_id` (rapports, REP-001) n'aura nulle part où l'écrire. | Éditeur de payload brut (édition écrasée au run suivant, dédup en danger), rejeter avec un motif « révision » (confond une demande ouverte avec une décision close, et `rejected` est terminal), ajouter un type d'événement de finding pour une décision de proposition (mélange deux vocabulaires) |
| 2026-07-23 | **DASH-005 : le lot est reconstruit DEPUIS LA BASE, et L4 est exclu dans le module pur** | `approve-batch` ne fait jamais confiance à la liste que le client envoie : il relit les lignes, rejoue `buildApprovalLots` et refuse si les ids ne forment pas **exactement un** lot homogène (même projet, action, niveau, risque). Une page modifiée, une requête forgée ou un onglet resté ouvert pendant le run hebdo ne peuvent donc pas faire approuver ensemble deux projets ou deux niveaux. L'exclusion des **L4** vit dans le module **pur** — testée — et non dans un `{#if}` de template : §12.1 en fait la classe des gestes irréversibles (301, canonical, suppression, désindexation), une validation groupée est une décision prise sur un résumé, donc le bouton **n'existe pas** plutôt qu'il n'existe et refuse. Corollaires : un risque `null` forme sa propre classe (`inconnu`, jamais mélangé à `low` — ne pas savoir n'est pas savoir que c'est faible), un lot d'un seul élément n'est pas un lot, et la réponse rend **toujours** le détail par item (un lot qui « réussit » en taisant deux items refusés se lit comme un lot complet). | Faire confiance aux ids du client (deux projets ou deux niveaux approuvés ensemble), masquer le bouton côté écran seulement (contourné au premier refactor de template ou par un POST direct), rendre un statut global sans détail par item (silence sur ce qui n'a pas été approuvé) |
| 2026-07-23 | **Réparation de données : recollecter la semaine, mais NE PAS rejouer la détection à la main** | La semaine `2026-07-06` était sous-comptée de ~35 % sur 5 projets sur 6 (tirée un jour après sa fin, avant que `latestCompleteWeekStart` n'impose 8+ jours). Un seul pull des 6 projets l'a réparée (+28 670 impressions) — sans `--dry-run` préalable, qui aurait consommé le **même** quota sur le compte partagé pour la même donnée. `spinlink` ressort **rigoureusement inchangé** : c'est la contre-épreuve du diagnostic, mesurée et non supposée. Deuxième geste, gratuit : le diff hebdo de `jonlabs` pour `2026-07-13` avait été calculé contre l'ancienne base et affirmait **+84,3 %** d'impressions ; recalculé (DB seule, zéro quota) à **+35,3 %** — le faux signal que GSC-006 interdit, dans son sens inverse. En revanche la **détection n'a pas été rejouée** : `detect.ts` à la main écrirait des findings hors du chemin gouverné par la file (sans `run_id` de cadence, sans plafond de capacité, sans dépendance) et ferait partir les 50 findings de `barberconcept` sans que la décision ait été prise. Le run hebdo re-décidera sur la donnée réparée. | Laisser la semaine sous-comptée (le détecteur décide chaque semaine sur une base fausse, et l'écart grandit), dry-run puis pull réel (double quota pour la même donnée), rejouer `detect.ts` dans la foulée (findings écrits hors file, et 50 findings partis sans décision) |
| 2026-07-25 | **DASH-002 : deux axes de santé qui ne fusionnent JAMAIS, et un pipeline cassé rend le signal `unknown`** | L'accueil devait « distinguer une intégration cassée d'une baisse de performance ». Un badge unique aurait fait exactement ce que l'acceptation interdit : confondre « le SEO baisse » et « le credential est mort », qui demandent des gestes opposés. D'où `pipeline` (la donnée arrive-t-elle ?) et `signal` (que dit-elle ?) tenus séparés jusqu'à l'écran, chaque carte portant **une phrase qui nomme l'axe**. Le point dur est la seconde règle : quand le pipeline est cassé, le signal devient **`unknown` et non `ok`** — sans elle, un projet dont la collecte est morte afficherait « 0 nouveau finding » et se lirait comme **le plus sain du portefeuille**, le pire mode de panne, celui qui ne se plaint pas. Même doctrine que §10.3 (« une fenêtre absente n'est pas des zéros ») portée à l'échelle du projet. Corollaire de tri : `unknown` se classe **avant** `watch` — ne pas savoir est plus urgent qu'un signal faible connu. | Un score de santé unique (confond les deux causes, c'est l'acceptation même), signal `ok` par défaut quand rien n'est détecté (une collecte morte devient le meilleur élève), `unknown` en fin de liste (le projet muet, seul capable de tout cacher, sort de l'écran) |
| 2026-07-25 | **DASH-002 : le nombre d'un compteur et son lien naissent du MÊME descripteur ; sinon il n'a pas de lien** | L'acceptation « chaque compteur ouvre une liste filtrée cohérente » est décorative si le compteur est calculé d'un côté et l'URL écrite de l'autre : les deux dérivent au premier changement de filtre. `buildCounter` produit donc les deux depuis un `CounterFilter` unique, et le read-model **compte avec ce descripteur** ; la preuve ne compare pas deux nombres du même code, elle **part de l'URL** et la rejoue comme le loader de l'inbox. Corollaire assumé : un compteur qu'aucune liste existante ne sait reproduire (avis — aucune vue cross-projet ; runs d'une période — SPEC §13.4 la fera) reste un **chiffre sans lien**, plutôt qu'un lien qui mènerait ailleurs. A nécessité d'ajouter le filtre d'activité `activityEvents`/`activitySince` à `listFindings`/`countFindings`, en **EXISTS** sur `finding_events` : une jointure dupliquerait la ligne autant de fois qu'elle a d'événements et ferait mentir le total. Et le compteur compte des **`DISTINCT finding_id`**, pas des lignes de journal — sinon un finding aggravé deux semaines de suite annonce « 2 aggravés » pour un problème (bug latent trouvé en écrivant la preuve ; `reconcileDetectionRun` écrit un `aggravated` par run, donc toute fenêtre 28/90 j le rencontrait). | Compteur et lien écrits séparément (divergent au premier refactor), lier tous les compteurs quitte à approximer la cible (un lien qui mentit est pire qu'un chiffre muet), filtrer sur `first_seen_at`/`resolved_at` plutôt que sur les événements (ces colonnes ne couvrent pas `aggravated`, et mélanger deux sources fait diverger deux compteurs de la même ligne) |
| 2026-07-25 | **IDX-001 : une table additive `sitemap_url_observations`, en SNAPSHOT complet par date et non en journal de changements** | IDX-001 exige un inventaire **par URL** (lastmod, locale, canonical attendu) qu'aucune table ne portait : `sitemap_observations` a la forme de l'API Sitemaps de GSC (submitted/indexed/errors/pending), soit du **par-fichier**, et le `payload_json` d'une observation est plafonné à 32 Ko — un inventaire n'y tient pas. Le « zéro DDL » des derniers lots était un **constat, pas une règle** (rappel JOB-006). Le choix du snapshot complet contre le journal est ce qui tient l'acceptation « un sitemap diff est reproductible et lié à deux snapshots » **au sens littéral** : `diffInventories` devient une fonction **pure de deux listes**, rejouable indéfiniment ; un journal cumulé ne se rejoue pas, il faudrait le replier depuis l'origine et espérer n'avoir rien perdu. Volume réel : ~6 projets × quelques centaines d'URLs × 52 semaines. ⚠️ L'unique porte **`url_normalized`** et non `url` : sinon `…/page` et `…/page#a` créeraient deux lignes pour une seule page, et le diff **inventerait un ajout à chaque run**. `sitemap_url` est en revanche dans le `set` d'upsert — une URL peut migrer d'un sitemap enfant à un autre lors d'une refonte sans cesser d'être la même page. | Journal état-courant + événements (moins de lignes, mais le diff cesse d'être rejouable — l'acceptation elle-même), tout mettre dans `payload_json` (32 Ko, impossible), ne stocker que le par-fichier (l'acceptation « diff reproductible » n'est pas tenue : sans inventaire, il n'y a rien à diffuser) |
| 2026-07-25 | **IDX-001 : rien n'est écrit avant que TOUT l'arbre soit parcouru, et un sitemap injoignable est un fait persisté** | Le legacy `fetchSitemapUrls` fait `catch {}` sur chaque sous-sitemap : « signaler les sitemaps invalides ou inaccessibles » y est donc **impossible par construction**, pas seulement absent. Il récurse aussi sans borne (un index auto-référent tourne jusqu'au timeout du worker) et jette lastmod/hreflang. Ici chaque fichier obtient sa ligne avec `errors > 0` et son statut HTTP — **interrogeable en SQL** — et un enfant mort n'annule pas ses frères (`partial: true`). La garde d'écriture est le pendant nécessaire : un inventaire coupé au 2ᵉ fichier sur 5 se lirait comme complet, et le run suivant annoncerait des dizaines de **retraits fantômes**. Ce n'est pas théorique — la preuve l'exécute : un run tronqué à 1 URL annonce **4 retraits qui n'ont jamais eu lieu**. Enfin, un retrait d'URL ne déclenche **rien** : c'est une observation, au plus un finding IDX-005, jamais une désindexation (tenu par construction — ce module ne sait pas soumettre ; vérifié par `indexing_submissions` inchangée). Le legacy reste en place pour les routes manuelles : dette datée, comme la double écriture GSC-002. | Réutiliser `fetchSitemapUrls` (l'acceptation est structurellement hors d'atteinte), écrire au fil du parcours (retraits fantômes au run suivant), faire échouer tout le run sur un enfant mort (un sitemap secondaire cassé masquerait l'inventaire entier) |
| 2026-07-25 | **IDX-002 : une erreur provider et un « non indexé » sont deux TYPES, pas deux valeurs du même** | Le legacy `inspectUrl` `catch` l'erreur et rend `{ verdict: null, coverageState: null, httpStatus: 0, error: '…' }`, que `classifyIndexStatus` classe `unknown` — **exactement comme une page réellement inconnue de Google**. Deux situations qui demandent des gestes opposés (réparer l'auth vs travailler la page) étaient indistinguables, et l'acceptation « distinguer erreur provider et résultat non indexé » invérifiable. `InspectionOutcome` est donc une **union discriminée** dont les branches ne partagent aucun champ exploitable : aucun appelant ne peut les confondre. Prouvé en base : 7 tentatives en erreur → **0 observation** et URL absente ; page vraiment non indexée → **1 ligne classée `not_indexed`**. Deuxième moitié : le legacy jetait une **chaîne**, où `classifyJobFailure` ne trouve ni `status` ni `reason` — un quota mal classé part en **dead-letter permanente**. En passant par `toGscApiError` (déjà écrit pour GSC-002), les 7 classes sont exactes, dont les deux que Google fait à l'envers. Une réponse 200 illisible forme un **3ᵉ état nommé** (`unreadable`) : rien écrit, lot poursuivi. | Rendre un résultat aux champs nuls sur erreur (l'acceptation devient invérifiable, et c'est le bug d'origine), absorber l'erreur URL par URL (les 199 suivantes brûlent contre un mur et le refroidissement JOB-006 n'apprend rien), écrire des `null` sur réponse illisible (se lit comme « Google ne connaît pas cette page ») |
| 2026-07-25 | **IDX-002 : écriture INCRÉMENTALE — l'inverse assumé de GSC-002 — et pas de mise au catalogue hebdo** | GSC-002 n'écrit rien avant la fin de la pagination, parce qu'une semaine partielle se **lit comme complète**, donc comme une chute. Le raisonnement ne se transporte pas ici : une URL inspectée est un fait **autonome**, elle ne prétend rien sur les autres et rien ne la compare à un total attendu. Écrire au fil de l'eau évite donc de perdre 199 inspections **payées au quota** parce que la 200ᵉ a échoué. Mais une erreur provider **interrompt et remonte structurée**, pour que le refroidissement morde sur toute la cohorte `gsc` (même service account que Search Analytics — un 429 d'inspection met donc aussi la collecte GSC au repos, ce qui est voulu sur un compte partagé par 6 projets). Second point : `collect:url_inspection` est **délibérément absent du catalogue hebdo**. Sans IDX-004, personne ne sait quelles URLs méritent le quota (2 000/j/propriété) ; un job planifié avec une liste vide tournerait pour rien chaque lundi. Les URLs viennent du **payload**, plafonnées à 200, et un payload sans `urls` le **journalise** au lieu d'inventer une sélection — installer ici une règle implicite (« les N premières du sitemap ») obligerait IDX-004 à la défaire. | Tamponner puis écrire comme GSC-002 (perd 199 inspections payées pour une 200ᵉ en échec, sans bénéfice : aucun total attendu ici), absorber le quota et continuer (refroidissement inopérant), enfiler le job au hebdo avec les URLs du sitemap (politique de fait qu'IDX-004 devra défaire, et un cap arbitraire décide en silence quelles pages ne sont jamais inspectées) |
| 2026-07-25 | **IDX-005 : la réconciliation gagne un `scope` — un détecteur à couverture PARTIELLE n'est pas autoritaire sur son projet** | `reconcileDetectionRun` (FIND-003) balaie tous les findings d'un type et traite toute absence de la closure comme une absence de **signal**. C'est juste pour GSC, où chaque requête est réobservée chaque semaine. Ça ne l'est pas pour l'indexation : l'inspection coûte du quota (2 000/j/propriété, 6 projets sur un service account) et ne couvre qu'une **sélection** d'URLs (IDX-004). Une page simplement non ré-inspectée serait donc absente de la closure et **auto-résolue en deux runs** — « je ne l'ai pas regardée » se lisant « elle est guérie », ce qui ruine l'acceptation « la résolution conserve l'historique complet ». D'où un champ **optionnel** `scope` : hors portée, le finding est laissé **strictement intact**, `consecutive_misses` compris (l'incrémenter compterait un manque de **mesure** comme une preuve de **guérison**). Le compteur `outOfScope` est **distinct de `held`**, qui veut dire « une décision les protège » : les confondre ferait lire « maintenu par décision » là où personne n'a rien décidé. Champ absent ⇒ comportement inchangé, `keyword-opportunity.ts` non touché. La contre-épreuve **mesure** le faux signal évité : la même scène sans `scope` fait passer une page jamais ré-inspectée de `open` à `resolved`. | Ne rien changer et accepter l'auto-résolution (le bug, et il est silencieux — un finding disparaît sans que personne ne décide rien), inclure les URLs non inspectées dans la closure (les marquerait « redétectées », donc rouvrirait à tort ce qui était résolu), un statut « en attente d'inspection » persisté (un état de plus qui peut diverger, alors que la portée se dérive du run) |
| 2026-07-25 | **IDX-005 : une fluctuation isolée est ÉCRITE mais plafonnée, et `unknown` n'est pas un état** | L'acceptation dit « une fluctuation isolée baisse la confiance **ou** attend confirmation » — les deux moitiés sont prises. Une bascule unique produit un finding `pending` : confiance 40, sévérité **plafonnée à `medium` même sur page stratégique**, et **jamais notifiable** ; la 2ᵉ observation consécutive confirme (confiance 90, `critical`, `aggravated` journalisé) sur le **même** finding. Taire le fait aurait été l'autre erreur : le cockpit doit pouvoir dire « je vois quelque chose, je ne le certifie pas encore ». La règle qui tient tout : une observation `unknown` **n'est pas un état** — elle ne rompt pas un streak de `not_indexed` **et** ne le confirme pas. Sans la 1re moitié, une seule erreur de lecture repousserait indéfiniment la confirmation d'une vraie désindexation ; sans la 2ᵉ, un trou vaudrait preuve. C'est la doctrine de l'union discriminée d'IDX-002 portée à la série temporelle. Corollaire du même esprit : `indexed → excluded` (`noindex`) n'est **PAS** un `index_drop` — c'est une décision du site qu'on respecte, et c'est précisément pour ce détecteur qu'IDX-002 avait séparé `excluded` de `not_indexed`. | Attendre la confirmation avant d'écrire quoi que ce soit (le cockpit se tait sur ce qu'il voit ; et l'inbox ne peut pas montrer une tendance), écrire à pleine sévérité dès la 1re bascule (fatigue d'alerte, exactement ce que §14.3 évite), traiter `unknown` comme `not_indexed` (le bug d'origine du legacy, réintroduit un niveau plus haut) |
| 2026-07-25 | **IDX-005 : produire le SIGNAL de notification, pas le canal ; et « page stratégique » se dérive au lieu de se deviner** | §14.3 demande de notifier immédiatement « la désindexation d'une page stratégique », mais **TEL-002 est BLOCKED** et rien n'existe hors `sendCriticalError` (Resend, dédup 1 h dans `gmb_settings`). Le câbler ici aurait installé un **second chemin** de notification, avec sa propre déduplication, à défaire au branchement du vrai canal — pour une alerte que personne n'a demandé à recevoir par e-mail. Le drapeau `notifyImmediately` vit donc dans `evidence_json` : interrogeable en base, le canal viendra le lire. Il est **cumulatif et strict** (drop **confirmé** + page stratégique), et `critical` lui est **réservé** — l'ouvrir plus largement viderait la notification de son sens avant même qu'elle existe. Second point : aucun registre de page stratégique n'existe. Elle est donc **dérivée** des clics GSC de la fenêtre récente, **surchargeable** par projet (`project_projections.payload.detectors.index_transition.strategicPages`) pour les pages de conversion sans clic organique, et les deux côtés passent par `normalizeUrl` (IDX-001) — sinon la déclaration ne rejoindrait jamais la mesure. Un projet sans donnée ni déclaration n'a **aucune** page stratégique : état à part, jamais « toutes ». | Envoyer l'e-mail dès maintenant (chemin de notification parallèle à défaire, dédup en double), ne rien marquer et laisser TEL-002 rejuger (il faudrait alors relire toute la série au moment de notifier), page stratégique **uniquement** configurée (la table `project_projections` est vide : le critère ne s'activerait jamais), **uniquement** dérivée des clics (une page devis ou contact ne serait jamais stratégique), défaut permissif « toute page est stratégique » (chaque désindexation devient une urgence critique, et l'alerte qui crie toujours n'est plus lue) |
| 2026-07-25 | **IDX-004 : le registre de sélection stocke la DÉCISION, jamais le résultat — et il est écrit AVANT la collecte** | Deux des trois acceptations sont **indérivables**, ce qui est le seul argument qui justifie une 59ᵉ table. « Chaque sélection expose sa raison » : la raison est fonction de l'état **au moment du choix** (diff sitemap, findings actifs, clics 28 j) — rejouer la politique une semaine plus tard rend une autre raison, et « nouvelle page » n'est vrai qu'une fois. « Une inspection manquée est replanifiée sans duplication » : « manquée » veut dire **sélectionnée mais non observée**, et sans trace de l'intention une URL dont le quota a été payé sans rien produire (429 au 3ᵉ appel, bail perdu, réponse illisible) est **invisible** — exactement « une absence de mesure prise pour une preuve ». Mais la table ne porte **aucun `status`** : ce serait un second état persistant dont personne n'est propriétaire du retour (motif du `health_status` rejeté par JOB-006). Honorée se **dérive** : `∃ index_observations(projet, url = url_normalized, observed_date >= due_date)`, le `>=` portant toute la sémantique J+N. Corollaire load-bearing : la sélection est persistée **avant** que la collecte parte — un 429 au 3ᵉ appel sur 10 laisse alors 7 intentions dues, reprises au run suivant sans une ligne de plus ; la contre-épreuve **mesure** qu'un code persistant *après* rendrait ces 7 URLs invisibles. La sélection se calcule enfin à l'**exécution** et non au plan : `scheduler.ts` sérialise un littéral, et une sélection calculée au plan le serait avant que `collect:sitemap` du même run ait écrit l'inventaire du jour, donc avec `new`/`changed` structurellement vides. | Tout dériver sans table (impossible pour 2 acceptations sur 3), une file avec `status` pending/done/failed (second état persistant, personne ne le referme), persister après la collecte (perd l'intention qui porte l'acceptation 3), calculer la sélection dans le scheduler (détruit le catalogue littéral vérifiable, et `new`/`changed` seraient toujours vides), réutiliser `schedulePostPublish`/`post_publish:check` pour J+N (type qui appartient à E03, granularité d'un job par URL et par offset, et sa clé d'idempotence `{contentId}:J+{offset}` **ne porte pas `publishedAt`** — republier ne reprogramme rien) |
| 2026-07-25 | **IDX-004 : la réserve de quota urgent est un ORDRE et un CANAL, pas un pourcentage — et `0` veut dire ZÉRO** | « Réserver du quota aux vérifications urgentes » a d'abord été implémenté en `urgentReservePct`, puis **supprimé** : l'urgent étant servi en premier et sans plafond propre, un pourcentage ne peut que **dégrader** une garantie déjà totale, tout en ajoutant un réglage qu'on peut mettre à zéro. Restent trois mécaniques structurelles : l'ordre des familles (urgent → routine → échantillon), le canal `scope: 'due'` que la configuration ne peut pas désarmer, et `poolUrgentReserve` — **cross-projet**, pour que le projet qui tire le lundi ne prive pas les cinq autres de leurs échéances. L'acceptation « le quota ne peut pas être consommé entièrement par l'échantillon » est portée par `samplePctMax` **clampé à 60 %** : `floor(budget × pct/100) < budget` pour tout budget ≥ 1, donc l'échantillon ne peut jamais prendre le dernier slot, et un réglage forgé à 100 % retombe à 60. Second point, à contre-courant du reste du repo : **`0` veut dire zéro ici**, alors que `job-limits.ts` pose « `0` = pas de limite ». JOB-006 gouverne une concurrence interne ; IDX-004 gouverne un **quota externe payant**, et lire `0` comme « illimité » brûlerait le pool en un job. Enfin `providerWindowBudget` **n'est pas corrigé** : il compte des jobs, le compte fin d'appels vit dans le sélecteur (1 URL = 1 appel exactement), et les deux couches ne se recouvrent pas. | `urgentReservePct` (redondant avec l'ordre, et désactivable à 0), plafond d'échantillon librement configurable (une garde désactivable par réglage n'est pas une garde), suivre la convention `0 = illimité` de JOB-006 (un budget à 0 deviendrait infini sur un quota payant), convertir `providerWindowBudget` en compteur d'appels (exigerait une table `provider_usage` écrite par tous les collecteurs — l'option rejetée le 2026-07-23), lire le quota restant chez Google (l'API ne l'expose pas ; tout chiffre affiché serait une borne prudentielle, d'où `dailyPoolTotal` à 800 et non 2 000) |
| 2026-07-25 | **Revue visuelle : deux mesures différentes ne portent JAMAIS le même mot, et un verdict ne dit jamais plus que ce qu'il a regardé** | Première revue à l'œil de DASH-002/003, livrés l'un et l'autre avec des preuves côté données uniquement. Les calculs étaient tous justes ; c'est le **vocabulaire** qui mentait, à deux endroits que seul un écran rendu pouvait montrer. (1) Le badge d'un panneau vient de la fraîcheur de l'**intégration** (`derivePanelState`), le caveat GSC-004 de la complétude de la **donnée** (`windowCompleteness`) — deux jugements légitimes qui tombent dans le même encadré et disaient tous les deux « à jour ». Sur `barberconcept` : **trois « à jour » contre un « données pas à jour », à quinze pixels**, chacun vrai, le lecteur devant deviner lequel répond à sa question. C'est le vice que les deux axes `pipeline`/`signal` évitent à l'échelle du projet, reparu **à l'intérieur d'un panneau** : la séparation des mesures ne sert à rien si elles se rejoignent dans le libellé. (2) `buildHeadline` se donne pour règle de **nommer toujours l'axe** ; le cas `ok` était le seul verdict nu des cinq (« Rien à traiter ») et portait donc sur un domaine que la fonction ne lit pas — la santé mesure la **donnée**, jamais la file de décisions, si bien que l'accueil affirmait « rien à traiter » à vingt pixels d'un compteur « 4 à valider ». Corollaire de méthode, plus important que les correctifs : les deux tests qui gardaient ces chaînes assertaient une **tournure** (`toMatch(/non branché/)`, `toBe('Rien à traiter')`), ce qui rendait le libellé fautif **intouchable sans casser le vert**. Ils assertent désormais la **propriété** — qu'une intégration désactivée ne ressorte pas son `last_error_code`, qu'un projet sain nomme ses axes avec 6 findings ouverts. Un test qui fige une phrase protège la phrase, pas l'invariant. | Laisser les libellés et documenter le piège (le cockpit existe pour être lu vite ; une contradiction à quinze pixels se paie à chaque lecture, pas une fois), unifier les deux mesures en un seul jugement de fraîcheur (referait le score unique que DASH-002 a refusé, et masquerait la semaine manquante), faire compter les propositions dans la santé (fusionnerait « la donnée va mal » et « une décision t'attend », deux gestes différents), garder les assertions sur les chaînes et changer le code autour (le test aurait continué à protéger la formulation contre sa correction) |
| 2026-07-26 | **Cutover Neon : merger `feat/neon` SEUL, et mesurer la dérive par ENSEMBLES de clés plutôt que par comptes** | Constat qui a ouvert la session : `main` — ce que Vercel déploie — porte encore `drizzle-orm/libsql` (dernier commit `af6da45`, 2026-07-15), donc **Turso est toujours la base de production** et reçoit les écritures des crons, pendant que `NEON-MIGRATION.md` et `CLAUDE.md` annonçaient la migration « terminée côté code et données » — vrai de la branche, faux de la prod. Deux décisions en découlent. **(1) Périmètre** : merger `feat/neon` seul (4 commits, **fast-forward**, `vercel.json` identique donc aucun cron ne change) et non `feat/cockpit` (77 commits d'un epic **en cours**) — la prod bascule sur Neon avec zéro surface nouvelle, et E00 se merge epic fini. **(2) Ordre du cutover** : variable Vercel **avant** le push, parce qu'une variable d'env ne s'applique qu'aux déploiements **suivants** — le déploiement en cours garde la sienne, d'où une bascule **atomique** sans fenêtre où code et URL se contredisent (les deux ordres naïfs cassent la prod). **(3) Méthode de mesure** : le `count(*)` de part et d'autre annonçait « 11 lignes à rattraper » ; le diff d'**ensembles de clés primaires** en a montré **26 181**, les deux dérives se compensant (Neon avait 1873 lignes de PLUS sur `gsc_query_page_data` tout en en ignorant 26 181). Le re-jeu ETL global prévu par `_MIGRATION.md` n'était plus jouable — Neon porte ~30 tables cockpit dont les FK pointent sur `projects`, un truncate cascaderait, et le loader d'origine (scratchpad) n'existe plus. D'où deux scripts versionnés : `05-drift-report.mjs` (diff, lecture seule, lit Turso par l'API HTTP libsql puisque `@libsql/client` est retiré des deps) et `06-backfill-delta.mjs` (**ajout seul**, idempotent, pré-filtres unicité **puis** FK évalués AVANT l'insert pour que le dry-run dise la vérité au lieu de la découvrir à l'écriture). **222 lignes rattrapées** dont un article publié le 2026-07-24 par l'autopilot ; **25 986 volontairement écartées** — semaines GSC déjà recollectées par le job cockpit, qui pagine mieux que le cron prod (13 591 lignes contre 11 579 pour le même projet-semaine) : les insérer violerait l'unique `(project_id, week_start)` et surtout **dédoublerait les agrégats de la semaine**. | Merger `feat/cockpit` entier (déploie du travail en milieu d'epic et donne au prod le cron `/tick`, qui écrirait 50 findings d'un coup sur `barberconcept`), re-load complet des 29 tables legacy (cascade sur les tables cockpit via `projects`), accepter la perte des 5 jours (abandonne un article publié et le snapshot GSC), se fier au diff de comptes (annonçait 11 au lieu de 26 181), rattraper les 26 181 sans discernement (deux collectes pour la même semaine, agrégats faux) |
| 2026-07-26 | **DASH-002 : un pipeline sain ne prouve pas qu'on ait REGARDÉ — `ok` exige un diagnostic complet, en TROIS degrés** | Seconde moitié de la décision du 2026-07-25 (« un pipeline cassé rend le signal `unknown` »). `barberconcept` s'affichait **« Sain »** sans avoir jamais été détecté : collecte GSC fraîche, pipeline vert, zéro finding — qui se lisait « zéro problème » là où ça voulait dire « personne n'a jamais ouvert le dossier ». L'axe `pipeline` dit si la donnée **arrive** ; rien ne disait ce qu'on en avait **fait**. D'où un axe dérivé, la **couverture de diagnostic** : détecteurs **attendus** lus dans le `SCHEDULE_CATALOG` (filtré `detect:*`) et non dans une liste tenue à la main — ajouter un détecteur l'intègre d'office, sinon un détecteur neuf ferait passer les projets pour couverts avant d'avoir tourné une fois — croisés au dernier job **`succeeded`** (un détecteur mort en dead-letter n'a rien diagnostiqué). L'invariant : **`ok` n'est atteignable que sur un diagnostic complet** ; ce qui est POSITIVEMENT su passe toujours (un critique reste un critique malgré l'angle mort), c'est la conclusion au vert qui exige d'avoir tout examiné. Le point dur est la **granularité**, trouvé en vérifiant sur Neon et non sur le papier : la 1re version renvoyait `unknown` pour toute couverture incomplète, et comme `detect:index_transition` n'a **jamais** tourné nulle part, **les 6 projets viraient au violet** — « 6 à traiter sur 6 » ne distinguait plus le projet jamais ouvert de celui suivi depuis des semaines, et un cockpit uniforme ne se lit pas « en moins d'une minute », il ne se lit plus du tout. D'où trois degrés qui ne se confondent pas : rien d'examiné → **`unknown`** (on ne sait rien), partiellement examiné sans rien trouver → **`watch`** (on ne peut pas conclure), tout examiné → le verdict des findings. Corollaire : `expectedCount === 0` vaut **`none`, pas `full`** — couper la planification d'un projet ne le rend pas sain, ce qui vise directement la décision en attente sur `barberconcept` (désactiver `weekly` le rendra muet, jamais vert). **Zéro DDL, rendu inchangé** (les deux badges existaient déjà côté template). | Ne rien changer (le bug, et il est silencieux — le projet jamais examiné est le mieux noté du portefeuille), forcer `unknown` sur toute couverture incomplète (mesuré : 6/6 au violet, l'écran ne différencie plus rien), ne traiter que le cas « aucun détecteur » et laisser passer le partiel (jonlabs, 10 findings `medium` et indexation jamais examinée, se serait déclaré `ok` — le bug d'origine en plus discret), lister les détecteurs attendus à la main (diverge du catalogue au premier ajout), compter les jobs `dead` comme passages (referait exactement l'erreur qu'on corrige) |
| 2026-07-26 | **DASH-006 : la panne d'automatisation se révèle par un CROISEMENT, et l'axe « tiré » ne fusionne jamais avec l'axe « réussi »** | Tout ce que le cockpit savait montrer était une **ligne en base** : un job mort a son `status='dead'`, un run raté son `status='failed'`. Mais un **tick qui ne tourne pas n'écrit rien** — ni run, ni job, ni erreur — et l'absence n'a pas de ligne à interroger. Elle ne se révèle que par la confrontation de deux choses qui ne vivent pas au même endroit : le dernier créneau **attendu**, calculé, et le run **observé**. D'où trois choix load-bearing. **(1)** Le créneau attendu passe par `dueOccurrences` — la fonction **même** du scheduler — et non par une réénumération : deux implémentations divergeraient au premier changement d'heure, et l'écran accuserait le cron d'un créneau qu'il n'a jamais eu à tirer. **(2)** La jointure porte sur `monitoring_runs.period_end`, qui contient le **créneau LOCAL** écrit par `planOne`, et non sur un intervalle autour de l'instant : un intervalle apparierait un run au créneau voisin le jour du changement d'heure, c'est-à-dire précisément là où la question se pose. **(3)** `late` et `missed` se séparent sur `DEFAULT_LOOKBACK_MS` **importée** du scheduler, jamais recopiée — au-delà de la fenêtre `dueOccurrences` ne regarde plus en arrière, donc le créneau est perdu pour de bon, et deux valeurs distinctes feraient promettre à l'écran un rattrapage que le tick ne ferait pas (la bascule est testée **pile sur la borne**, du bon côté : `instantMs > since` ⇒ un créneau sur la borne est déjà `missed`). Le point produit, lui, est la **séparation des axes**, jumelle de `pipeline`/`signal` : `health` dit si le créneau a été **tiré**, le statut du run dit si ce qui a été tiré a **réussi**. Les fondre peindrait en rouge un projet dont le run hebdo est parti à l'heure et a échoué faute de quota — et en **vert** celui dont le dernier run est `success` mais qui n'a plus rien tiré depuis trois semaines, qui est exactement la panne cherchée. Contre-épreuve exécutée en base : un run `failed` posé sur le créneau manquant passe la planification à `ok` **tout en affichant l'échec** ; son retrait la fait retomber à `missed`. Corollaire d'ordonnancement : `unwired` est évalué **avant** `disabled`, parce que `planDueJobs` écarte les cadences sans handler avant de lire `enabled` — l'inverse donnerait deux raisons au même silence, dont une fausse. Mesure immédiate sur les vraies données : **12 créneaux manqués sur 12**, aucun run hebdomadaire n'ayant jamais existé. | Fusionner les deux axes en un seul état de santé (le projet dont le cron est mort se lit « au vert » — la panne qu'on vient chercher), déduire la panne d'un `updated_at` de run vieillissant (un projet neuf ou une cadence désactivée deviendraient des pannes, et un seuil de plus à régler), stocker un `last_fired_at` par cadence (état persistant qui peut diverger, refus déjà tranché par JOB-005 « aucune table de planification »), réénumérer les créneaux dans le module d'affichage (divergence DST garantie), recopier la fenêtre de rattrapage en constante locale (l'écran promettrait des rattrapages que le tick ne fait pas), joindre le run par intervalle de temps autour du créneau (faux appariement le jour du changement d'heure) |
| 2026-07-26 | **DASH-006 lot 2 : une pause est une DÉCISION, donc un journal append-only dont l'état se dérive — jamais un booléen** | Le lot 1 montrait le créneau qui n'a pas eu lieu, sans pouvoir dire pourquoi : un cron mort et un projet suspendu produisent le même silence, et les confondre fait soit s'alarmer pour rien, soit ranger un incident sous « c'est normal ». Une pause porte un auteur, une cause, une date, parfois une échéance — c'est une décision, pas une configuration. `automation_pauses` (seul DDL, 60 tables) n'a **aucun unique** : rejouer un geste ne doit pas ÉCHOUER, il ne doit RIEN ÉCRIRE (idempotence dans la transaction, contrat d'`approveProposal`). Trois portées en **union, pas en préséance** — rien à arbitrer, donc rien qui puisse diverger entre l'écran et le scheduler. L'expiration `until` est **dérivée à la lecture** : aucun job de réveil, donc aucun moyen qu'une pause « expirée en base » contredise une pause « expirée à l'écran » | `project_projections.payload.schedules.enabled` (**rejeté** : projection recompilée, la pause y serait effacée sans bruit — un monitoring qui redémarre sans que personne l'ait décidé) · `system_settings` (**rejeté** : KV réécrit sur place, le geste précédent disparaît et « pause et reprise sont auditables » devient infalsifiable) · une colonne d'état + journal à côté, comme `findings.status` (**rejeté** ici : le volume est minuscule, et deux sources de vérité pour une garantie d'audit sont exactement ce qu'on ne veut pas) |
| 2026-07-26 | **DASH-006 lot 2 : un job suspendu est CONCLU (`skipped`) et empêché — les deux, pas l'un ou l'autre** | Trois options existaient pour les jobs déjà en file quand la pause tombe. Les laisser partir : « en pause » à l'écran ne veut rien dire en réalité. Les rendre seulement non réclamables : ils dorment **à vie**, et leurs dépendants obligatoires avec eux — le trou exact que JOB-004 avait fermé. D'où les deux : la 4ᵉ passe `pauseOnce` conclut (avant `settleOnce`, pour que le skip se propage dans le **même drain**), la garde d'admission empêche (dans `claimJob`, où vit déjà celle de JOB-006). Le statut `skipped` n'est pas cosmétique : c'est ce que `classifyDependencyGate` lit comme prérequis mort, donc la propagation est **gratuite et déjà prouvée**. Un skip ne consomme **pas** de tentative — le job n'a rien raté, on lui a retiré l'autorisation de partir | Annuler les jobs (`cancelled`, **rejeté** : ce statut dit « un humain a annulé CE job », pas « une décision couvre sa catégorie ») · supprimer les lignes (**rejeté** : un job disparu est un trou d'audit) · ne garder que la garde de réclamation (**rejeté** : dépendants bloqués à vie) |
| 2026-07-26 | **`normalizeDbTimestamp` : une chaîne déjà au format DB ne repasse JAMAIS par `toDbTimestamp`** | Trouvé par un test de `pause-state` sur la borne exacte d'une échéance. ECMA-262 parse une date-time sans `Z` ni offset en heure **LOCALE** : `new Date('2026-07-26 12:00:00')` puis `toISOString()` décale d'une à deux heures à Zurich. Une pause échéant à 12:00 se serait lue active jusqu'à 14:00 — et le bug n'aurait été visible qu'aux abords de l'échéance, deux fois l'an avec une amplitude différente. Posé dans `timestamps.ts`, sa vraie maison, pas dans le module appelant | Comparer en `Date` partout (**rejeté** : les colonnes sont du `text`, la comparaison lexicale est le contrat de tout le schéma) · n'accepter que des `Date` en entrée (**rejeté** : les valeurs relues de la base sont des chaînes, l'appelant referait la conversion à la main — et la referait mal) |
| 2026-07-26 | **DASH-003 lot 2 : `paused` est un 6e etat de PROJET, range APRES `ok` — pas une valeur d'axe, pas `unknown`** | DASH-006 avait etabli qu'« une decision n'est pas une panne » sur `/automations`, mais `home-state.ts`/`home.ts`/`project-cockpit*.ts` ne connaissaient pas `automation_pauses` (0 occurrence) : un projet volontairement suspendu se lisait comme un pipeline mort sur l'ecran qu'on ouvre en premier. `unknown` semblait suffire — un projet suspendu ne dit plus rien — mais il signifie « muet, ET je ne sais pas pourquoi », le contraire de ce qui est vrai. L'argument decisif est un effet de bord : `STATE_RANK` place `unknown` en 3e position, donc un arret volontaire serait passe DEVANT un projet a surveiller. D'ou un etat dedie range apres `ok`, exclu de `needsAction`, et un champ `ProjectCard.pause` qui porte raison/auteur/portee/echeance (un enum ne peut pas les porter). `worst` ignore `paused` sauf si TOUT le parc est suspendu — six projets geles ne sont pas « au vert ». | 5e valeur sur `PipelineState` (sur-affirme : une pause peut etre partielle, et duplique ce que le champ dedie porte deja), replier sur `unknown` (nomme le symptome au lieu de la cause, et prend la tete de la file), ne rien changer (la confusion que DASH-006 supprime, reintroduite un ecran plus loin) |
| 2026-07-26 | **DASH-003 lot 2 : l'ordre des regles de `classifyPipeline` est celui de ce qui SURVIT a la reprise** | Une pause n'entre qu'a UN endroit : la fraicheur. Un credential `revoked` sous pause reste `broken` (le jour ou l'on reprend, la panne est encore la) ; un job en dead-letter reste une degradation (il est deja en file, la pause ne le concerne pas) ; seul le retard de collecte cesse d'etre un symptome, parce qu'il est la consequence attendue de la decision. Corollaire mesure : la pause n'explique le retard que si TOUS les jobs du provider de fraicheur sont suspendus — `collect:gsc_query_page` (hebdo) et `collect:url_inspection` (quotidien) appellent tous deux `syncGscIntegration`, donc « au moins un » aurait silencie un vrai retard en le presentant comme normal. Meme discipline pour le diagnostic : un detecteur n'est suspendu que si CHACUNE des cadences cablees qui l'enfilent l'est, propagation JOB-004 comprise (un prerequis obligatoire mort ⇒ `skipped`). | Faire passer la pause avant les pannes (une pause reparerait un credential), « au moins un job du provider » (silencie un retard reel des qu'une cadence secondaire est suspendue), suspendre le diagnostic en bloc (une pause `monthly`, au catalogue vide, pretendrait suspendre la detection) |
| 2026-07-26 | **DASH-003 lot 2 ch.2 : donner un lecteur a un read-model n'a de valeur que si les DEUX chemins de lecture sont prouves egaux** | `loadInspectionFreshness` (`indexing-read.ts`, IDX-002) n'avait **aucun appelant nulle part** : ecrite, testee par personne a l'usage, donc *crue* juste. L'onglet Indexation lui en donne un — mais la vue d'ensemble lit la meme mesure par son `max(observed_date)` local (`project-cockpit.ts`). Trois options : recopier la requete (2e autorite sur la meme mesure, qui diverge au premier changement de borne), ne pas l'utiliser (le read-model reste sans lecteur, et le chantier rate son objet), ou l'utiliser **et prouver l'egalite**. Retenu : le panneau `indexing` et l'`IndexationSummary` de l'onglet sont compares champ par champ (`JSON.stringify`) a ceux de `loadProjectCockpit`, sur un projet reel et au meme instant (§A de la preuve). C'est le pendant exact de l'egalite §A du lot 1, a une difference pres qui en fait tout l'interet : **la carte du lot 1 venait de la meme fonction**, ici les deux valeurs viennent de **deux requetes differentes**. Sans cette assertion, un taux de couverture aurait pu differer d'un onglet a l'autre sans que personne sache lequel croire. Corollaire : la sante du projet n'est **pas chargee du tout** par l'onglet — le moyen le plus sur de tenir l'invariant anti-divergence sur un ecran qui ne l'affiche pas. | Recopier `max(observed_date)` dans `project-indexing.ts` (rejete : 2e autorite, divergence garantie a terme), appeler `loadProjectCockpit` en entier et y piocher le panneau (rejete : ~14 allers-retours, fenetres GSC et timeline chargees pour un onglet qui n'en montre aucun), refactorer `project-cockpit.ts` pour qu'il passe aussi par `loadInspectionFreshness` (rejete ici : touche un lot livre et prouve, pour un gain que l'egalite §A rend deja verifiable) |
| 2026-07-26 | **DASH-003 lot 2 ch.2 : un seul snapshot ne rend pas un diff VIDE, et « honoree » se derive jusque dans l'ecran** | Deux etats voisins que l'onglet devait cesser de confondre. **(1)** Rendre `{added: [], removed: [], changed: []}` pour un premier inventaire sitemap se lirait « rien n'a change » la ou la verite est « il n'y a rien a quoi comparer » — le meme vice que `never` vs `0 h` sur la fraicheur, applique au diff. `SitemapSummary.diff` vaut donc `null` tant qu'il n'y a pas DEUX snapshots, avec sa note propre, et les deux cas sont prouves l'un contre l'autre (vitest **et** Neon). **(2)** `dueNow` ne compte jamais `index_selection` : il vient de `loadDueSelections`, qui a deja joint `index_observations` — la table ne contient que des **intentions**, et la compter dirait « pages inspectees » (le piege nomme au lot 1 d'IDX-004). La preuve le montre en base : poser l'observation **a** la date d'echeance vide la file **sans qu'une ligne d'`index_selection` ne bouge** (2 → 2 comptees), ce qui rend `observed_date >= due_date` visible comme la seule definition de « honoree ». Corollaire d'affichage : le pool consomme se dit **« au plus N »**, jamais « il reste N » — le compte ne voit ni les appels echoues, ni les reponses illisibles, ni ceux de `/seo-index-diagnose` et de la route legacy `seo-data`, qui tapent le meme service account. | Diff vide au premier inventaire (rejete : « rien n'a change » est faux et invisible), compter `index_selection` pour `dueNow` (rejete : annonce des inspections qui n'ont pas eu lieu), stocker un booleen `honored` sur la selection (rejete : 2e etat persistant dont personne n'est proprietaire du retour — deja tranche par IDX-004), afficher « il reste N » (rejete : presente une borne inferieure comme un solde) |
| 2026-07-27 | **FIND-005 : le detecteur de baisses ne compare QUE les couples presents dans les deux fenetres, et l'ecart 4 sem./1 sem. devient le niveau de confirmation** | Un couple query×page absent de la fenetre courante est **indiscernable** d'un couple dont la semaine n'a pas ete collectee. Le compter comme « −100 % de clics » fabriquerait un finding a partir d'un trou de collecte — exactement ce que l'acceptation FIND-005 interdit (« une collecte partielle ne cree pas de baisse »). Le chiffre reel tranche : sur `lecureux`, **107** couples ont disparu entre les deux fenetres de 4 semaines ; une lecture naive aurait ecrit 107 findings faux en un run. Ils sont donc **comptes et annonces** (`vanished`) et laisses a FIND-006, qui aura ses propres seuils de volume. Corollaire : c'est aussi ce qui repond a « distinguer baisse reelle et changement de perimetre » — un perimetre qui retrecit sort du calcul au lieu de le fausser. Second arbitrage : le ticket demande « 7 et 28 jours », mais les traiter comme deux detections produirait **deux findings pour une seule baisse**. Sur le canon hebdomadaire (28 j = 4 semaines, 7 j = 1 semaine), l'ecart entre les deux fenetres porte donc l'information : `confirmed` (les deux tombent, la chute se creuse encore), `sustained` (seul le 4 semaines tombe), `emerging` (seule la derniere semaine tombe → ecrit mais **plafonne a `medium`**, comme IDX-005 plafonne une fluctuation isolee : une chute de 90 % sur une semaine peut etre un jour ferie). ⚠️ La fenetre recente compare **w0 a w1**, PAS a la periode d'avant : un palier bas et stable est donc `sustained` et non `confirmed` — decouvert par une assertion de preuve qui echouait en ayant tort. | Traiter la disparition comme une baisse totale (rejete : fabrique un finding depuis un trou de collecte, 107 faux positifs mesures), exiger la presence sur les DEUX fenetres ET un volume minimal sur la courante (rejete : gater sur la fenetre courante effacerait les pires baisses, celles tombees a presque rien — le seuil porte donc sur la fenetre precedente), un detecteur par span (rejete : deux findings pour une seule baisse), ignorer le signal a une seule semaine (rejete : retarderait d'un mois la detection d'un vrai decrochage), un facteur de saisonnalite neutre en attendant les donnees N-1 (rejete : indistinguable d'un facteur mesure a 1,0 — l'indisponibilite est declaree) |
| 2026-07-27 | **FIND-005 : une page ne se regroupe que si SON TOTAL baisse, et `propose:actions` ne depend pas du nouveau detecteur** | Sans regroupement, une page depubliee remplirait a elle seule le plafond `maxCandidates` de findings disant tous la meme chose. Mais le regroupement ne peut pas etre un simple `count >= seuil` : l'agregat de page doit etre calcule sur **tous** les couples apparies de la page, pas seulement ceux en baisse — sinon une page dont 3 requetes baissent et 12 montent serait annoncee en perte, et le chiffre du finding contredirait celui de la page (`pagesStable` compte ce cas). Il reste « reversible et inspectable » : les preuves portent le delta de **chaque** requete du groupe, et l'`entityType` separe l'identite page de l'identite requete. ⚠️ Cout assume : la semaine ou un groupe se forme, les findings de requete de la meme page sortent de la closure et sont auto-resolus au 2e run — voulu (deux vues du meme probleme ne restent pas ouvertes en parallele) mais ca se lit comme une resolution dans le journal. Second point : `propose:actions` reste attache au SEUL `detect:keyword_opportunity`. AGT-000 ne traite que ce type ; lier le producteur au detecteur de baisses creerait un couplage **faux** — un detecteur mort ferait sauter des propositions qu'il n'alimente pas. Une baisse se **diagnostique** avant de se corriger : ces findings vivent dans l'onglet findings de `/inbox`, jamais dans la file d'approbation. | Un finding par requete sans regroupement (rejete : une page depubliee sature le plafond), regrouper sur le seul nombre de requetes en baisse (rejete : une page qui se recompose serait annoncee en perte), regrouper sur l'agregat des seules requetes en baisse (rejete : l'agregat ne verrait que les pertes et confirmerait toujours le groupe), enchainer `detect:keyword_decline` apres `detect:keyword_opportunity` (rejete : un tour de propagation de skip en plus, JOB-004 propage en N-1 passes, sans contrepartie — ils lisent les memes observations et n'appellent aucun provider), faire dependre `propose:actions` des deux detecteurs (rejete : couplage faux) |
| 2026-07-27 | **REP-001 : l'absence d'un provider est un ETAT DU TYPE, pas une valeur — et le gate d'examen passe avant le comptage** | L'acceptation dit « un provider optionnel absent apparait comme absent, pas comme zero ». La tenir par convention (« penser a ecrire null plutot que 0 ») aurait suffi jusqu'au premier oubli, et l'oubli aurait ete invisible : `0 visite` est un chiffre parfaitement credible. `Availability<T>` est donc une **union discriminee** — une section absente n'a **pas de `SectionBody`**, donc pas de tableau de metriques, donc **aucune case ou un zero pourrait s'ecrire**. Trois absences distinctes plutot qu'un booleen, parce qu'elles appellent trois gestes : `not_wired` (brancher), `never_collected` (attendre ou reparer la collecte), `not_examined` (lancer le diagnostic). Prouve en base sur le trafic : 0 integration `plausible`, section absente, JSON sans `"value":0`, et le texte rendu de la section **ne contient pas un seul chiffre**. Second arbitrage, de meme nature : sur un parc dont aucun detecteur n'a jamais tourne, les sections de findings se declarent `not_examined` **avant** de compter quoi que ce soit — meme si des findings sont passes en entree (import, run manuel, autre version). C'est la regle DASH-002 « jamais regarde ≠ rien a signaler » portee jusqu'au rapport : « 0 nouveau finding » sur une page blanche est le plus dangereux des messages, parce qu'il est rassurant et faux. Corollaire : une section qui PARLE porte ses angles morts (`blindSpots`), donc un `0` n'y est jamais lu seul. | Ecrire `0` et documenter la nuance ailleurs (rejete : un chiffre credible ne se relit pas), un booleen `available` sans raison (rejete : brancher, attendre et diagnostiquer sont trois gestes qu'un badge unique confondrait), interroger `plausible_page_observations` pour en tirer des zeros (rejete : lire une table vide pour en deduire un resultat est la maniere technique de mentir — on lit l'etat du BRANCHEMENT, et la section s'allumera seule le jour ou E10 ecrira), gater l'examen apres le comptage (rejete : le chemin qui ecrit le zero existerait encore) |
| 2026-07-27 | **REP-001 : le JSON est l'artefact, le texte n'en est qu'une projection — et le compteur d'une section vient d'un `count(*)`, jamais des lignes lues** | « Un rapport peut etre genere sans LLM » se prouve mal par une intention. `buildWeeklyReport` produit un JSON **versionne** (`schemaVersion`, parce qu'il aura trois consommateurs qui n'evolueront pas ensemble : archive REP-004, agent REP-002, client REP-005), et `renderWeeklyReportText(report)` **n'a d'autre parametre que ce JSON** — ni base, ni horloge, ni modele. Il ne peut donc rien ajouter que le JSON ne porte, et tout ce qu'il omet reste disponible en aval. Corollaire assume : une projection a le droit de **compresser** ce que le JSON repete — la liste des angles morts du parc est portee une fois au niveau du rapport et rappelee **chiffree** dans chaque section, apres qu'une impression reelle a montre la meme liste de 9 lignes repetee dans les 12 sections (108 lignes qui noyaient tout). Compresser, jamais taire : le nombre elide est toujours dit. Second arbitrage : le total d'une section vient d'un `count(*)` partageant les filtres de la liste (`findingFilters`), et **jamais de `rows.length`**. Deriver le total des lignes lues ferait d'un plafond de lecture un **fait** : 200 findings en base, 15 lus, le rapport annoncerait 15 et se lirait comme une semaine calme. La troncature compte donc contre le total reel, et couvre les DEUX coupures (lecture et affichage). | Rendre le texte depuis la base (rejete : le texte pourrait dire ce que le JSON ne contient pas, et personne ne saurait lequel fait foi), un JSON non versionne (rejete : un rapport archive qu'on ne saura plus relire), repeter les angles morts par section dans le TEXTE (rejete : 108 lignes, le rapport devient illisible donc non lu), les retirer aussi du JSON (rejete : une section extraite seule doit rester lisible avec sa reserve), deriver le total des lignes lues (rejete : un plafond de lecture deviendrait un constat) |
| 2026-07-27 | **REP-003 : la publication du rapport n'est PAS un job de la file, parce que le rapport n'a pas de projet** | Le reflexe etait d'ajouter `report:weekly` au catalogue hebdo (JOB-005) et de le faire dependre des detecteurs (JOB-004). Deux obstacles structurels : (1) `jobs.project_id` et `monitoring_runs.project_id` sont NOT NULL alors que le rapport est **cross-projet** — neuf jobs auraient tente d'ecrire le meme rapport, huit sans effet, et un no-op est indistinguable d'un incident dans une console de file ; (2) les aretes de JOB-004 sont **intra-occurrence** (meme projet, meme run) : elles ne peuvent pas exprimer « attendre les steps des neuf projets ». D'ou une table SANS `project_id` (`weekly_reports`, la premiere du schema dans ce cas), une attente cross-projet bornee par une echeance, et un appel de `publishWeeklyReport` par le tick **apres** son drain. L'unique sur `period_slot` (le CRENEAU LOCAL, comme les cles d'idempotence de JOB-005) porte litteralement « un seul rapport logique par semaine ». | `report:weekly` au catalogue (9 lignes pour 1 rapport, ou une convention non gardee), une ligne par projet portant le meme JSON (perd l'unicite logique et multiplie le stockage par 9), un cron Vercel dedie a 10:00 (deux entrees saisonnieres a permuter a la main deux fois l'an — exactement ce que le tick horaire de JOB-005 a supprime) |
| 2026-07-27 | **REP-003 : le contenu du rapport est une fonction du CRENEAU, sa ligne date de l'ECRITURE** | `loadWeeklyReport` recoit `now = slot` (lundi 09:00), pas l'instant de publication : la periode couverte est donc la meme que la publication tombe a 09:05 ou, apres une panne de cron, le mercredi. Sans cette ancre, deux publications du meme lundi porteraient deux periodes differentes et REP-004 comparerait des semaines qui ne se recouvrent pas. En regard, `published_at` est l'heure REELLE de l'ecriture (le tick ne passe pas son `now` de depart) : un drain de quatre minutes ne s'attribue pas une ponctualite qu'il n'a pas eue, et c'est cette valeur que mesure le SLO. Le SLO lui-meme se **derive** (`published_at <= due_at`), sans colonne de verdict — meme discipline qu'« honoree » (IDX-004) et que l'expiration d'un snooze (FIND-003) : un `slo_met` persiste serait faux le jour ou l'echeance change. | Ancrer le contenu sur l'instant de publication (deux rapports du meme lundi incomparables), stocker `slo_met` (second etat a tenir, mensonger apres un changement d'echeance), prendre `published_at` au debut du tick (surestime la ponctualite de plusieurs minutes, sur un SLO qui en compte 60) |
| 2026-07-27 | **REP-003 : `complete` exige un perimetre attendu non vide, et une PAUSE en sort le projet au lieu de le degrader** | Le statut repond a une seule question : « ce rapport repose-t-il sur une semaine complete pour tout ce que le cockpit devait examiner ? ». Un projet dont la cadence hebdo est **suspendue** n'avait aucun run planifie (le scheduler n'en produit pas) : l'attendre serait attendre ce que personne n'a demande, et le compter comme manque rendrait `partial` permanent des qu'un client est gele trois mois — le statut perdrait toute valeur discriminante. Il sort donc du denominateur, en restant **nomme** (`paused`), jamais melange aux incidents. Corollaire indispensable : `complete` exige `expected > 0`, sinon un parc entierement suspendu (blockers vides, zero degrade) s'annoncerait « complet » sur zero projet examine — la faute DASH-002 « jamais regarde ≠ rien a signaler » portee au statut de publication. Un run EXISTANT l'emporte en revanche sur une pause posee depuis : une decision de mercredi ne retroactive pas le lundi. | Faire tomber le rapport en `partial` des qu'un projet est en pause (`complete` devient inatteignable en exploitation normale), ignorer les projets en pause silencieusement (le lecteur croit le parc entier examine), publier `complete` sur un perimetre vide (rassurant et faux) |
| 2026-07-27 | **REP-003 : l'annonce de disponibilite est PRODUITE, l'envoi reste TEL-002** | « Notifier disponibilite et incidents » suppose un canal ; TEL-001 (bot + webhook) est BLOCKED. Ecrire un envoi maintenant obligerait a choisir un transport de secours (email Resend) que TEL-002 devrait ensuite dedupliquer avec le vrai canal, en violant son acceptation « une alerte repetee ne spamme pas ». Ce lot rend donc l'annonce **derivable a tout moment** de la ligne publiee (`renderPublicationAnnouncement`, pur, dont le seul parametre est ce qui est persiste — meme discipline que `renderWeeklyReportText`), la journalise au tick, et laisse le tuyau a brancher. `readiness_json` porte la matiere : attendus, prets, degrades, bloquants, ecartes pour pause, incidents nommes. | Envoyer un email de secours (cree un 2e chemin de notification a unifier, contre l'acceptation TEL-002), ne rien produire du tout (TEL-002 devrait deviner « pourquoi partial » a partir d'un statut nu), attendre TEL-001 pour livrer REP-003 (bloque la derniere case P0 de la gate M2 sur un ticket qui depend de GOV-003/SEC-001) |
| 2026-07-27 | **FIND-008 : la normalisation d'URL n'est pas un utilitaire d'affichage, c'est la moitie du detecteur — et son cout est asymetrique** | GSC remonte `…/article#section`, `http://`, `https://www.` et `…/page/` comme des pages DISTINCTES : ce sont la meme ressource. Le chiffre tranche — sur `barberconcept`, **143 pages brutes se replient en 51**, et les conflits persistants tombent de **397 a 180** : **217 faux conflits evites**. Et le detail qui rend la decision non negociable : deux ancres d'un meme article se partagent les impressions **a parts quasi egales**, donc elles prennent exactement la forme d'une competition equilibree — **391 de ces faux conflits se seraient lus « probables » contre 171 vrais** (`split` : 299 avant repli, 58 apres). C'est le symetrique du regroupement de variantes de FIND-006, avec la meme asymetrie de cout : trop pauvre ⇒ on FABRIQUE des conflits, trop riche ⇒ on en EFFACE. D'ou une regle FERMEE (5 gestes, liste close de parametres de tracking), VERSIONNEE (`URL_NORMALIZATION_RULE`, incrementee avec `DETECTOR_CANNIBALIZATION` — un regroupement qui change en silence rendrait deux semaines de findings incomparables), PUBLIEE dans chaque preuve et REJOUABLE (§B rejoue `normalizePageUrl` sur chaque forme brute et retrouve la cle publiee), le repli restant reversible via `rawUrls`. Un geste s'ecarte du legacy : la **query string est CONSERVEE** (tracking retire, cles triees) — le parc porte `?f9688240_page=4`, et fusionner une page 4 de listing avec la racine fabriquerait un conflit entre la home et sa propre pagination, sur la page la plus strategique du site. | Reutiliser `gsc-analytics.normalizePageUrl` tel quel (rejete : importe `db` au niveau module, donc inutilisable dans un module pur, et jette la query string entiere), normalisation plus riche — casse du chemin, parametres inconnus (rejete : fusionne de vraies pages distinctes, et effacer un conflit reel est pire que d'en manquer un), pas de normalisation (rejete : 217 faux conflits mesures, dont 220 « probables ») |
| 2026-07-27 | **FIND-008 : le grain HEBDO est obligatoire, la persistance est un gate DUR, et la forme mecanique ne gate rien — elle plafonne** | Trois arbitrages lies. **(1)** `aggregateWindow` (query×page) existe deja et collapse les semaines : l'utiliser « pour reutiliser l'existant » ferait perdre l'ALTERNANCE — le meilleur discriminant, declenche sur **122 des 197** conflits du parc — **sans que rien n'echoue**. D'ou `aggregateByQueryUrl`, qui garde la serie hebdomadaire. Corollaire : la contiguite de l'alternance est celle de la **sous-suite de chevauchement**, pas de la fenetre — on compare les choix de Google sur les semaines ou il avait un choix a faire, sinon une semaine de collecte manquante casserait l'alternance (faux signal que GSC-004 interdit). **(2)** L'acceptation dit « un conflit n'est cree qu'apres persistance minimale » : c'est un gate **dur**, a l'inverse de FIND-005 (baisse d'une semaine ecrite mais plafonnee) et d'IDX-005 (fluctuation isolee ecrite en `pending`). Sous le seuil : rien n'est ecrit, tout est COMPTE. Cas trompeur nomme : deux URLs significatives qui ne se **chevauchent jamais** (A en sem. 1-2, B en 3-4) sont un **REMPLACEMENT** — Google n'a pas hesite, il a change d'avis une fois. **(3)** La forme (`alternating`/`split`/`dominant`/`stacked`), en revanche, ne peut PAS gater : le fingerprint est la requete et la forme bouge d'une semaine a l'autre, donc gater ferait clignoter les findings (ecrit → auto-resolu a la 2e absence → rouvert → re-resolu), le churn meme que le fingerprint evite, deplace d'un cran. Elle est donc ecrite dans les preuves, **plafonne** (`!probable ⇒ ≤ low`) et **trie** ; `maxCandidates` supprime, la closure reste complete. Et c'est ecrit franchement dans le module : la regle mecanique n'ecarte que **11 conflits sur 197** — elle n'EST PAS la classification, SPEC §10.5 reserve le verdict au skill `seo-cannibalisation`. | Reutiliser `aggregateWindow` (rejete : l'alternance devient inobservable en silence), exiger la contiguite dans la fenetre (rejete : une semaine non collectee casserait un conflit reel), ecrire les conflits d'une semaine en les plafonnant comme FIND-005 (rejete : l'acceptation dit « n'est cree qu'apres », pas « est cree mais faible »), gater sur `probable` (rejete : ~10 findings clignotants sur `barberconcept`), taire les conflits non probables (rejete : la machine ne sait pas les distinguer, les taire serait un verdict qu'elle n'a pas le droit de rendre) |
| 2026-07-27 | **FIND-008 : fenetre UNIQUE (`comparable` n'est pas un gate), portee = MESURABILITE, et aucune fusion de variantes de requetes** | Trois ecarts volontaires avec les deux freres. **(1)** FIND-005/006 comparent deux fenetres parce qu'ils mesurent un DELTA ; la cannibalisation est un **ETAT** (« Google montre deux de mes pages, et depuis N semaines »), donc la persistance se lit DANS la fenetre et `comparison.comparable` n'est pas un gate — seul `current.weeks >= minOverlapWeeks` l'est. Consequence mesuree et voulue : `spinlink` et `wildcat` (6 semaines) **PRODUISENT** la ou les deux freres s'arretent. **(2)** La portee reprend la doctrine IDX-005/FIND-006 mais sur un autre axe : une requete disparue de la fenetre ou tombee sous le plancher de volume n'a pas « gueri », elle est **immesurable** — son finding reste strictement intact (`consecutive_misses` compris), et sa disparition est deja le metier de `lost_query`. ⚠️ `measurableQueries` se calcule **AVANT** le gate de significativite : une requete revenue a une seule URL — la guerison meme — n'a plus 2 URLs significatives, donc la calculer apres la sortirait de la portee et son finding ne pourrait JAMAIS resoudre (§I et §I′ prouvent les deux moities). Mode de defaillance accepte et nomme dans le code : un conflit dont le volume s'effondre durablement reste `open`, atenue par `lost_query`, le dismiss humain, et le compteur `outOfScope` visible au log. **(3)** Inversion explicite de FIND-006 : on ne regroupe **pas** les variantes de requetes. La-bas la fusion empeche deux faux signaux ; ici elle en FABRIQUERAIT un — deux orthographes qui sortent legitimement sur deux pages differentes deviendraient un conflit qui n'a jamais eu lieu. L'entite est la requete BRUTE, fingerprint compris (seul detecteur du parc dans ce cas) ; `urlSetKey` est pose dans les preuves pour rendre un dedoublonnage futur possible SANS toucher au fingerprint. | Exiger deux fenetres comparables (rejete : ecarterait `spinlink`/`wildcat` pour une comparaison dont le detecteur n'a pas besoin), calculer la portee apres la significativite (rejete : aucune guerison ne resoudrait jamais), pas de portee du tout comme FIND-005 (rejete : une requete disparue auto-resoudrait le conflit en deux runs, « je ne peux plus mesurer » lu « c'est regle »), fingerprint sur l'ensemble d'URLs (rejete : une URL qui entre ou sort auto-resout et recree, alors que le probleme n'a pas change), fingerprint sur la cle normalisee de requete facon FIND-006 (rejete : fabrique des conflits) |
| 2026-07-27 | **REP-004 lot 1 : « ne remplace pas silencieusement » cesse d'etre une abstention et devient une FORME — l'unique porte (creneau, revision), le tick n'ecrit que la revision 1** | REP-003 tenait l'acceptation en ne faisant rien : republier etait un no-op. C'etait vrai et inutile — le seul moyen de ne pas ecraser l'original etait de ne jamais corriger le rapport, donc un `partial` publie a l'echeance restait faux pour toujours meme quand la collecte finissait a 10:30. Reviser INSERE : trois colonnes (`revision`, `revision_reason`, `supersedes_id`), l'unique deplace de `(period_slot)` vers `(period_slot, revision)`, aucun UPDATE nulle part. L'acceptation REP-003 « un seul rapport logique par semaine » n'est pas perdue, elle change de gardien : le chemin AUTOMATIQUE (`publishWeeklyReport`, appele par le tick) n'ecrit **jamais que `revision = 1`**, avec `onConflictDoNothing` sur le couple — un cron qui repasse cent fois produit toujours une ligne (prouve), et une revision >= 2 demande un geste delibere. Sans cette asymetrie, un tick instable reecrirait la semaine indefiniment et l'histoire deviendrait du bruit. Deux corollaires non negociables : (1) le numero se derive du **MAX**, jamais d'un `count(*)` — les deux coincident tant que rien n'est purge et divergent des que la retention (lot 2) videra les plus anciennes, la revision 4 s'appellerait alors 3 et heurterait l'unique ; (2) la raison est **obligatoire** des la revision 2, tenue par un CHECK en base et par le modele pur avant meme de reconstruire le rapport — une revision muette est un remplacement silencieux qui a simplement garde l'ancienne ligne. | Un UPDATE avec colonne `revised_at` (rejete : l'original disparait, c'est litteralement l'acceptation), une table d'archive separee (rejete : deux formes pour le meme objet, et « accessible apres restart » devrait etre re-prouve sur la seconde), autoriser le tick a incrementer la revision (rejete : un cron instable reecrit la semaine sans qu'aucun humain ne l'ait decide), `count(*) + 1` comme numero (rejete : casse silencieusement des la premiere purge), raison facultative (rejete : « ne remplace pas silencieusement » redevient une convention) |
| 2026-07-27 | **REP-004 lot 1 : une disponibilite qui change n'est PAS un ecart, et la comparaison n'apparie jamais sur de la prose** | Troisieme endroit ou « absent ≠ zero » se defait, et le seul ou il produit un **mouvement invente** plutot qu'un compteur a zero : une section `not_wired` la semaine derniere et remplie cette semaine annoncerait `+13` (un simple branchement lu comme une degradation), et une collecte tombee en panne annoncerait `−13` — donc « treize problemes resolus ». `SectionComparison` est donc une union discriminee dont `became_available` / `became_absent` **ne portent aucun champ chiffre** : il n'existe pas de case ou un delta pourrait vivre (verifie sur le parc reel — `indexation` et `traffic_conversions` sortent `both_absent` sans metriques, sans items, sans « delta » nulle part dans le JSON, contre 10 sections comparables qui portent bien les leurs). Meme famille de refus, trois autres fois : les sections d'**activite** ne produisent aucun mouvement d'items (un finding absent cette semaine n'est pas sorti, il a cesse d'etre *nouveau* — le compter ferait lire douze resolutions), les listes **plafonnees** non plus (un item hors des 15 se lirait « entre », mouvement fabrique par une limite d'affichage, jumeau de ce que FIND-005 refuse), et un **schema** ou une **fenetre** differents bloquent l'ecart en laissant les deux valeurs lisibles (doctrine GSC-004). Second arbitrage, de meme nature : `ReportMetric` gagne une `key` (schema de rapport 1 → 2) parce que le libelle de la metrique L4 est `dont L4 (humain obligatoire), parmi les 12 listees` — un nombre qui change chaque semaine. Apparie sur son texte, il aurait annonce une metrique disparue et une metrique apparue a **chaque** comparaison, pour une section immobile. La cle identifie, le libelle raconte : un libelle reecrit devient un **renommage tracable**, pas une rupture de serie — et c'est aussi ce qui rend « les changements de template sont tracables » verifiable au lieu d'etre promis. | Rendre `+13` avec une note « section nouvellement disponible » (rejete : le chiffre est lu, la note ne l'est pas), traiter une section absente comme 0 pour « garder la serie continue » (rejete : fabrique une chute a chaque panne de provider), apparier les metriques par libelle (rejete : un faux couple disparue/apparue chaque semaine, mesure sur la metrique L4), apparier par position dans le tableau (rejete : casse des qu'une metrique conditionnelle apparait — `runs.${status}` en produit un nombre variable), comparer les items des sections d'activite (rejete : douze findings devenus vieux se liraient comme douze resolutions), comparer des listes plafonnees (rejete : le plafond fabrique les mouvements) |
| 2026-07-27 | **REP-004 lot 1 : le SLO d'un creneau se derive de sa PREMIERE publication, pas de la ligne lue** | Le SLO §17.3 mesure la ponctualite du **cron** sur un creneau. Une revision est un geste delibere, ecrit des heures ou des jours plus tard : la mesurer contre l'echeance de 10:00 ferait passer une correction volontaire pour un retard (30 h dans la preuve), et corriger un rapport degraderait apres coup la ponctualite de la semaine — l'inverse exact de ce qu'on veut encourager. `toMeta` derive donc sur `firstPublishedAt` (la revision 1, avec repli sur la plus ancienne ligne restante si la retention l'a purgee), tandis que `publishedAt` reste l'ecriture de la ligne lue. Les deux sont exposes separement parce qu'ils repondent a deux questions distinctes, et la phrase de l'ecran **nomme** la publication d'origine uniquement sur un creneau revise — sans quoi « SLO tenu » au-dessus d'une ligne datee du mercredi se lirait comme la ponctualite de la revision. Meme discipline que partout : aucune colonne de verdict, et `due_at` recopie de l'original (jamais recalcule avec l'echeance du jour, qui reecrirait ce que le creneau devait tenir). | Deriver le SLO de la ligne courante (rejete : reviser degrade la ponctualite du cron), figer un `slo_met` a la premiere publication (rejete : colonne de verdict, fausse des que l'echeance change — deja tranche par REP-003), recalculer `due_at` a chaque revision (rejete : reecrit apres coup la promesse du creneau), exposer un seul horodatage (rejete : « quand la ligne a ete ecrite » et « quand le creneau a ete honore » sont deux faits) |
| 2026-07-27 | **REP-004 lot 2 : on purge le DETAIL, jamais la LIGNE — et un detail disparu SANS adresse est interdit par la base** | Un rapport pese ~28 kio de `payload_json`, et §7.11 range les rapports en « sans limite, proteges ». Les deux tiennent ensemble des qu'on cesse de confondre le RAPPORT (creneau, statut, SLO, preparation, lignage — des faits qui ne pesent rien et qu'on relit des annees apres) et son DETAIL (la matiere). Aucun DELETE nulle part : `payload_json` devient nullable, et cinq colonnes disent ce qu'il etait et ou il est parti. Deux decisions du lot 1 en dependaient deja sans le dire — `supersedes_id` sans FK, numero de revision derive du `max` : purger des LIGNES ferait mentir le second (la revision 4 s'appellerait 3 et heurterait l'unique). LE point du lot n'est pas les colonnes mais le **CHECK** `weekly_reports_payload_presence_check` : rendre une colonne nullable cree un etat neuf — « ligne sans detail » — aussi dangereux que l'etait une section absente dans REP-001, parce que relu naivement il se lit « rapport vide », soit douze sections non branchees pour un rapport qui en portait dix. La contrainte interdit la version MUETTE de cet etat : sans payload, la ligne DOIT porter son adresse (`payload_archive_ref`), sa date de purge et son empreinte. Prouve en base sur trois UPDATE nus successifs, tous refuses. Corollaire cote lecture : `PublishedReport.detail` est une union discriminee (`available` / `purged`), donc `buildReportView` n'accepte plus qu'un rapport present et `compareReports` rend `unavailable / detail_purged` — sans quoi comparer contre un rapport purgé aurait annonce douze sections « hors plan », c'est-a-dire un changement de TEMPLATE invente par une politique de retention. | Supprimer les lignes anciennes (rejete : casse le lignage et la numerotation des revisions, et fait disparaitre le SLO d'un creneau qui a bien eu lieu), `payload_json` nullable sans CHECK (rejete : rien n'empeche un `UPDATE ... SET payload_json = NULL` dans psql, et la garde ne vit alors que dans le code applicatif), garder un resume chiffre a la place du detail (rejete : deuxieme autorite sur des nombres que le rapport porte deja, qui divergerait), `report: WeeklyReport | null` en lecture (rejete : invite le premier `?? EMPTY` venu, et un rapport vide n'est pas un rapport purge) |
| 2026-07-27 | **REP-004 lot 2 : « archive » est une CONDITION verifiee, pas une intention — et la fenetre par defaut ne purge rien** | Un rapport ne se regenere pas : REP-003 l'a construit sur l'etat du parc de son creneau, et `loadWeeklyReport` repondrait aujourd'hui avec le parc d'aujourd'hui. Une purge non couverte est donc une perte definitive, et `not_archived` retient une ligne **quel que soit son age** — rien dans le modele pur ne permet de passer outre. Mais « archive » declaratif ne garde rien : `confirmReportArchived` ne marque une ligne qu'apres avoir RETROUVE la note du vault et compare son SHA-256 a celui du detail en base. La note embarque le `payload_json` sur une ligne dans un bloc ```json ; l'empreinte du frontmatter sert a INDEXER, celle du bloc rehashe sert a PROUVER (teste : un caractere modifie dans le detail embarque fait refuser la confirmation, donc interdit la purge). Le mode de panne est du bon cote — on refuse plutot qu'on detruise. Meme logique pour le reglage : `report.detail_retention_weeks` (`system_settings`, sans redeploiement) vaut `null` par defaut, c'est-a-dire **conserver sans limite**, et toute valeur illisible y retombe — une valeur corrompue qui vaudrait « 4 semaines » purgerait sur un malentendu. Plancher de 4 semaines (jumeau du clamp a 60 % d'IDX-004) pour qu'aucun reglage ne puisse purger le rapport que quelqu'un est en train de lire. Enfin, la purge **re-assert ses conditions en SQL** : un plan est une photo, et une ligne desarchivee entre son calcul et son execution n'est pas purgee (prouve). | Marquer « archive » sur simple presence d'un fichier au bon nom (rejete : un homonyme suffirait a autoriser la destruction de l'original), se fier au `content_hash` du frontmatter seul (rejete : c'est un champ que n'importe quoi peut ecrire — il indexe, il ne prouve pas), une fenetre par defaut de 52 semaines (rejete : ecrire une politique de purge chiffree pour une table a 0 ligne est de la speculation, et le defaut doit etre celui qui ne detruit rien), autoriser 1 semaine (rejete : purgerait le detail du rapport de la semaine derniere), n'appliquer le plan qu'a l'aveugle (rejete : chaque seconde entre plan et purge devient une fenetre de perte) |
| 2026-07-27 | **Mise en prod : la pause du premier tick se pose en CLI, sur la cadence `weekly` SEULE** | Le cockpit part en production avec un catalogue hebdo a 9 entrees sur 9 projets, soit **81 jobs** pour `MAX_JOBS_PER_TICK = 25`, et jusqu'a **150 findings** sur `barberconcept` (jamais diagnostique, plafond `maxCandidates` applique par detecteur). La decision de desarmer ce premier battement doit etre prise **avant** le deploiement — or l'unique porte humaine des pauses est `POST /api/ops/automations/pause`, qui exige `locals.user`, donc l'ecran `/automations`, qui n'est pas encore en prod. D'ou `scripts/pauses.ts`, qui n'implemente **aucune** regle : il appelle `recordPauseDecision`, donc l'idempotence (relecture de l'etat derive dans la transaction) et le journal append-only restent a un seul endroit. Second arbitrage, plus subtil : `--pause-all` ne vise que **`weekly`**. La cadence `daily` porte `findings:lifecycle` (les veilles qui expirent), `collect:url_inspection` scope `due` (les echeances J+3/J+7/J+28) et `detect:index_transition` — trois gestes qu'on veut voir tourner **pendant** la reprise progressive, et qui sont quasi vides aujourd'hui (`index_selection` sans echeance due). Suspendre `daily` reste possible, mais comme un geste explicite. ⚠️ Une pause de cadence empeche `planDueJobs` d'OUVRIR le run ; elle n'arrete ni le drain ni la publication REP-003 — le premier rapport sera donc un constat d'absence, revisable. | Attendre le deploiement pour pauser depuis `/automations` (rejete : le cron horaire tire son premier creneau avant qu'un humain n'ouvre l'ecran), relever `MAX_JOBS_PER_TICK` (rejete : c'est une constante de la route, donc un redeploiement, et laisser passer 81 jobs ne resout pas les 150 findings), pauser aussi `daily` (rejete : eteint les veilles et les echeances d'inspection pour un gain nul, la cadence etant deja vide), pauser le scope `project` entier (rejete : plus large que le probleme, et masquerait la sante des projets dans DASH-003), un flag d'environnement « premier tick a blanc » (rejete : un etat non journalise, invisible dans `/automations`, exactement ce que DASH-006 a supprime) |
| 2026-07-28 | **GMB-002 : `replied_at` (local) et `remote_reply_at` (distant) ne fusionnent JAMAIS** | Une seule colonne « repondu » repond a deux questions differentes — « le hub a-t-il envoye ? » et « Google montre-t-il une reponse ? » — donc ne peut jamais se contredire, donc ne peut jamais reveler une divergence. Mesure : `physiopommier` portait un avis « en attente » depuis le 15 mars sans que quiconque puisse dire s'il etait oublie ou repondu ailleurs (verdict apres reconciliation : reellement oublie, brouillon de mars jamais envoye). Corollaire : `pendingReviewFilter()` interroge les DEUX sources et est la seule definition de « en attente » (11 copies avant ce lot). Un avis `replied_at` non nul et `remote_reply_at` nul n'est PAS remis en file — ce serait une seconde reponse au meme client. | Une colonne unique (aucune divergence detectable), reparer `replied_at` retroactivement (deviner qui a repondu a un client), rouvrir la file sur les divergents (double reponse) |
| 2026-07-28 | **GMB-002 : la synchro des avis est QUOTIDIENNE, contre SPEC §8.1 et le SLO §17.3** | La SPEC prescrit une synchro horaire et « nouvel avis synchronise en moins de 2 heures ». Le parc mesure 4 projets equipes, 3 100 avis, ~1 nouvel avis par jour : 24 reveils quotidiens sur un compte Google unique ne se justifient pas, et le geste de repondre reste humain. Le choix est ECRIT (commentaire `hourly: []` reecrit + test qui verrouille) pour qu'un lecteur ne « repare » pas vers l'horaire en citant la SPEC. | Horaire conforme SPEC (cout sans benefice mesure), horaire en heures ouvrees (viole le SLO la nuit sans le dire), laisser le commentaire d'origine (invite a une correction silencieuse) |
| 2026-07-28 | **GMB-002 : `collect:gmb_reviews` remplace le cron autonome, il ne s'y ajoute pas** | Le hub n'a qu'UNE ligne de credential (`gmb_settings.account_tokens`) et le rafraichissement la reecrit sans verrou : deux chemins de collecte concurrents ne consomment pas seulement deux fois le quota, ils font une course en ecriture dont le perdant garde un jeton invalide. Precedent identique `gsc-snapshot`. La route reste appelable a la main pour un rattrapage, retiree de `vercel.json`. | Garder les deux chemins (course sur le credential), supprimer la route (perte du rattrapage manuel), un verrou applicatif (complexite pour un probleme qu'on peut ne pas avoir) |
| 2026-07-28 | **GMB-002 : l'auth provider vit dans un module `process.env`, jamais derriere `$env`** | Le handler importait `gmb.ts` (qui lit `$env/dynamic/private`) : il fonctionnait sur Vercel et mourait en dead-letter des qu'un worker local le reclamait (« Cannot find package '$env' »). Un type de job executable seulement en production est un type de job qu'on ne peut pas prouver. `gmb-auth.ts` suit le precedent `gsc-auth.ts` et devient la source unique des jetons pour les trois appelants (handler, route de rattrapage, CLI). | Import dynamique de `gmb.ts` (job non drainable hors prod), reconstruire l'auth dans chaque script (duplication du dechiffrement et du refresh) |
| 2026-07-28 | **GMB-002 : `last_seen_at IS NULL` est un etat porteur, pas une donnee manquante** | NULL veut dire « etat distant jamais lu chez Google ». Aucun DEFAULT sur cette colonne ni sur `last_sync_status` : un DEFAULT `now()` ferait croire que chaque ligne heritee a ete observee, et un DEFAULT `'success'` ferait lire comme saine une fiche jamais synchronisee. Confirme empiriquement : les 88 lignes qui semblaient « divergentes » sont TOUTES `last_seen_at IS NULL` — Google ne les renvoie plus, ce sont des lignes heritees du `backfill`, pas des divergences. La garde « jamais vu ⇒ hors scope » du lot 2 n'est donc pas theorique. | DEFAULT `now()` (arme le detecteur sur des avis jamais observes), DEFAULT `'success'` (une panne se lit comme une sante), colonne NOT NULL (impose d'inventer une valeur pour l'historique) |
| 2026-07-28 | **GMB-002 lot 2 : `review_pending_sla` et `negative_review` COEXISTENT sur un même avis** | Un 2★ de 20 jours produit DEUX findings, deux fingerprints, deux cycles de vie. §10.4 leur donne deux gestes distincts : le SLA route vers le skill `gmb-review-responder`, l'avis négatif vers une **escalade humaine** et AUCUN skill (`NEGATIVE_REVIEW_SKILL` est une constante explicitement nulle, pas un oubli). Faire absorber l'un par l'autre déciderait à la place de l'humain lequel est le bon geste, et serait asymétrique dans le temps : à J+2 le 2★ ne produirait qu'un `negative_review`, à J+4 le SLA serait masqué — « avis négatif EN RETARD » deviendrait indiscernable de « avis négatif frais », soit exactement le fait qu'on cherche à voir. Corollaire porteur : le signal de `negative_review` est « un avis 1–3★ **NON TRAITÉ** », pas « un avis 1–3★ » — sans le prédicat d'absence de réponse, le détecteur écrirait des centaines de findings permanents que rien ne pourrait jamais résoudre. | Absorption du SLA par le négatif (inbox plus calme, mais fusionne deux gestes que la SPEC sépare et masque le retard), un seul type paramétré par la note (perd le routage vers deux actions différentes) |
| 2026-07-28 | **GMB-002 lot 2 : `slaLookbackDays = 180` et `slaDays = 3` cadrent l'ALERTE, jamais l'affichage** | L'arriéré réel mesuré est de **502 avis en attente**, dont 332 d'avant 2025 sur le seul `barberconcept`. Sans borne de fenêtre, la closure contiendrait 499 entrées et l'inbox deviendrait un inventaire. La fenêtre choisit ce qu'on **alerte** ; les avis antérieurs restent un **stock consultable** sur `/projects/[slug]/reviews`, jamais transformés en alertes. À ne pas confondre avec l'arbitrage d'affichage (encore ouvert) : borner l'écran cacherait un fait vérifié contre l'API Google, borner le détecteur choisit ce qui mérite un geste. **Symétrie indispensable** : la même borne vit dans la closure ET dans le scope, sinon un avis franchissant J+180 sortirait de la closure en restant dans la portée, et serait auto-résolu au bout de deux runs — 332 mensonges. Vérifié sur Neon (preuve P5). Mesure au premier run réel : 13 SLA + 4 négatifs sur `barberconcept`. | Aucune fenêtre (499 findings d'un coup sur un seul projet), fenêtre unique pour les deux types (un 1★ de 200 jours cesserait d'être suivi comme un 5★ — d'où 180 j pour le SLA et 365 j pour le négatif), borner l'écran au lieu du détecteur (cache un fait vérifié) |
| 2026-07-28 | **GMB-002 lot 2 : le plafond d'écriture se répartit par FICHE (tour d'équité), une première dans le parc** | Aucun autre détecteur n'a de quota par sous-entité : le plafond `maxCandidates` retenait jusqu'ici les N meilleurs, point. Ici ça ne marche pas, parce que l'arriéré est **concentré** : Eaux Vives 190/541 et Jonction 179/499 portent 74 % du retard pendant que Lausanne tient 301/302. Un plafond global aurait donné les 30 places aux deux plus grosses fiches et **tronqué le 2★ de Sion** — c'est-à-dire précisément le fait que le lot 1 a mis quatre mois à découvrir. `selectByLocation` trie totalement (priorité desc, puis `reviewId`) puis fait un tour de table des fiches, ordonnées par `locationId` (identifiant, PAS arriéré : classer par volume redonnerait au plus gros l'avantage que le tour existe pour retirer). La closure reste **intégrale** : le tour décide qui est ÉCRIT, il ne ferme rien. Vérifié sur Neon (preuve P10). | Plafond global (étouffe les petites fiches, le cas d'usage central du lot), un job de détection par fiche (multiplie les jobs par 6 sur `barberconcept` sans rien apporter, `jobs.project_id` est NOT NULL), plafond par fiche fixe (rigide, gaspille des places quand une fiche est propre) |
| 2026-07-28 | **GMB-002 lot 2 : le SCOPE du détecteur vient de la SANTÉ de synchro, statut ET fraîcheur** | Comme IDX-005, ce détecteur n'est pas autoritaire sur tout son projet : il ne peut juger que les avis d'une fiche dont la synchro a **réussi** et est **récente** (48 h = deux passages du quotidien). Les deux conditions sont indissociables, et c'est le piège : `collect:gmb_reviews` écrit `last_sync_at` **aussi en cas d'échec** (c'est ce qui rend la panne observable, lot 1). La date seule dirait donc « synchronisée » d'une fiche en panne depuis avril, et deux runs auto-résoudraient tout son arriéré. S'y ajoutent deux gardes au niveau de la LIGNE : `last_seen_at IS NULL` (les 88 lignes héritées du backfill) et `last_seen_at < last_sync_at` (l'avis a disparu chez Google, sans jamais un DELETE). Hors portée ⇒ finding laissé **strictement intact**, `consecutive_misses` compris. Vérifié sur Neon (preuve P3). | Fraîcheur seule (une fiche en panne se lirait comme une fiche assainie), statut seul (une fiche « success » d'avril ferait juger un état de quatre mois), pas de scope du tout (le détecteur GSC peut se le permettre, celui-ci non) |
| 2026-07-28 | **GMB-002 lot 2 : l'arête `collect:gmb_reviews → detect:review_pending` est OBLIGATOIRE, et le détecteur reste quotidien** | Le mode de défaillance est asymétrique : si la collecte meurt AVANT d'écrire (`invalid_grant`, 429 au premier appel), `last_sync_at` reste à hier — donc **encore frais à 48 h**. Le détecteur se croirait autoritaire et tournerait sur l'état d'hier, sans voir l'avis négatif arrivé aujourd'hui : le défaut du lot 1 (le 2★ de Sion, dix jours de silence) rejoué une journée à la fois. L'arête obligatoire rend le trou VISIBLE (détection `skipped`, run `partial`, `/jobs` le dit). ⚠️ Comme pour IDX-005, elle garantit l'ORDRE, pas la FRAÎCHEUR : une collecte qui rend `no_gmb_location` réussit, donc l'arête est satisfaite — c'est cohérent, et c'est le cas de 5 projets sur 9. Coût mesuré sur le cadran quotidien : 36 → **45 jobs/jour** pour `MAX_JOBS_PER_TICK = 25`, soit toujours **2 ticks** (coût marginal nul) ; les 9 nouveaux sont `provider: 'none'` et ne retardent aucune collecte payante. **Le prochain ajout au cadran quotidien fera basculer à 3 ticks.** | Arête optionnelle (l'arriéré ancien produirait quand même ses findings malgré un 429, mais un faux « rien de neuf » coûte la confiance dans l'inbox, et le refroidissement provider reporte déjà la collecte), cadence hebdo (un avis négatif découvert six jours après coup est le défaut que ce lot ferme), n'enfiler que pour les projets équipés (le catalogue est un littéral statique sans conditionnel par projet ; un job qui réussit en quelques dizaines de ms avec un motif nommé coûte moins qu'un mécanisme de filtrage à maintenir) |
| 2026-07-30 | **Un prédicat de divergence sans sa garde ment : `reviews-divergence.ts` porte la partition hors du détecteur** | `replied_at IS NOT NULL AND remote_reply_at IS NULL` se tape en une ligne et se lit « le hub croit avoir répondu, Google ne le confirme pas ». Appliqué tel quel dans un audit, il a produit **88 divergences** sur `barberconcept` — toutes fausses : ces lignes ont `last_seen_at IS NULL`, donc Google n'a **jamais** été lu pour elles. « Je n'ai pas regardé » se lisait « Google dit non », la même classe d'erreur qu'« absent ≠ zéro » (REP-001), sur une colonne au lieu d'une section. Tranché **contre l'API Google**, fiche par fiche : **88 sur 88 sont absents de Google**, 0 encore présent — ce sont des avis répondus puis **disparus**, et leur `replied_at` en ISO avec millisecondes est la signature de `reviews/backfill`, qui y écrivait le `replyTime` **de Google**. Le code de production ne s'y est jamais laissé prendre (`review-pending-state.ts` : `!lastSeenAt ⇒ neverSeen, continue` **avant** le compteur `divergent`) : ce qui manquait, c'était un moyen de poser la question **sans** retaper le prédicat. `scripts/reviews-divergence.ts` rend les quatre classes du détecteur interrogeables hors d'un run (`RÉELLE` / `DISPARUE` / `NON LUE` / `HORS`) et `--probe` tranche les indécidables en interrogeant Google **sans rien écrire**. Lecture seule, aucun `--execute`. `replied_at` n'est **pas** réparé rétroactivement : la valeur qu'il porte est vraie (Google montrait bien une réponse), et `manual-gmb-002.sql` refuse de deviner sur « a-t-on répondu à ce client ». | Réparer `replied_at` sur les 88 (devine sur la question la plus sensible du domaine, et la valeur héritée n'est même pas fausse), ajouter la garde dans une vue SQL (une deuxième autorité sur la divergence à côté du détecteur — le refus déjà posé pour le SLO en DASH-003), ne rien faire (le prochain audit retape le prédicat nu et retrouve 88) |
| 2026-07-31 | **Le run hebdo de reprise est FORCÉ le soir même, jamais attendu au lundi** | Le canary tel qu'écrit au merge (reprendre un projet, observer son tick) a une cadence d'observation **hebdomadaire** : reprendre 9 projets un par un en observant chacun aurait pris deux mois. `scripts/schedule.ts --now=<lundi passé> --project=<slug> --lookback-hours=1 --execute` rejoue le créneau, la file draine dans l'heure, et la chaîne complète (collecte → 5 détecteurs → propositions) est jugée le soir même. Ce qui rend le geste sûr : la clé d'idempotence porte le créneau LOCAL, donc le lundi suivant ouvre un run distinct ; et le rapport de ce créneau étant déjà publié, `already_published` court-circuite toute réécriture. ⚠️ **`--lookback-hours=1` est load-bearing** : la fenêtre par défaut (6 h) embarque aussi le run QUOTIDIEN de 07:00, soit 14 jobs au lieu de 9 par projet. Le forçage a immédiatement payé — il a sorti les deux défauts ci-dessous, qui seraient sinon apparus le lundi matin sur les 9 projets à la fois. | Attendre le lundi (deux mois pour reprendre le parc, et les deux défauts découverts en production sur 9 projets simultanément), tout reprendre sans forcer (même exposition, sans le bénéfice de l'observation), un worker local (`scripts/worker.ts`) au lieu du tick (ne prouve pas le chemin de production, qui est précisément celui qui casse) |
| 2026-07-31 | **Un `fetch` sans plafond de temps ne peut être interrompu par personne — le plafond vit dans le transport, pas au point d'appel** | Les boucles de collecte testent bien le signal du job (bail perdu, budget de durée) mais **entre** deux appels seulement : un appel qui ne rend jamais la main ne repasse par aucun test. La fonction Vercel est alors tuée à son plafond de 300 s, et un `SIGKILL` ne repasse par aucun `finally` — la ligne reste `running` jusqu'au reaper, qui la rend à la file pour que la tentative suivante meure pareil. Mesuré sur `cardrank` : `collect:url_inspection` abandonné **deux fois à l'heure près** (19:04→20:01, 20:05→21:00), 3 tentatives restantes avant dead-letter, son détecteur bloqué derrière l'arête obligatoire — puis une 3ᵉ tentative réussie en 107 s, ce qui prouve un appel **mort**, pas un volume. Le plafond (30 s) est posé dans `resolveFetch` : il couvre les **4** appels du module au lieu du seul qui a cassé. Le dépassement devient un `GscApiError` **504** et jamais un `AbortError` nu — c'est ce qui le fait classer `retryable` (donc rejouer) au lieu de tomber dans une classe permanente ; et le signal de l'appelant est **combiné**, pas remplacé, sinon un abandon sur bail perdu se lirait « Google n'a pas répondu ». | Plafond au point d'appel dans `url-inspection.ts` (laisse les 3 autres appels exposés au même mode de panne), timeout côté job seulement (le signal existe déjà et ne sert à rien : il n'est testé qu'entre deux appels), augmenter `maxDuration` (déplace le mur sans le retirer), laisser remonter l'`AbortError` (classé au petit bonheur, et un timeout mal classé part en dead-letter) |
| 2026-07-31 | **Un run ne peut plus se dire `success` sur un sous-ensemble de ses jobs** | `classifyRunOutcome` dérivait le statut des steps **écrits**. Or un step n'est écrit qu'à l'issue TERMINALE de son job (`concludeJobStep`) : un job encore en file n'en a aucun, et son absence était indiscernable de son inexistence — le run hebdo de `cardrank` était `success` avec **7 steps sur 9**. Ce n'est pas cosmétique : `classifyProjectReadiness` (REP-003) mappe `success` → `ready` et `deriveStatus` rend `complete` quand aucun projet ne bloque, donc le rapport hebdo pouvait s'annoncer **complet alors qu'une détection n'avait pas tourné**. C'est « absent ≠ zéro » (REP-001) transposé des sections du rapport aux steps d'un run, et c'était le seul endroit du chemin de publication où l'invariant n'était pas gardé. Le compte des jobs `queued`/`running` se fait dans `recomputeRunStatus`, la fonction qui classe déjà ; le paramètre de la fonction pure est **optionnel et vaut 0**, donc la non-régression tient au défaut et non à une condition. | Comparer aux entrées du catalogue plutôt qu'aux jobs du run (un run légitimement amputé — dépendance sautée, job annulé — se lirait éternellement inachevé), compter côté appelant (chaque chemin d'écriture déciderait s'il regarde la file), réutiliser `DEPENDENCY_PENDING` de `job-graph` (ferait dépendre la publication du rapport d'une constante écrite pour le graphe des dépendances) |
| 2026-07-31 | **`report.publish_deadline_minutes` passe à 300 avant la reprise du parc** | Le défaut de 60 min place l'échéance à 10:00 locales pour un créneau de 09:00. Or le catalogue hebdo porte 9 entrées × 9 projets = **81 jobs**, drainés à `MAX_JOBS_PER_TICK = 25` : la conclusion tombe vers 12:00. À 10:00, REP-003 aurait publié à l'échéance un rapport dont les détecteurs n'avaient pas encore tourné — et le contenu est construit **à la publication**, donc ce n'était pas une étiquette `partial` de plus mais un rapport vide de sa semaine, non réécrit ensuite. Réglé en base, sans redéploiement. ⚠️ Le vrai plafond, `MAX_JOBS_PER_TICK`, reste une **constante de la route** : le déplacer demanderait un déploiement, alors que l'échéance ne demande rien. | Monter `MAX_JOBS_PER_TICK` (demande un redéploiement, et augmente la charge d'une seule invocation Vercel bornée à 300 s), laisser 60 min et réviser le rapport à la main chaque lundi (un geste manuel hebdomadaire pour un problème de configuration), décaler le créneau hebdo à 06:00 (déplace le rapport hors de l'heure où Jonathan le lit) |
