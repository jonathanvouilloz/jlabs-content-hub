# Decisions techniques

| Date | Decision | Contexte | Alternatives considerees |
|------|----------|----------|--------------------------|
| 2026-03-31 | SvelteKit + Svelte 5 (runes) | Stack habituel Jon Labs, SSR + API routes natifs | Next.js (trop lourd), Astro (pas assez dynamique) |
| 2026-03-31 | Turso (libSQL) + Drizzle ORM | Leger, edge-compatible, gratuit pour ce volume, deja maitrise | Supabase (overkill), PlanetScale (plus cher) |
| 2026-03-31 | Better Auth | Deja utilise sur d'autres projets Jon Labs, magic link + email/password | Auth.js (instable), Lucia (deprecated) |
| 2026-03-31 | Skeleton UI + Tailwind | Composants Svelte natifs, theme customisable, support dark mode | shadcn-svelte (moins mature), DaisyUI (moins flexible) |
| 2026-03-31 | GitHub = backup, DB = source de verite | Le repo GitHub existant contenait deja du contenu, mais le systeme de fichiers ne scale pas | GitHub comme source de verite (fragile avec >3 projets) |
| 2026-03-31 | Structure GitHub : {projet}/{type}/{YYYY}/{MM}/ | Avec 8-12 articles + posts par mois par projet, la separation par mois garde les dossiers navigables | Flat par type (trop de fichiers), par statut draft/published (supprime) |
| 2026-03-31 | 3 mecanismes auth : API key, client token, session admin | Chaque use case a besoin d'un niveau de securite different | Token unique pour tout (pas assez granulaire) |
| 2026-03-31 | createId() avec crypto.randomBytes | Simple, pas de dependance externe, suffisant pour le volume | cuid2 (dependance inutile), uuid (trop long) |
| 2026-03-31 | Skeleton UI v4 (pas v2) | v4.13 est la version actuelle compatible Svelte 5 | v2 (deprecated, pas Svelte 5) |
| 2026-03-31 | Layout custom (pas AppShell) | Skeleton UI v4 n'a plus AppShell/AppRail, layout custom avec Tailwind | Chercher un autre design system (inutile, Tailwind suffit) |
| 2026-03-31 | Better Auth auto-detection env vars | BETTER_AUTH_SECRET et BETTER_AUTH_URL detectes automatiquement | Passer manuellement via config (redondant) |
| 2026-03-31 | Endpoint /api/setup one-shot | Creer le premier admin sans seed script, bloque si user existe deja | Script CLI (plus complexe a executer en prod) |
| 2026-03-31 | Tailwind v4 Vite plugin (pas PostCSS) | Plus simple, pas besoin de postcss.config.js ni tailwind.config.js | PostCSS plugin (plus de config) |
| 2026-04-27 | csrf.checkOrigin desactive (svelte.config.js) | Les skills clients postent du multipart server-to-server vers /api/blob/upload depuis n'importe ou. Toutes les routes /api/* ont leur propre auth (Bearer API key ou session Better Auth), Better Auth gere la CSRF de /api/auth/*. checkOrigin est deprecie (warning svelte-check) au profit de trustedOrigins, mais trustedOrigins ne couvre pas le cas "pas de header Origin" (script Node fetch). A migrer le jour ou checkOrigin sera retire : ajouter un hook handle qui patch event.request avec un Origin synthetique pour les routes /api/* validees Bearer. | trustedOrigins liste fermee (impractical, le script Node n'envoie pas de header Origin par defaut), endpoint alternatif non-multipart (oblige a tout reencoder cote skill) |
| 2026-04-27 | Cle d'unicite GMB splittes = position 1-indexed | Re-POST d'un calendrier doit etre idempotent meme si les titres changent. Le slug `${batchSlug}-${i+1}` est stable, simple, sans dependance sur un champ payload. Pas de cleanup auto des orphelins (suppression manuelle via UI si calendrier reduit). | source_article_slug (casse pour les events), external_id explicite (oblige a modifier /gmb-generate), slugify(title) (instable - source du bug) |
| 2026-04-27 | Crons cale sur l'heure CEST (ete) | Vercel crons tournent en UTC sans option timezone. Schedule `0 7 * * *` = 9h CEST (avril-octobre) / 8h CET (novembre-mars). Le digest hebdo passe a `0 6 * * 1` = 8h CEST / 7h CET lundi. En hiver, les posts programmes a 9h locale (08h UTC) seront publies au cron de J+1 (decalage 1 jour, acceptable car le cron est idempotent et les posts ne sont pas time-critical a la minute pres). | Hardcoder UTC (incoherent pour le user), 2 crons saisonniers (anti-pattern, switch manuel 2x/an), cron toutes les heures avec dedup (gaspillage de quotas) |
| 2026-06-24 | Pivot produit "Content Hub" → **jokiSEO** (cockpit SEO & presence locale) | Apres 3 mois d'usage : la valeur recurrente est dans le monitoring (positions, GMB, avis, indexation), pas la production de contenu. La surface Articles/LinkedIn fait du bruit. Recentrage sur SEO + presence locale + livrables client. Voir PRD-jokiseo.md. | Garder le hub generaliste (dilue la valeur), repartir de zero (rejete, voir ligne suivante) |
| 2026-06-24 | **Refactor in-place, pas de rewrite** | Les moteurs durs (gsc-analytics 1160 l., gmb 850 l., indexing 460 l.) sont fiables et encapsulent un savoir durement acquis (gestion tokens/refresh/retry, latence GSC, normalisation URL, cannibalisation locale). Le schema est propre. Ce qui manque est ~100% surface neuve qui se branche a cote. L'outil est un daily driver → un rewrite = periode de regression inacceptable. | Rewrite from scratch (jette le dur deja resolu, reintroduit les bugs corriges, casse l'usage quotidien) |
| 2026-06-24 | **Turso conserve, pas de migration Neon** | Rien dans le scope jokiSEO n'exige Postgres. A 6 projets, libSQL/SQLite tient des annees. Le seul irritant (limite ~999 params/insert) est deja contourne par le chunking de 200 lignes. Migrer = reecrire dialecte SQL/Drizzle + Better Auth + data live + re-tester, pour zero feature gagnee. Si le volume time-series explose plus tard (geo-grid + rank quotidien) → ajouter un store analytique dedie, pas tout migrer. | Migration Neon/Postgres (risque pur, aucun gain fonctionnel a cette echelle), DB analytique d'emblee (premature) |
| 2026-06-24 | **DataForSEO = fournisseur SEO externe unique** | Deja utilise cote skill `/seo-keywords`. Couvre rang SERP reel, geo-grid local, backlinks et AI Overviews avec une seule integration. Centralise dans une couche `providers/` (epic 25) pour eviter la duplication d'appels. | Ahrefs (meilleur backlinks mais 2e fournisseur + cout), multi-fournisseurs des le debut (complexite prematuree), scraping maison (fragile, anti-pattern) |
| 2026-07-20 | **~~Turso conserve~~ → INVERSE : migration Neon (Postgres), 1 base / multi-schema** | La decision du 2026-06-24 (garder Turso) est annulee. Raison : tout l'ecosysteme Jonathan est deja sur **Neon** ; unifier le fournisseur DB simplifie l'exploitation. Surtout, seo-stats et **invoices** partagent les memes clients → une **seule base Neon** (`neondb`) avec **3 schemas Postgres** `core` / `invoices` / `seostats` permet une jointure SQL directe. `core.entities` = registre canonique des slugs (loi noyau n°3), possede par invoices ; seo-stats le reference en FK lecture-seule. Refactor code fait + commite (branche `feat/neon`, typecheck 0 err). Reste : migration des donnees Turso → Neon (voir [NEON-MIGRATION.md](NEON-MIGRATION.md) Phase 4). | Garder Turso (ecosysteme fragmente, pas de jointure cross-app clients), 2 bases Neon separees (perd la jointure client↔projet), Supabase (2e fournisseur) |
| 2026-07-20 | **Liaison client↔projet par SLUG via `core.entities`, pas de FK directe** | Au plus simple et aligne loi n°3 « un slug, partout ». Un client facture (`invoices.clients.slug`) et un projet SEO (`seostats.projects.slug`) se rejoignent parce qu'ils **pointent le meme slug canonique** dans `core.entities`. `core.entities` reste minimal (slug + display_name + created_at). Recoupement reel : exact `barberconcept` ; a canonicaliser `bis-repetita`/`bisrepetita` ; SEO seul `jonlabs`/`physiopommier`/`spinlink`/`wildcat` ; facturation seule `bc-chenois`/`cpv`/`swiss-dental-care`. | FK directe `projects.client_id → clients.id` (couple les 2 apps, casse pour les projets SEO sans facturation), table de jointure dediee (sur-ingenierie a cette echelle) |
| 2026-07-21 | **Pivot ~~jokiSEO~~ → seo-stats, cockpit agentique de monitoring** | Le pivot jokiSEO (2026-06-24) est absorbe et depasse par une cible plus ambitieuse : deleguer 90% du monitoring SEO/presence-locale recurrent a des agents IA, avec validation humaine des actions sensibles. Primitive centrale = le **finding** (fait persistant, fingerprint stable). Chaine : providers → observations → detectors → findings → agent → proposals → approval → execution. Source de verite : [SPEC.md](SPEC.md) ; execution : [BACKLOG.md](BACKLOG.md). Refactor in-place conserve. | Rester sur jokiSEO (surface de dashboard sans delegation, valeur plafonnee), rewrite complet (jette le dur GSC/GMB deja resolu) |
| 2026-07-21 | **UI cockpit neuve from-scratch, backend garde tel quel** | La reconstruction agentique coupe entre UI et backend, pas entre neuf et ancien repo. Backend conserve INTACT : 71 endpoints `/api/*`, schema Neon 29 tables (migre + verifie Phase 4), integrations GSC/GMB/LinkedIn/OAuth, Better Auth, crons — c'est le moat, la SPEC construit dessus (§1.1). L'UI, elle, est repensee a neuf car le paradigme change (dashboard de production → cockpit de lecture/validation/supervision) : nouveau groupe de routes `(cockpit)/` (layout + inbox findings + validation + supervision) qui CONSOMME les endpoints existants + les nouveaux du BACKLOG. L'ancien `(app)/` est gele puis retire une fois le cockpit a parite. Stack UI conservee (SvelteKit 2 / Svelte 5 runes / Tailwind v4 / Skeleton v4 / lucide) : deja moderne, aucune raison de migrer. | Reprise incrementale des 26 pages (traine la dette visuelle + ancien paradigme pendant des semaines, hybride), greenfield total nouveau repo (refait OAuth/GSC/GMB/auth/crons/schema pour zero gain, des mois de risque sur un daily driver), changer de design system vers shadcn-svelte (cout de migration sans benefice, Skeleton v4 fait le job) |
| 2026-07-21 | **Config runtime au boot log-only, fail-fast delegue a `requireEnv`** | Un cold-start serverless ne doit pas tomber sur une var optionnelle absente. `validateStartup()` (config.ts) LOG un rapport de presence par role sans throw ; l'exigence stricte est appliquee au point d'usage via `requireEnv(name, feature)` quand la fonctionnalite est reellement sollicitee. Flags de migration (flags.ts) OFF par defaut, un flag route le comportement sans jamais effacer de donnee. | Throw au boot sur toute var manquante (risque de tuer le daily driver a chaque cold-start), validation dispersee au point d'usage seule (pas de vue d'ensemble ni de .env.example fiable) |
| 2026-07-21 | **IDX-008 : garde Indexing API a deux etages (flag + type)** | La Google Indexing API n'est officiellement valable que pour JobPosting/BroadcastEvent (SPEC 9.4) ; la soumission generique d'articles/pages locales est non conforme et etait declenchee auto en prod au passage `published`. Garde unique dans `indexing.ts` en amont de tout reseau : flag maitre `indexnow` (OFF par defaut pendant la migration) ET validation de type d'eligibilite. Refus = ligne d'audit `status:'blocked'`, zero quota (aucun `fetch`). Aucun appelant ne porte de type eligible aujourd'hui → toutes les soumissions generiques sont neutralisees sans toucher le schema. | Flag seul (ne bloque pas un usage non conforme si le flag est ON), validation de type seule (le feature file exigeait explicitement le flag), retrait pur du code (perd la capacite pour les futurs JobPosting/BroadcastEvent) |
| 2026-07-21 | **DATA-002 en phase expand seule, application par SQL additif** | Creer `project_integrations` + `project_projections` (SPEC 7.1/7.2) SANS backfill ni retrait des tables heritees (project_contexts, *_settings, indexing_credentials, cms_connections restent) : migrate/contract = phase suivante, sur base non vide. `resource_key` discrimine plusieurs proprietes/locations d'un provider ; projections en historique (unique `(project_id, source_hash)` + unique partiel `current`) ; garde `assertNoInlineSecret` sur payload/config, secrets via `secret_ref`. Applique par `drizzle/manual-data-002.sql` (idempotent) + script Pool, PAS `db:push` (deterministe, zero risque sur les autres tables), verifie par re-run de l'introspection (32 tables, zero derive). | Expand + backfill immediat (touche la donnee prod, allonge, secrets a mapper), `db:push` (interactif/moins deterministe sous Windows), `db:generate` (impossible sans baseline snapshot cote seo-stats) |
| 2026-07-22 | **FIND-003 : la closure d'un run de detection = la liste COMPLETE des signaux, jamais la liste ecrite** | L'auto-resolution ferme un finding qui « ne matche plus ». Or le detecteur plafonne ses ecritures (`maxCandidates=50`) alors que barberconcept produit **1310** signaux au-dessus des seuils : se fonder sur les 50 ecrits fermerait 1260 findings bien vivants. `selectOpportunities` expose donc `matched` (complet, avant troncature) a cote de `candidates` (tronque, ecrit), et la reconciliation ne lit que `matched`. Deux gardes de plus : aucune reconciliation sur un **run non autoritaire** (fenetre absente ou zero observation — un projet sans donnee ne vide pas son inbox) et **N absences consecutives** requises avant resolution (defaut 2, configurable par projet, SPEC 10.3). Tout futur detecteur qui plafonne ses ecritures doit exposer sa liste complete. | Reconcilier depuis `candidates` (ferme des findings vivants des que le plafond mord), relever `maxCandidates` a l'infini (fait exploser l'inbox et le cout d'ecriture, ne resout pas le principe), resoudre des la 1re absence (une collecte partielle suffirait a fermer toute l'inbox) |
| 2026-07-22 | **FIND-003 : le snooze tient jusqu'a son echeance, le dismiss vaut a vie** | Deux promesses faites a l'humain que la machine ne reprend jamais. **Snooze** : aucune re-detection ni aggravation ne le rompt (l'aggravation est journalisee, donc rien n'est perdu et le finding remonte a l'expiration avec son historique) — sinon un snooze ne garantit plus le silence et la severite peut osciller d'une semaine a l'autre. **Dismiss** : vaut a vie pour ce fingerprint ; `occurrence_count` continue de monter (matiere premiere de la mesure de faux positifs, FIND-010) mais seul `reopenFinding` (humain) le defait. Corollaire : la legalite du graphe 10.1 est **gardee a l'ecriture** (`canTransition` dans `transitionFinding`, qui porte aussi tous les effets de bord du cycle de vie) et l'expiration de veille a son **propre type de job** (`findings:lifecycle`) — une veille doit expirer meme les semaines sans detection, sinon le snooze devient un enterrement. | Reveil du snooze sur aggravation (le snooze ne garantit plus rien), reveil seulement sur `critical` (compromis mais seuil arbitraire), dismiss avec duree de vie 90j (bruit recurrent, oblige a re-dismisser), dismiss lie a la version du detecteur (fin mais complique la lecture de l'inbox) |
| 2026-07-22 | **JOB-002 : l'exactly-once des effets externes est un MECANISME (registre + garde), pas une convention** | « Deux executions ne produisent pas deux effets externes » (acceptation JOB-002) : apres reprise d'un job mort, le handler re-tourne. Les 2 handlers actuels sont deja idempotents (upsert par fingerprint, expireSnoozes), mais s'appuyer la-dessus ferait reposer l'acceptation sur une discipline que le premier handler a effet reel (IndexNow, publication GMB) violerait sans que rien ne le detecte. D'ou `job_effects` + `guardExternalEffect` en **claim-then-apply** : on RESERVE la cle (`pending`, unique `(project_id, effect_key)`) PUIS on applique — appliquer avant de reserver laisserait une fenetre de double effet. Un effet `failed` reste reprenable (il n'a jamais abouti), un `applied` ne se rejoue jamais. Brique attendue par IDX-007 (outbox IndexNow) et la publication GMB. | S'appuyer sur l'idempotence des handlers + documenter le contrat (zero table, mais l'acceptation repose sur une convention non verifiable), verrou applicatif type mutex (ne survit pas au crash, c'est precisement le cas a couvrir) |
| 2026-07-22 | **JOB-002 : timeout provider vs crash local se tranche par OU le fait se constate** | L'acceptation demande de distinguer les deux, mais un reaper ne peut pas interroger un process mort. La separation retenue est donc mecanique, pas devinee : le **crash se constate de l'exterieur** (bail expire → `classifyAbandonedLease` lit le rythme des battements : plus de battement bien avant l'echeance = `worker_death`, battement jusqu'au bout puis silence = `lease_stall`) ; le **timeout se constate de l'interieur**, worker vivant, via le budget de duree du runner (defaut 30 min) → `ProviderTimeout`, rejouable. La politique de retry n'est PAS reinventee : `decideAfterAbandon` delegue a `decideAfterFailure` (DATA-003). Corollaires : le signal du job n'est **pas** relie au signal d'arret gracieux (sinon l'acceptation JOB-001 « un job commence avant l'arret est mene a son terme » casse en silence), et l'`UPDATE` du reaper est garde par `lease_owner` **ET** `lease_until` observes (sans le second, une course avec un renouvellement volerait un job vivant). | Heuristique sur la duree ecoulee seule (ne distingue pas un worker mort d'un provider lent), classification fine des erreurs des maintenant (c'est JOB-003, hors perimetre), pas de distinction du tout (l'acceptation l'exige et les deux cas appellent des reponses differentes) |
| 2026-07-22 | **JOB-002 : l'historique des tentatives est un JOURNAL append-only, la page reste JOB-007** | « L'interface montre la tentative abandonnee et la reprise » : `jobs` ne portait qu'un compteur `attempts` et un `last_error_*` **ecrase a chaque reprise** — l'historique etait donc structurellement absent. `job_attempts` (1 ligne par reclamation) le retablit ; `scripts/jobs-inspect.ts` le rend lisible tout de suite, et la console d'exploitation (JOB-007, qui depend deja de JOB-002) lira ces memes lignes. **Pas d'unique `(job_id, attempt_no)`** : `releaseJob` REND la tentative (`attempts - 1`, JOB-001), donc un numero se repete legitimement — et ecraser la ligne `released` effacerait justement l'historique qu'on cree. Meme idiome que `finding_events` / `policy_promotions`. Le reaper tourne dans la boucle worker (demarrage + tours a vide) + `scripts/reap.ts` ; le cron dedie reste JOB-005. Limite assumee : **sur Vercel aucun worker ne tourne en continu**, la reprise n'a lieu qu'au prochain lancement. | Colonnes `abandoned_at`/`abandon_kind` sur `jobs` (moins cher mais l'historique reste ecrase, et JOB-003 le redemanderait), page /jobs des maintenant (empiete sur JOB-007), cron Vercel dedie (duplique ce que JOB-005 doit cadrer) |
| 2026-07-22 | **JOB-003 : la RAISON de l'erreur prime sur son statut HTTP** | Classer sur le code HTTP nu ferait l'inverse exact de ce qu'exigent les acceptations, parce que Google ne respecte pas la semantique naive : un depassement de quota arrive en **403 + `rateLimitExceeded`** (le condamner en « structurel » tuerait un job sain) et un refresh token mort arrive en **400 + `invalid_grant`** (le traiter en « permanent » masquerait une cause d'auth qui appelle une action humaine precise). `classifyJobFailure` examine donc : codes internes connus → marqueurs de quota → d'auth → permanents → **puis seulement** le statut (429 = quota, 401 = auth, **tout autre 4xx = permanent** car une erreur du client rejouee a l'identique redonne la meme erreur, 5xx = retryable). Une erreur **illisible retombe sur `retryable`** : on ne condamne jamais un job sur ce qu'on n'a pas su lire. | Classer sur le statut HTTP seul (simple mais faux sur Google, le fournisseur principal), une classe par provider (duplique la logique a chaque integration), tout retenter uniformement (l'etat d'avant JOB-003 : 5 tentatives brulees sur un 403 sans espoir) |
| 2026-07-22 | **JOB-003 : un 429 ne consomme pas de tentative, il se compte a part et sous plafond** | Un quota n'est pas une faute du job. Lui faire consommer le budget `max_attempts` signifie qu'une journee de saturation provider envoie en dead-letter des jobs parfaitement sains. La tentative est donc **rendue** (`deferJob`, meme geste que `releaseJob`) et un compteur **separe** `deferrals` s'incremente, **plafonne a 20** — sans ce plafond, un provider definitivement ferme ferait tourner le job sans fin. Le `Retry-After` du provider est un **contrat** : honore (secondes ou date HTTP, plafonne a 6 h pour qu'un en-tete deraisonnable ne parque pas un job pendant des jours), et le jitter peut l'**allonger, jamais le raboter** (repasser dessous rejoue le 429 a coup sur). | Consommer une tentative comme tout echec (simple, aucun etat neuf, mais tue des jobs sains un jour de saturation), reports illimites (boucle infinie sur un provider mort), file d'attente par provider (c'est JOB-006, qui PREVIENT le 429 la ou JOB-003 y REAGIT) |
| 2026-07-22 | **JOB-003 : le jitter vit dans la couche IO, la decision reste pure** | Le backoff de DATA-003 est deterministe, donc N jobs echoues sur le meme provider reviennent **tous a la meme seconde** (thundering herd). `applyJitter` recoit son `random` en **injection** — meme discipline que le `nonce` de `deriveWorkerId` — et **sans `random`, le delai ressort inchange** : tout le comportement deterministe de JOB-001/002 et ses tests restent valides tels quels, seuls `failJob` et le reaper fournissent `Math.random`. Corollaire de la reprise manuelle : `attempts` **repart a zero** (sinon un job repris a 5/5 remourrait au premier echec et la reprise ne serait qu'un geste), et l'acceptation « une reprise manuelle conserve l'historique des tentatives » est portee par le **journal** `job_attempts` (append-only, la reprise y ecrit sa ligne `requeued` avec auteur et raison), jamais par le compteur. | Jitter tire dans le module pur (casse la purete et la rejouabilite, toute la discipline du repo), jitter par defaut sans injection (change silencieusement le comportement des appelants existants), conserver `attempts` et relever `max_attempts` a chaque reprise (oblige a choisir un nouveau plafond a chaque fois et le job remeurt vite) |
| 2026-07-22 | **JOB-007 : un job EN COURS s'annule en lui retirant son bail, pas en tuant son worker** | Le backlog exige l'annulation, mais rien ne permet de tuer un process depuis une page web — et un worker qui meurt en plein handler laisse exactement l'etat que JOB-002 a passe une session a reparer. `cancelJob` ecrit donc `status='cancelled'` et **libere `lease_owner`** : au prochain battement (<= `leaseMs/3`, ~100 s), `renewLease` — garde par `lease_owner` ET `status='running'` — ne matche plus, le runner l'apprend et `abort()` son handler, et ses ecritures finales (`completeJob`/`failJob`/`finishAttempt`) portent les memes gardes, donc ne reecrivent rien. C'est le mecanisme de JOB-002 reutilise, pas une voie parallele (le commentaire de `renewLease` anticipait deja « job annule »). Prix assume : l'arret n'est pas immediat, et un handler qui ignore son `signal` finit son travail en cours. | Colonne `cancel_requested` sondee par le runner (2e chemin d'arret a maintenir, meme latence), refuser l'annulation d'un `running` (le cas ou l'on veut un bouton est justement le job parti sur un provider bloque), tuer le process worker (ingerable depuis le web, et recree le probleme des jobs orphelins) |
| 2026-07-22 | **JOB-007 : l'audit d'une action d'exploitation est une ligne de journal, pas un champ sur le job** | « Retry et annulation sont audites » : `jobs` n'a aucune place pour un auteur ni une raison, et `last_error_*` est ecrase a chaque reprise. Chaque action ecrit donc sa propre ligne dans `job_attempts` (append-only) — `outcome='cancelled'`/`'requeued'`, l'acteur en `worker_id` (`user:{email}`, pris **dans la session**, jamais fourni par le client), la raison en `metadata_json` —, exactement l'idiome pose par `requeueDeadJob` en JOB-003. Consequence assumee : annuler un job qui tourne produit **deux** lignes, celle de la tentative (auteur = le worker) et celle de la decision (auteur = l'humain) ; ce sont deux faits distincts et le journal etant append-only, aucun n'ecrase l'autre. Un refus n'ecrit **rien**. Corollaire : l'acceptation « aucune operation ne permet de modifier arbitrairement le payload » tient par **absence de chemin** (aucune route n'accepte `payload_json`, `type`, `priority`, `max_attempts`), pas par une validation. | Table `audit_events` dediee (une table de plus pour 2 actions, et l'historique du job se lirait a deux endroits), colonnes `cancelled_by`/`cancel_reason` sur `jobs` (ecrasees a la reprise suivante, donc pas un audit), journaliser dans les logs applicatifs (non requetable, perdu au redeploiement) |
| 2026-07-22 | **JOB-007 : les horodatages restent affiches en UTC, comme ils sont stockes** | Les colonnes temporelles sont des `text` UTC (`timestamps.ts`), et `jobs-inspect.ts` les rend telles quelles. Convertir en Europe/Zurich dans la console ferait exister **deux lectures d'un meme instant selon l'outil consulte** — precisement la famille de bug qui a coute le correctif transverse de FIND-001/JOB-001 (deux formats dans une meme colonne). L'interface affiche donc l'heure stockee et **le dit** (« UTC » a cote des valeurs). Corollaire pratique : les libelles et formats vivent dans `$lib/utils/job-format.ts`, consommes par la CLI **et** par les pages (une page Svelte ne peut pas importer `$lib/server`), pour qu'un `deferred` ne soit jamais « reporte » d'un cote et « echoue » de l'autre. | Convertir en Europe/Zurich cote page (plus confortable, mais l'heure d'un meme job differe entre CLI et console — 2 h d'ecart en ete), stocker un fuseau par ligne (change le schema pour un besoin d'affichage), afficher les deux (bruit dans une table dense) |
| 2026-07-22 | **JOB-005 : un creneau se nomme par son heure LOCALE, jamais par son instant UTC** | Toute la garantie anti-doublon du scheduler tient dans ce choix. La cle d'idempotence porte `2026-07-20T09:00` (Europe/Zurich), pas l'instant : le lundi 09:00 metier s'ecrit alors 08:00 UTC en hiver et 07:00 UTC en ete **sans changer de cle logique**, et le retour a l'heure d'hiver ne peut pas produire deux jobs puisque 02:30 local n'existe qu'une fois au calendrier — meme quand l'instant correspondant, lui, se presente deux fois. Corollaire : rejouer un tick, redemarrer a 09:00 et rattraper un creneau manque sont **la meme operation**, sans effet la deuxieme fois. La conversion locale→instant teste les DEUX offsets en vigueur autour du creneau et garde ceux qui se relisent : deux valides = creneau ambigu (on prend le premier, convention `compatible` de Temporal), zero valide = creneau inexistant (29 mars 02:30) qui **glisse a 03:30** plutot que d'etre escamote. | Nommer le creneau par son instant UTC (deux creneaux distincts pour un meme lundi 09:00 selon la saison, et un seul le jour du retour a l'heure d'hiver), une passe unique de conversion (fausse aux bordieres : l'offset a appliquer n'est pas celui de l'instant qu'on calcule), une lib (date-fns-tz/Temporal : `Intl` fait deja le travail, base ICU du runtime, zero dependance) |
| 2026-07-22 | **JOB-005 : aucune table de planification — l'etat, c'est la cle d'idempotence** | « Un restart a 09:00 ne cree qu'un run logique » n'est PAS garde par un `last_fired_at` : un tel champ peut diverger du calcul, se perdre au redeploiement, ou mentir apres une restauration. Il l'est par l'unique `(project_id, idempotency_key)` **deja en base** (`jobs_idempotency_unique`, `monitoring_runs_idempotency_unique`). La « prochaine execution par projet » est donc **calculee** a la demande, ce qui la rend structurellement incapable de contredire ce que le tick fera. Zero DDL (57 tables, zero derive). La config par projet passe par `project_projections.payload.schedules`, meme idiome tolerant que les seuils detecteurs (override corrompu → defauts, jamais de planification muette). | Table `job_schedules` (etat editable en UI, mais un second endroit ou la verite peut diverger du calcul, plus DDL + script apply), fichier de config versionne (pas de reglage par projet sans redeploiement) |
| 2026-07-22 | **JOB-005 : le cron perd sa semantique metier — il bat, le code sait l'heure qu'il est** | Revise la decision du 2026-04-27 (« cron toutes les heures avec dedup = gaspillage de quotas »), qui calait les crons sur l'heure CEST et acceptait un decalage d'un jour en hiver. JOB-005 exige d'etre exact sur les DEUX bascules DST : un creneau metier fixe demanderait deux entrees `vercel.json` saisonnieres a permuter deux fois l'an a la main (anti-pattern deja ecarte en avril). Le tick `0 * * * *` n'a donc plus aucune semantique metier, et il ne fait plus que dedupliquer : il sert **aussi** de worker et de reaper. C'est ce qui leve enfin « sur Vercel aucun worker permanent », repete comme piege depuis JOB-002 — un scheduler seul aurait rempli une file que personne ne vide, avec l'illusion que ca tourne. Le drain est borne (`once`, 25 jobs, budget 240 s sous `maxDuration: 300`) : la plateforme tue sans repasser par les `finally`. | Deux crons saisonniers (switch manuel 2x/an), cron unique cale sur une saison (decalage d'une heure l'autre moitie de l'annee), scheduler et worker sur deux routes distinctes (ordre non garanti, deux entrees a surveiller), worker externe permanent (infra a payer et a exploiter pour ~1 min de travail par heure) |
| 2026-07-22 | **JOB-005 : un run se conclut a l'issue TERMINALE de ses jobs, pas a chaque tentative** | Bug trouve en verifiant : un run planifie restait `queued` **a vie**, parce que `classifyRunOutcome` derive le statut d'un run de ses STEPS et que personne n'en ecrivait pour un job de queue — la supervision aurait montre des runs eternellement en attente au-dessus de jobs reussis. `concludeRunStep` ecrit le step et recalcule le run, mais **seulement** quand le job a fini son histoire (reussi, ou mort apres epuisement) : un job encore rejouable n'a rien conclu, et lui ecrire un `failed` ferait basculer son run en echec avant la fin de la partie. Ecriture non bloquante (comme la passe de reaper) : l'execution du job fait foi, sa comptabilite de run ne doit jamais pouvoir la faire echouer apres coup. Limite assumee : un run dont un job est en retry reste `queued` — le suivi fin d'un run **en cours** est l'affaire de JOB-004 (DAG + statut `partial`). | Ecrire un step a chaque tentative (le run passe `failed` puis `partial` au gre des retries, il raconte l'histoire du retry au lieu du resultat), step `queued` a la planification (le run parait `running` alors que rien ne tourne), ne pas ouvrir de run du tout (perd la tracabilite creneau→job, et `monitoring_runs` etait fait pour ca) |
| 2026-07-22 | **AGT-000 : le payload d'une proposition ne contient AUCUNE mesure — c'est lui qui porte la dedup** | `createProposal` deduplique sur `payload_hash`. Y mettre la position (7.4 cette semaine, 7.1 la suivante) ou les impressions produirait un hash neuf a chaque run hebdomadaire, donc une proposition neuve : **l'inbox doublerait de volume toutes les semaines**, sans erreur, sans log, sans test rouge. Le payload ne porte donc que l'IDENTITE et l'INTENTION (action, cible, finding, fingerprint, skill) ; les chiffres du moment vivent dans `rationale`/`expected_impact` — **hors du hash**, donc rafraichis a chaque run par le `onConflictDoUpdate` — et dans `input_hashes_json`. Corollaire : `createProposal` a du etre etendu pour rafraichir ces champs non hashes, sinon une proposition afficherait eternellement les mesures de sa premiere semaine ; l'approbation liee reste valide puisque le hash n'a pas bouge (§12.2). Un test dedie garde l'invariant : memes findings, mesures differentes → payload identique. | Inclure les mesures et deduper sur `(projet, finding, action)` seul (perd la distinction « la situation a change » vs « rien n'a bouge »), arrondir les mesures avant hachage (repousse le probleme au premier franchissement de palier), comparer les payloads en SQL a l'ecriture (refait a la main ce que l'unique fait deja) |
| 2026-07-22 | **AGT-000 : le niveau d'approbation sort d'une table figee, jamais d'un calcul ni d'un modele** | La SPEC §12.1 donne les niveaux par EXEMPLES (« plan de refresh » = L2, « modification contenu » = L3, « 301, canonical, suppression, desindexation » = L4) : ils sont recopies tels quels dans `APPROVAL_LEVEL_BY_ACTION`, et un `action_type` inconnu rend `null` (donc aucune proposition) au lieu d'un defaut permissif. Raison de fond : si un modele — ou meme une heuristique — derivait le niveau, alors le NIVEAU D'AUTORISATION deviendrait lui-meme negociable, c'est-a-dire qu'un agent pourrait s'accorder un L4. C'est exactement l'invariant que `canActorApprove` existe pour interdire. Meme logique pour le choix de l'action : `keyword_opportunity` rend UNE action tranchee par la position (<= 10 → `meta_rewrite` L3 ; > 10 → `refresh_plan` L2), pas deux, et un type de finding sans correspondance ne produit RIEN (compte en `withoutAction`). L'IA viendra redigier le rationale et regrouper les findings correles, sur une structure deja gouvernee (SPEC §3.3). | Deriver le niveau du risque calcule (le risque devient la variable qui ouvre les droits), laisser un LLM proposer action+niveau (l'agent choisit son propre plafond), un niveau par defaut pour les types inconnus (une action non prevue devient approuvable) |
| 2026-07-22 | **AGT-000 : le risque vient de l'ACTION, module par ce qu'il y a a perdre — pas de la gravite du finding** | Le plan prevoyait `deriveRiskLevel(actionType, severity)`. Ecarte : un finding critique ne rend pas plus risque d'ecrire un brouillon — ce serait dire « cette action est plus dangereuse parce que le probleme est pire », ce qui est l'inverse. Le risque est intrinseque au niveau (L2 `low`, L3 `medium`, L4 `high`), avec une seule modulation qui a un sens reel : reecrire le title d'une page a 0 clic ne peut rien faire perdre, le reecrire sur une page a 55 clics/semaine expose un acquis → `medium` monte a `high` au-dela de `riskyClicksThreshold` (10). Un `action_type` inconnu est traite comme le plus risque, jamais banalise. | Moduler par la severite du finding (inverse le raisonnement), risque purement intrinseque (ne distingue pas une page morte d'une page qui performe), calculer un score de risque (un chiffre non explicable la ou trois classes suffisent a decider) |
| 2026-07-22 | **AGT-000 : une proposition APPROUVEE devenue obsolete est remontee, jamais perimee en silence** | Quand un nouveau payload remplace l'ancien pour la meme action, `decideSupersession` ne perime que les propositions encore OUVERTES (`proposed`/`invalidated`). Une proposition `approved` dont le payload ne reflete plus la realite pose un vrai dilemme : la perimer efface une decision humaine, la laisser filer ferait executer une action sur un etat perime. Elle est donc renvoyee dans `staleApproved` — visible en CLI, intacte en base — et un humain tranche. Meme discipline pour `executing`/`executed`/`rejected` : une machine ne reecrit pas une decision prise. | Perimer aussi les `approved` (efface une validation humaine sans trace), ne rien perimer du tout (deux propositions vivantes ciblent la meme page et l'une des deux sera approuvee au hasard), invalider automatiquement via `updateProposalPayload` (techniquement possible, mais transforme une approbation explicite en rejet implicite) |
| 2026-07-22 | **AGT-000 : deux corrections transverses trouvees en verifiant DATA-006/007** | (1) `proposals.ts` et `policies.ts` ecrivaient leurs horodatages en **ISO** dans des colonnes dont le DEFAULT SQL est au format DB — le piege lexical documente par `timestamps.ts` (`'T'` 0x54 > `' '` 0x20), et `proposal-state.isApprovalValid` compare precisement ces chaines : deux formats melanges y font expirer une approbation au mauvais moment. Rien n'avait casse **parce que les tables etaient vides** ; AGT-000 est le premier a y ecrire. (2) Les deux modules importaient `db` **statiquement** (`db/index.js` → `$env/dynamic/private`), donc etaient inchargeables hors runtime SvelteKit — aucune preuve sur Neon n'etait possible. Passes au client injecte de `findings.ts` (import dynamique, parametre `client?` optionnel, appelants app inchanges). Dette nommee et NON corrigee : `observations.ts` ecrit `fetched_at` en ISO dans une table peuplee, sans comparaison lexicale aujourd'hui. | Ecrire les propositions via un module parallele compatible runner (duplique la logique d'ecriture et ses gardes), convertir les formats a la lecture (le desordre reste en base et chaque nouveau lecteur doit y penser), corriger `observations.ts` dans la foulee (table peuplee = migration de donnees, hors perimetre d'un lot sans DDL) |
| 2026-07-22 | **JOB-004 : la garde de dependance vit DANS la reclamation, et elle bloque aussi les prerequis obligatoires DEJA MORTS** | Mettre le controle dans le worker aurait rendu la promesse dependante de chaque appelant : un futur runner qui reclamerait autrement l'aurait perdue en silence. La condition est donc dans le `SELECT ... FOR UPDATE SKIP LOCKED` de `claimJob` — structurelle, subie identiquement par deux workers concurrents. Elle a DEUX moities, et la seconde n'est pas cosmetique : bloquer aussi les jobs dont un prerequis obligatoire est mort/annule/saute/introuvable. Sans elle, un dependant dont le prerequis vient de mourir deviendrait reclamable AVANT que `settleBlockedJobs` ne l'ait conclu — une course, et le job tournerait sur des donnees absentes. Corollaire impose : `enqueueJob` serialise lui-meme `depends_on` (type `JobDependency[]`, plus jamais du texte libre), parce que la garde casse la colonne en `jsonb` et qu'une valeur malformee ferait echouer la reclamation de TOUTE la file. | Verifier les dependances dans le worker apres reclamation (perdue des qu'un appelant l'oublie ; et le job a deja consomme une tentative), une table `job_dependencies` (DDL pour une file de quelques dizaines de lignes, et un second etat qui peut diverger de `depends_on`), ne bloquer que sur les prerequis en cours (ouvre la course avec la passe de resolution) |
| 2026-07-22 | **JOB-004 : un prerequis obligatoire mort donne un statut `skipped`, distinct de `cancelled`** | Il fallait un statut TERMINAL, sinon le dependant restait `queued` a vie — non reclamable, invisible, et son run inachevi avec lui. Reutiliser `cancelled` etait tentant (zero vocabulaire nouveau) mais faux : `cancelled` est une decision HUMAINE, avec un acteur et une raison au journal (JOB-007), et `explainFailure` aurait annonce « annule par un operateur » la ou personne n'a rien decide — envoyant l'operateur relancer le mauvais job. `skipped` (colonne `text`, zero DDL) porte sa propre cause `DependencySkipped`, nomme le prerequis manquant, ne consomme AUCUNE tentative, et ecrit un step `skipped` qui fait basculer le run en `partial`. Il est REPRENABLE a la main (`requeueDeadJob` l'accepte) : rien ne ressuscite un statut terminal, meme prerequis repare, et sans cette reprise le skip rouvrait le cul-de-sac que JOB-003 avait ferme pour la dead-letter. | Reutiliser `cancelled` (confond consequence mecanique et decision humaine, fait mentir le verdict d'operateur), laisser le job `queued` indefiniment (invisible, run jamais conclu), un statut `blocked` persiste (deux etats qui peuvent diverger la ou l'attente se recalcule en une requete) |
| 2026-07-22 | **JOB-004 : le lien `detect` -> `propose` du catalogue hebdo est OBLIGATOIRE (decision de Jonathan)** | Jusqu'ici `priority: 10` contre `8` n'etait qu'un ordre de SERVICE : deux workers, ou un detecteur reporte pour quota, et le producteur travaillait sur les findings de la semaine precedente — sans doublon (il est idempotent) mais sans rien dire du decalage. Avec une arete obligatoire, une detection morte fait SAUTER le producteur et le run vaut `partial` : le trou de la semaine devient visible, au lieu d'etre masque par des propositions fondees sur des mesures perimees. Cout assume : pas de propositions cette semaine-la. L'alternative optionnelle gardait l'inbox vivante au prix d'un mensonge par omission. Un prerequis OPTIONNEL reste possible et ne bloque personne — c'est la forme qu'auront les providers non essentiels (Plausible, SPEC §8.2). | Arete optionnelle (l'inbox continue de vivre, mais sur des mesures perimees et sans que rien ne le signale), garder le simple ordre de priorite (le decalage reste silencieux), faire echouer le producteur au lieu de le sauter (un echec suggere un bug a corriger, alors que rien n'a ete tente) |
| 2026-07-22 | **JOB-004 : le dernier verdict d'un step se trie par le TEMPS, jamais par le numero de tentative** | Trouve en ecrivant les tests de `latestAttemptPerStep`. Le reflexe — « la tentative la plus haute gagne » — est faux ici, parce que `requeueDeadJob` remet `attempts` A ZERO (decision JOB-003) : la tentative qui reussit apres une reprise porte un numero PLUS PETIT que celle qui est morte. Trier sur `attempt` aurait fige le verdict d'echec pour toujours. C'est `finished_at` qui fait foi, `attempt` ne servant que de departage — et le comparateur normalise le separateur (`'T'` 0x54 > `' '` 0x20) pour qu'une ligne ISO egaree ne se trie pas apres toute la journee. Ce tri corrige un defaut ANTERIEUR au lot : `recomputeRunStatus` agregeait toutes les lignes de `monitoring_steps`, donc un job mort puis repris et reussi laissait son run `partial` a vie. | Trier par `attempt` (fige le verdict d'echec des qu'une reprise a eu lieu), ne garder qu'un step par type en ecrasant (detruit le journal des tentatives, que JOB-002 existe pour construire), comparer les horodatages sans normaliser (rejoue le piege lexical documente par `timestamps.ts`) |
| 2026-07-23 | **JOB-006 : une table `system_settings` pour les limites, et le « zéro DDL » n'était pas une règle** | L'acceptation exige des limites « configurables sans redéploiement ». Sur Vercel une variable d'environnement n'est relue **qu'au redéploiement** : elle ne peut pas la tenir. `gmb_settings` est pourtant déjà un KV (`key`/`value`) et sert déjà à des clés non-GMB (`critical_sent_*`) — mais y ranger les quotas GSC ferait mentir son nom à la prochaine lecture. Le « zéro DDL » de JOB-004/005/007 était un **constat** (« tout était là depuis DATA-003 »), pas une doctrine : ici il n'y avait rien. La table naît **vide**, et c'est ce qui rend le DDL sans risque : sans ligne, `resolveLimits` rend les défauts du code, donc appliquer la migration ne change **aucun** comportement — c'est y écrire qui en change un. Portées séparées : le **système** ici, le **projet** dans `project_projections.payload.limits` (calque des `schedules` de JOB-005). | Variables d'environnement (ne tiennent pas l'acceptation sur Vercel), réutiliser `gmb_settings` (zéro DDL mais le nom ment sur le contenu), garder les plafonds globaux en constantes de code (acceptation tenue à moitié, et un incident de quota exigerait un déploiement pour desserrer) |
| 2026-07-23 | **JOB-006 : le refroidissement provider est DÉRIVÉ de `job_attempts`, jamais persisté** | Un échec classé `quota` met **tout le provider** au repos pour sa cohorte, au lieu que chaque job redécouvre le 429 un par un. L'état de repos n'est stocké nulle part : il se recalcule depuis le dernier `error_class='quota'` du journal — table append-only déjà écrite à chaque tentative. Même raison qu'au « zéro table de planification » de JOB-005 : deux états qui peuvent diverger valent moins qu'un seul qui se recalcule. Corollaire assumé : ce lot **n'écrit pas** `project_integrations.health_status = 'quota_limited'` (SPEC §17.1), parce que ce serait un second état persistant dont personne ne serait clairement propriétaire du retour à `healthy` ; le statut est **dérivé et affiché** comme tel. Dette nommée, laissée à OPS-002. **Limite assumée** : le budget de fenêtre compte des **jobs**, pas des **appels API** (une détection = N appels GSC) — c'est un garde-fou grossier, la vraie prévention est le refroidissement, et le compte fin viendra avec les collecteurs E03 quand quelque chose pourra réellement l'écrire. | Une table `provider_usage` écrite par les collecteurs (exacte au niveau appel, mais **les collecteurs sont E03** : une table vide qui promet une précision qu'elle n'a pas — le piège exact d'AGT-000, un module complet que personne n'appelle pendant deux lots), écrire `health_status` (second état, propriétaire du reset indéfini), réagir seulement au 429 sans rien mesurer (ne tient pas le « prévenir » du backlog) |
| 2026-07-23 | **JOB-006 : l'équité est un TOUR qui se rouvre, pas un quota de drain** | Chaque projet prend au plus `perProjectPerLap` jobs ; quand **tous ceux qui ont du travail servable** ont eu leur part, un nouveau tour s'ouvre et les exclusions d'équité tombent. Un simple quota par drain aurait laissé le tick s'arrêter avec du budget restant et une file pleine — pire qu'un tick déséquilibré. La réouverture est calculée dans le module **pur**, donc un test la ferme. Deux subtilités payées comptant : un projet **occupé** (à son plafond de concurrence) ne compte pas parmi ceux « à servir », sinon il empêcherait la réouverture sans que rouvrir ne le libère ; et `projectsWithClaimableWork` doit filtrer sur **les types que le worker sait traiter** — sans ce filtre (bug trouvé par la preuve), un projet dont le seul travail est d'un type non géré figeait le tour **pour toujours** et affamait les projets réellement servables. | Un quota fixe par drain (tick qui s'arrête avec du budget et de la file), un round-robin strict par projet (impose un ordre là où la priorité doit rester le premier critère), pas d'équité du tout (un site volumineux repousse les cinq autres d'une heure — l'acceptation n'était tenue par rien) |
| 2026-07-23 | **JOB-006 : la garde de capacité vit DANS `claimJob`, et un bail EXPIRÉ ne compte pas comme « en cours »** | Comme la garde de dépendance de JOB-004 : les restrictions descendent dans le `SELECT ... FOR UPDATE SKIP LOCKED` plutôt que de rester dans le worker — aucun appelant futur ne peut les oublier, et deux workers concurrents les subissent tous les deux. Prouvé en base par contraste : le même job est refusé **avec** la garde et réclamé **sans**, alors qu'il est parfaitement disponible (sans cet ordre, un refus après la passe de refroidissement s'expliquerait tout autant par un `available_at` poussé, et la preuve aurait confondu deux mécanismes). Second point, trouvé en vérifiant : `status='running'` ne suffit pas à compter la concurrence — un job dont le **bail a expiré** ne tourne nulle part (c'est la définition de l'abandon, JOB-002), le compter occuperait une place au nom d'un worker mort, et quelques workers morts suffiraient à geler la file jusqu'au passage du reaper. Enfin, un report de capacité ne touche **ni `attempts` ni `deferrals`** et n'écrit aucune tentative : ces jobs n'ont pas été réclamés, ils n'ont rien tenté. | Vérifier la capacité dans le worker après réclamation (perdue dès qu'un appelant l'oublie, et le job a déjà consommé une tentative), compter tous les `running` (une poignée de workers morts gèle la file), réutiliser `deferJob` de JOB-003 (exige un bail, donc un job réclamé — et ferait consommer un report à un job qui n'a rien fait) |
| 2026-07-23 | **JOB-006 : `0` vaut « pas de limite », et un type inconnu vaut `none`** | Deux défauts qui vont dans des sens opposés, chacun choisi pour son mode de panne. (1) Un plafond à `0` signifie **aucune borne**, jamais « zéro job autorisé » — même sémantique que `maxJobs`/`reapLimit` — et une valeur illisible ou hors bornes retombe sur le **défaut du code**. Une file arrêtée par une limite corrompue serait le pire mode de panne : celui qui ne se plaint pas. `formatQuota` rend donc `3/∞` et jamais `3/0`. (2) Un type de job absent de `PROVIDER_BY_JOB_TYPE` vaut `none` : il reste soumis aux plafonds global et projet, mais aucun budget provider ne le bloque. C'est le choix **inverse** de `required` en JOB-004, et délibéré — là-bas relâcher la garde sur ce qu'on n'avait pas su lire faisait tourner un job sur des données absentes ; ici la resserrer empêcherait **tout nouveau handler** de démarrer. Corollaire d'exploitation : un worker bridé ne doit jamais se lire comme un worker inactif, d'où `throttledTicks`. | Traiter `0` comme un plafond réel (une config vide éteint la file en silence), rabattre une valeur hors bornes sur la borne la plus proche (fait croire le réglage appliqué — `scripts/limits.ts` annonce au contraire ce qui a été ignoré), traiter un type inconnu comme soumis à tous les budgets (aucun nouveau handler ne peut démarrer sans toucher la table) |
| 2026-07-23 | **DASH-005 : l'approbation est OPTIMISTE (hash attendu) et IDEMPOTENTE dans la transaction** | « Chaque approbation est liée au hash exact » ne tient pas sous concurrence si l'appelant approuve « la version courante, quelle qu'elle soit » : entre l'affichage et le clic, le run hebdomadaire peut avoir périmé la proposition. L'écran renvoie donc le `payloadHash` **qu'il a montré**, `approveProposal` le compare sous transaction et refuse (`stale_hash` → 409) **sans rien écrire** ; obligatoire côté endpoint, optionnel côté fonction (le chemin agent vient d'écrire le payload et n'a rien à comparer). Bénéfice gratuit : l'acceptation « modifier une proposition l'exclut du lot » est obtenue par la **même** comparaison, sans aucune règle d'interface. Second point : si la proposition est déjà `approved` avec une approbation `active` sur le même hash, l'existante est **rendue sans insertion** — sinon un double clic écrit deux lignes dans `proposal_approvals`, et §14.3 fait de cette table la trace de référence : l'audit dirait deux décisions là où un humain n'en a pris qu'une. Enfin, une garde de **statut** a été ajoutée : sans elle, une proposition `rejected` par un humain repassait `approved` au simple rappel de la fonction — une machine réécrivant une décision humaine, chaque semaine, en silence (jamais mordu : aucun projet n'a de policy, donc l'appel n'a jamais lieu). | Approuver le hash courant sans le comparer (rouvre la course, et l'acceptation devient invérifiable), dédupliquer la double soumission côté HTTP (une clé d'idempotence de plus à faire vivre, et deux workers concurrents la contournent), laisser `approveProposal` sans garde de statut (la machine réécrit les décisions humaines) |
| 2026-07-23 | **DASH-005 : « request changes » est un STATUT (`changes_requested`), pas un éditeur de payload** | Le backlog demande « approve, reject et request changes » alors que `PROPOSAL_STATUSES` n'avait rien pour la révision. Éditer le payload à la main — l'option évidente, `updateProposalPayload` existe déjà — était un piège à deux détentes : l'édition serait **`superseded` au lundi suivant** (`decideSupersession` périme tout `proposed` dont le hash a changé, donc la version humaine face à la version canonique du producteur), et tout champ volatil ajouté à la main ferait **exploser la dédup** (le piège central d'AGT-000). Le statut, lui, a un effet de bord voulu : `decideSupersession` ne touchant que `proposed`/`invalidated`, une révision demandée **survit au run hebdomadaire** — prouvé en base. Colonne `text` : zéro DDL. La raison va dans `finding_events` (`agent_comment`, acteur `user:{email}`), seul journal disponible ; conséquence nommée : une proposition sans `finding_id` (rapports, REP-001) n'aura nulle part où l'écrire. | Éditeur de payload brut (édition écrasée au run suivant, dédup en danger), rejeter avec un motif « révision » (confond une demande ouverte avec une décision close, et `rejected` est terminal), ajouter un type d'événement de finding pour une décision de proposition (mélange deux vocabulaires) |
| 2026-07-23 | **DASH-005 : le lot est reconstruit DEPUIS LA BASE, et L4 est exclu dans le module pur** | `approve-batch` ne fait jamais confiance à la liste que le client envoie : il relit les lignes, rejoue `buildApprovalLots` et refuse si les ids ne forment pas **exactement un** lot homogène (même projet, action, niveau, risque). Une page modifiée, une requête forgée ou un onglet resté ouvert pendant le run hebdo ne peuvent donc pas faire approuver ensemble deux projets ou deux niveaux. L'exclusion des **L4** vit dans le module **pur** — testée — et non dans un `{#if}` de template : §12.1 en fait la classe des gestes irréversibles (301, canonical, suppression, désindexation), une validation groupée est une décision prise sur un résumé, donc le bouton **n'existe pas** plutôt qu'il n'existe et refuse. Corollaires : un risque `null` forme sa propre classe (`inconnu`, jamais mélangé à `low` — ne pas savoir n'est pas savoir que c'est faible), un lot d'un seul élément n'est pas un lot, et la réponse rend **toujours** le détail par item (un lot qui « réussit » en taisant deux items refusés se lit comme un lot complet). | Faire confiance aux ids du client (deux projets ou deux niveaux approuvés ensemble), masquer le bouton côté écran seulement (contourné au premier refactor de template ou par un POST direct), rendre un statut global sans détail par item (silence sur ce qui n'a pas été approuvé) |
| 2026-07-23 | **Réparation de données : recollecter la semaine, mais NE PAS rejouer la détection à la main** | La semaine `2026-07-06` était sous-comptée de ~35 % sur 5 projets sur 6 (tirée un jour après sa fin, avant que `latestCompleteWeekStart` n'impose 8+ jours). Un seul pull des 6 projets l'a réparée (+28 670 impressions) — sans `--dry-run` préalable, qui aurait consommé le **même** quota sur le compte partagé pour la même donnée. `spinlink` ressort **rigoureusement inchangé** : c'est la contre-épreuve du diagnostic, mesurée et non supposée. Deuxième geste, gratuit : le diff hebdo de `jonlabs` pour `2026-07-13` avait été calculé contre l'ancienne base et affirmait **+84,3 %** d'impressions ; recalculé (DB seule, zéro quota) à **+35,3 %** — le faux signal que GSC-006 interdit, dans son sens inverse. En revanche la **détection n'a pas été rejouée** : `detect.ts` à la main écrirait des findings hors du chemin gouverné par la file (sans `run_id` de cadence, sans plafond de capacité, sans dépendance) et ferait partir les 50 findings de `barberconcept` sans que la décision ait été prise. Le run hebdo re-décidera sur la donnée réparée. | Laisser la semaine sous-comptée (le détecteur décide chaque semaine sur une base fausse, et l'écart grandit), dry-run puis pull réel (double quota pour la même donnée), rejouer `detect.ts` dans la foulée (findings écrits hors file, et 50 findings partis sans décision) |
| 2026-07-25 | **DASH-002 : deux axes de santé qui ne fusionnent JAMAIS, et un pipeline cassé rend le signal `unknown`** | L'accueil devait « distinguer une intégration cassée d'une baisse de performance ». Un badge unique aurait fait exactement ce que l'acceptation interdit : confondre « le SEO baisse » et « le credential est mort », qui demandent des gestes opposés. D'où `pipeline` (la donnée arrive-t-elle ?) et `signal` (que dit-elle ?) tenus séparés jusqu'à l'écran, chaque carte portant **une phrase qui nomme l'axe**. Le point dur est la seconde règle : quand le pipeline est cassé, le signal devient **`unknown` et non `ok`** — sans elle, un projet dont la collecte est morte afficherait « 0 nouveau finding » et se lirait comme **le plus sain du portefeuille**, le pire mode de panne, celui qui ne se plaint pas. Même doctrine que §10.3 (« une fenêtre absente n'est pas des zéros ») portée à l'échelle du projet. Corollaire de tri : `unknown` se classe **avant** `watch` — ne pas savoir est plus urgent qu'un signal faible connu. | Un score de santé unique (confond les deux causes, c'est l'acceptation même), signal `ok` par défaut quand rien n'est détecté (une collecte morte devient le meilleur élève), `unknown` en fin de liste (le projet muet, seul capable de tout cacher, sort de l'écran) |
| 2026-07-25 | **DASH-002 : le nombre d'un compteur et son lien naissent du MÊME descripteur ; sinon il n'a pas de lien** | L'acceptation « chaque compteur ouvre une liste filtrée cohérente » est décorative si le compteur est calculé d'un côté et l'URL écrite de l'autre : les deux dérivent au premier changement de filtre. `buildCounter` produit donc les deux depuis un `CounterFilter` unique, et le read-model **compte avec ce descripteur** ; la preuve ne compare pas deux nombres du même code, elle **part de l'URL** et la rejoue comme le loader de l'inbox. Corollaire assumé : un compteur qu'aucune liste existante ne sait reproduire (avis — aucune vue cross-projet ; runs d'une période — SPEC §13.4 la fera) reste un **chiffre sans lien**, plutôt qu'un lien qui mènerait ailleurs. A nécessité d'ajouter le filtre d'activité `activityEvents`/`activitySince` à `listFindings`/`countFindings`, en **EXISTS** sur `finding_events` : une jointure dupliquerait la ligne autant de fois qu'elle a d'événements et ferait mentir le total. Et le compteur compte des **`DISTINCT finding_id`**, pas des lignes de journal — sinon un finding aggravé deux semaines de suite annonce « 2 aggravés » pour un problème (bug latent trouvé en écrivant la preuve ; `reconcileDetectionRun` écrit un `aggravated` par run, donc toute fenêtre 28/90 j le rencontrait). | Compteur et lien écrits séparément (divergent au premier refactor), lier tous les compteurs quitte à approximer la cible (un lien qui mentit est pire qu'un chiffre muet), filtrer sur `first_seen_at`/`resolved_at` plutôt que sur les événements (ces colonnes ne couvrent pas `aggravated`, et mélanger deux sources fait diverger deux compteurs de la même ligne) |
| 2026-07-25 | **IDX-001 : une table additive `sitemap_url_observations`, en SNAPSHOT complet par date et non en journal de changements** | IDX-001 exige un inventaire **par URL** (lastmod, locale, canonical attendu) qu'aucune table ne portait : `sitemap_observations` a la forme de l'API Sitemaps de GSC (submitted/indexed/errors/pending), soit du **par-fichier**, et le `payload_json` d'une observation est plafonné à 32 Ko — un inventaire n'y tient pas. Le « zéro DDL » des derniers lots était un **constat, pas une règle** (rappel JOB-006). Le choix du snapshot complet contre le journal est ce qui tient l'acceptation « un sitemap diff est reproductible et lié à deux snapshots » **au sens littéral** : `diffInventories` devient une fonction **pure de deux listes**, rejouable indéfiniment ; un journal cumulé ne se rejoue pas, il faudrait le replier depuis l'origine et espérer n'avoir rien perdu. Volume réel : ~6 projets × quelques centaines d'URLs × 52 semaines. ⚠️ L'unique porte **`url_normalized`** et non `url` : sinon `…/page` et `…/page#a` créeraient deux lignes pour une seule page, et le diff **inventerait un ajout à chaque run**. `sitemap_url` est en revanche dans le `set` d'upsert — une URL peut migrer d'un sitemap enfant à un autre lors d'une refonte sans cesser d'être la même page. | Journal état-courant + événements (moins de lignes, mais le diff cesse d'être rejouable — l'acceptation elle-même), tout mettre dans `payload_json` (32 Ko, impossible), ne stocker que le par-fichier (l'acceptation « diff reproductible » n'est pas tenue : sans inventaire, il n'y a rien à diffuser) |
| 2026-07-25 | **IDX-001 : rien n'est écrit avant que TOUT l'arbre soit parcouru, et un sitemap injoignable est un fait persisté** | Le legacy `fetchSitemapUrls` fait `catch {}` sur chaque sous-sitemap : « signaler les sitemaps invalides ou inaccessibles » y est donc **impossible par construction**, pas seulement absent. Il récurse aussi sans borne (un index auto-référent tourne jusqu'au timeout du worker) et jette lastmod/hreflang. Ici chaque fichier obtient sa ligne avec `errors > 0` et son statut HTTP — **interrogeable en SQL** — et un enfant mort n'annule pas ses frères (`partial: true`). La garde d'écriture est le pendant nécessaire : un inventaire coupé au 2ᵉ fichier sur 5 se lirait comme complet, et le run suivant annoncerait des dizaines de **retraits fantômes**. Ce n'est pas théorique — la preuve l'exécute : un run tronqué à 1 URL annonce **4 retraits qui n'ont jamais eu lieu**. Enfin, un retrait d'URL ne déclenche **rien** : c'est une observation, au plus un finding IDX-005, jamais une désindexation (tenu par construction — ce module ne sait pas soumettre ; vérifié par `indexing_submissions` inchangée). Le legacy reste en place pour les routes manuelles : dette datée, comme la double écriture GSC-002. | Réutiliser `fetchSitemapUrls` (l'acceptation est structurellement hors d'atteinte), écrire au fil du parcours (retraits fantômes au run suivant), faire échouer tout le run sur un enfant mort (un sitemap secondaire cassé masquerait l'inventaire entier) |
| 2026-07-25 | **IDX-002 : une erreur provider et un « non indexé » sont deux TYPES, pas deux valeurs du même** | Le legacy `inspectUrl` `catch` l'erreur et rend `{ verdict: null, coverageState: null, httpStatus: 0, error: '…' }`, que `classifyIndexStatus` classe `unknown` — **exactement comme une page réellement inconnue de Google**. Deux situations qui demandent des gestes opposés (réparer l'auth vs travailler la page) étaient indistinguables, et l'acceptation « distinguer erreur provider et résultat non indexé » invérifiable. `InspectionOutcome` est donc une **union discriminée** dont les branches ne partagent aucun champ exploitable : aucun appelant ne peut les confondre. Prouvé en base : 7 tentatives en erreur → **0 observation** et URL absente ; page vraiment non indexée → **1 ligne classée `not_indexed`**. Deuxième moitié : le legacy jetait une **chaîne**, où `classifyJobFailure` ne trouve ni `status` ni `reason` — un quota mal classé part en **dead-letter permanente**. En passant par `toGscApiError` (déjà écrit pour GSC-002), les 7 classes sont exactes, dont les deux que Google fait à l'envers. Une réponse 200 illisible forme un **3ᵉ état nommé** (`unreadable`) : rien écrit, lot poursuivi. | Rendre un résultat aux champs nuls sur erreur (l'acceptation devient invérifiable, et c'est le bug d'origine), absorber l'erreur URL par URL (les 199 suivantes brûlent contre un mur et le refroidissement JOB-006 n'apprend rien), écrire des `null` sur réponse illisible (se lit comme « Google ne connaît pas cette page ») |
| 2026-07-25 | **IDX-002 : écriture INCRÉMENTALE — l'inverse assumé de GSC-002 — et pas de mise au catalogue hebdo** | GSC-002 n'écrit rien avant la fin de la pagination, parce qu'une semaine partielle se **lit comme complète**, donc comme une chute. Le raisonnement ne se transporte pas ici : une URL inspectée est un fait **autonome**, elle ne prétend rien sur les autres et rien ne la compare à un total attendu. Écrire au fil de l'eau évite donc de perdre 199 inspections **payées au quota** parce que la 200ᵉ a échoué. Mais une erreur provider **interrompt et remonte structurée**, pour que le refroidissement morde sur toute la cohorte `gsc` (même service account que Search Analytics — un 429 d'inspection met donc aussi la collecte GSC au repos, ce qui est voulu sur un compte partagé par 6 projets). Second point : `collect:url_inspection` est **délibérément absent du catalogue hebdo**. Sans IDX-004, personne ne sait quelles URLs méritent le quota (2 000/j/propriété) ; un job planifié avec une liste vide tournerait pour rien chaque lundi. Les URLs viennent du **payload**, plafonnées à 200, et un payload sans `urls` le **journalise** au lieu d'inventer une sélection — installer ici une règle implicite (« les N premières du sitemap ») obligerait IDX-004 à la défaire. | Tamponner puis écrire comme GSC-002 (perd 199 inspections payées pour une 200ᵉ en échec, sans bénéfice : aucun total attendu ici), absorber le quota et continuer (refroidissement inopérant), enfiler le job au hebdo avec les URLs du sitemap (politique de fait qu'IDX-004 devra défaire, et un cap arbitraire décide en silence quelles pages ne sont jamais inspectées) |
