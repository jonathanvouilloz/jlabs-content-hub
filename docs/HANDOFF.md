# HANDOFF — 2026-07-22 (DATA-007)

## Features actives
| Feature | Fichier | Statut |
|---|---|---|
| Reconstruction agentique — E00 fondations | [features/e00-fondations-cockpit.md](features/e00-fondations-cockpit.md) | **EN COURS** |
| Migration Turso → Neon | [NEON-MIGRATION.md](NEON-MIGRATION.md) | EN ATTENTE (reste P6, reporté) |

Cadre produit : [SPEC.md](SPEC.md) · [BACKLOG.md](BACKLOG.md) · décision UI → [DECISIONS.md](DECISIONS.md) (2026-07-21).

## Reprendre ici
E00 sur `feat/cockpit` — fondations + IDX-008 + DATA-001→007 + MIGRATE/backfill exécuté en DB réelle. **DATA-007 FAIT (2026-07-22)** : `review_automation_policies` + `policy_promotions` (SPEC §7.10) posées et **appliquées sur Neon**. Politiques d'avis & d'automatisation **versionnées** (même modèle que `project_projections` : une seule courante par scope, l'ancienne passe `superseded`) → **aucune ancienne proposition ne profite silencieusement d'une nouvelle policy**. Mode `draft_only|guarded_auto|manual`, **kill switch** (global ou par localisation via `scope_key = location_id ?? '*'`), note minimale, délai+jitter, plages horaires, catégories d'escalade. Invariants dans le module pur `policy-state.ts` : **`evaluatePolicyGates`** (le kill switch bloque les envois **sans** bloquer la sync) + **`canAutoSendReview`** (§8.4 : draft_only/manual jamais, guarded_auto seulement 5★ non escaladé). Toute promotion est **journalisée append-only** (`policy_promotions` : from/to version+mode, kind, acteur, cause → policy effective auditable). Write-helper `policies.ts` (`promotePolicy` idempotent transactionnel dédup par `policy_hash` sha256, `setKillSwitch` = promotion journalisée, `getEffectivePolicy`). `npm run test` = **144/144** · `check` = **0 err / 42 warn** · introspection = **52 tables, zéro dérive**. **Prochain = DATA-008** (rétention/purge, débloqué : agrégats semaine/mois/année avant purge, 24 mois de détail, dry-run+métriques+reprise), puis la chaîne agentique aval (1er détecteur + agent réel qui produit findings→proposals gouvernés par ces policies). **GMB-005** (application réelle des modes/kill switch au flux review-reply) reste **BLOCKED** mais débloqué côté données. **CONTRACT différé** (l'app lit encore `gsc_query_page_data` + `gmb_insights_daily`). `ai_jobs → jobs` reste **écarté** (voir feature file § Décisions).
Commits : `256c1a2` IDX-008 · `d7484a9` DATA-001 · `b6df05e` DATA-002 · `1ab115f` DATA-003 · `7d3ae9c` DATA-004 · `4696919` migrate/backfill · `7cb94c1` fix chunk · `f9432ce` DATA-005 · `4c24bc9` docs DATA-005 · `16fa000` DATA-006 · `43fe9d7` docs DATA-006 · _(DATA-007 : à committer)_.
