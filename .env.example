# Database (Neon Postgres — schéma `seostats` de la base partagée avec invoices)
DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require

# Auth (Better Auth)
BETTER_AUTH_SECRET=xxx
BETTER_AUTH_URL=https://hub.jonlabs.ch
# Optional : URLs supplementaires acceptees (comma-separated) si tu accedes au hub via plusieurs domaines
BETTER_AUTH_TRUSTED_ORIGINS=https://jlabs-content-hub.vercel.app,https://hub.jonlabs.ch

# API
API_KEY=xxx

# Chiffrement (tokens OAuth stockés) — 32 bytes hex
ENCRYPTION_KEY=xxx

# Google OAuth (GSC + GMB)
GOOGLE_CLIENT_ID=xxx
GOOGLE_CLIENT_SECRET=xxx

# LinkedIn OAuth
LINKEDIN_CLIENT_ID=xxx
LINKEDIN_CLIENT_SECRET=xxx

# App
PUBLIC_APP_URL=https://hub.jonlabs.ch

# Notifications email (Resend) — FROM_EMAIL doit etre sur un domaine verifie dans Resend
RESEND_API_KEY=re_xxx
ADMIN_EMAIL=contact@jonlabs.ch
FROM_EMAIL=hub@factures.jonlabs.ch

# Vercel Blob (images GMB)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx

# Cron (Vercel envoie automatiquement Bearer ${CRON_SECRET} sur les routes /api/cron/*)
CRON_SECRET=xxx

# LLM pour synthèse IA des rapports d'avis Google (API OpenAI-compatible)
# Par défaut : Kimi K2.6 via Moonshot AI
# Pour basculer sur un autre provider : set LLM_BASE_URL et LLM_MODEL
LLM_API_KEY=sk-xxx
# LLM_BASE_URL=https://api.moonshot.ai/v1   # default
# LLM_MODEL=kimi-k2.6                        # default

# Observabilité (OPS-001) — niveau de log : debug | info | warn | error (défaut info)
# LOG_LEVEL=info

# Feature flags de migration (GOV-005) — tous OFF par défaut.
# Activer une verticale sur un canary : FLAG_<NOM>=true
# Flags disponibles : JOBS_V2, FINDINGS, INDEXNOW, PLAUSIBLE, GMB_AUTO_SEND, TELEGRAM, AGENT_RUNNER
# FLAG_INDEXNOW=true
